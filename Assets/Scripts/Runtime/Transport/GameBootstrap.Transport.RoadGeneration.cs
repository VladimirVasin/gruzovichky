public partial class GameBootstrap
{
    private void GenerateInitialRoadNetwork()
    {
        SessionDebugLogger.Log("ROAD", $"Initial road network skipped for {selectedGameStartMode}: player starts without generated city roads.");
    }
}
