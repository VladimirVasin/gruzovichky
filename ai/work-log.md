# Work Log

Last updated: 2026-05-17

Purpose: compact active memory for recent work. Older detailed history was intentionally collapsed on 2026-04-20, 2026-05-03, 2026-05-08, and 2026-05-09 to keep agent startup light. Use git history for exact old implementation details.

## Active Queue

- No active queued memory items.

## Recent Work

- 2026-05-17: Added walking leg motion for imported adult residents. Imported citizen FBX rigs now create left/right leg motion pivots from their existing `Leg_*`, `Boot_*`, and `Knee_*` parts, so the shared adult `ApplyDriverPose()` walk cycle animates legs for all race/gender imported models while preserving procedural resident leg animation and the imported hair/cloth/glow secondary motion. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Hid technical imported-building ground marker meshes after they are used for grounding. `GroundBase` / `GroundPatch` renderers now still participate in imported model scale/height placement but are disabled for regular imported building visuals including Logging Camp, Sawmill, Gas Station, Canteen, Furniture Factory, Kiosk, schools, Labor Exchange, Cleaning Depot, and Personal Houses; surface-led models such as Parking, City Park, Docks, and Car Market are left visible. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Enlarged truck visuals again and upgraded truck driving animation. Imported trucks now target `1.59 x 1.27 x 2.12` world units with matching procedural fallback proportions, larger wheel radius, and cabin/body/cargo motion pivots. Normal driving and purchased-truck arrival now share a cartoon suspension animation where the visual root bobs while body, cabin, headlights, and visible cargo shake independently. Tutorial scenario was checked; this is visual-only and does not alter flow. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Fixed Parking truck bay placement. Imported Parking now registers `P_TruckParkingBay_XX_Center` markers from the FBX, truck spawn/purchase completion uses the slot marker position and orientation, and trucks that return to Parking snap from the drive anchor into their assigned bay instead of piling up on the shared Parking anchor. Fallback procedural slot placement now respects the building anchor orientation. Tutorial scenario was checked; Parking still provides vehicles automatically and no Tutorial flow/copy changes were needed. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-17: Fixed the `debug.log` performance/log-spam regression around active worker thoughts. Active thoughts that are still pending now refresh the existing pending entry in place instead of re-running the opinion-bias/influence formation pipeline every frame, `THOUGHT_BIAS` diagnostics are verbose-only, and the imported truck prefab is cached/preloaded during truck setup so first runtime truck provisioning no longer pays the `Resources.Load` cost in gameplay. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Increased the imported Furniture Factory visual by 70%. `FurnitureFactoryImportedModelFootprintFill` moved from `2.35` to `4.00`; placement footprint, walk buffers, road access, staffing, production, and logistics are unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Replaced procedural ambient squirrels with the imported `Assets/Resources/Misc/squirel.fbx` visual, keeping the procedural squirrel as fallback. The imported squirrel is fitted below cat size (`0.24 x 0.28 x 0.38` target versus the cat's larger target), has stable Unity model metadata, and now uses dedicated head/tail/leg rig pivots for idle, foraging, running, and climbing animations. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Connected the imported City Park model. `Assets/Resources/Buildings/citypark.fbx` now loads through the shared imported-building fitting/material/night-light/interaction pipeline as `Resources.Load<GameObject>("Buildings/citypark")`, with a stable `.meta`; the previous procedural park remains as fallback only. Logical bench positions are still registered so City Park leisure/sitting behavior remains available with the imported visual. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Tuned another imported building visual-size pass and restored direct world-click resident selection for imported citizen models. Gas Station, Parking, Kiosk, Kindergarten, and all Personal House variants are visually larger, Cleaning Depot is reduced from the previous overshoot, and resident click selection now lives in `GameBootstrap.Transport.Selection.cs` with a screen-space visible-worker fallback after collider raycasts so disabled FBX colliders no longer make citizens unselectable. Confirmed City Park still uses procedural tree/decor generation through the service-decoration/enhancement path rather than an imported model. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Aligned another imported building visual-size pass around the current Warehouse/Motel/Labor Exchange scale. Gas Station, Furniture Factory, Logging Camp, Sawmill, Kindergarten, Primary School, Secondary School, Car Market, Cleaning Depot, and Docks now use larger footprint-fill values, while Kiosk and Personal House stay intentionally small/lot-scaled. The shared imported-building fit now logs `BUILD_MODEL_SCALE` entries with footprint, fill, target size, fit scale, and unscaled/scaled renderer bounds for future exact tuning from `debug.log`. Placement footprints, walk buffers, road access, staffing, and gameplay logic are unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Increased the imported Labor Exchange visual by another ~20% after the latest model-size pass. `LaborExchangeImportedModelFootprintFill` moved from `2.37` to `2.84`; placement footprint, walk buffers, road access, staffing, and Labor Exchange logic are unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Enlarged imported Warehouse, Labor Exchange, and Cleaning Depot visuals by another 50% from the current tuned values. Imported Warehouse footprint fill moved from `2.62` to `3.93`, Labor Exchange from `1.58` to `2.37`, and Cleaning Depot from `1.62` to `2.43`; placement footprints, walk buffers, road access, and gameplay logic are unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Investigated the latest `debug.log` performance drop and added guardrails around the likely hot spots. Runtime now logs `PERF` entries for large real-time frame gaps separately from slow `Update()` duration, including counts for active Unity lights, residents, vehicles, locations, HUD state, camera height, and far-LOD state. Night lighting keeps warm material/cell-light visuals but caps real Unity point/spot lights by camera-focus distance and far-zoom LOD; resident flashlight halos/beams only use real lights near the camera focus, and truck headlights no longer cast expensive realtime shadows. Labor Exchange work-availability checks now reuse a per-frame open-vacancy candidate cache instead of rebuilding the candidate list for every complaint/work check. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Enlarged another imported building visual batch. Imported Motel footprint fill moved from `3.28` to `3.70`; Labor Exchange from `1.05` to `1.58`, Cleaning Depot from `1.08` to `1.62`, and Parking from `0.98` to `1.47` for roughly 50% larger visuals on those three buildings. Placement footprint, walk buffers, road access, and gameplay logic are unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Tuned imported core building visual sizes. Imported Warehouse footprint fill moved from `2.45` to `2.62`, Motel from `3.10` to `3.28`, and City Hall from `2.45` down to `2.30`; placement footprint, walk buffers, road access, and gameplay logic are unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-17: Fixed building placement preview exit highlighting after expanded walk buffers. The build-mode blue exit/driveway tile now moves to the outer edge of the current building buffer for road-required and roadless building tools, and the orange buffer preview removes the corridor from the inner building entrance to that outer tile. Docks use the resolved shore placement anchor for the same outer-edge preview. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-16: Reduced character world visuals with one shared multiplier. Adult resident `DriverVisualRoot` models, worker children stage scales, and ambient forest workers now use `CharacterWorldVisualScale = 0.88`, preserving imported race/gender height differences, female offsets, jitter, procedural fallbacks, carry props, and existing animation poses while making all character models proportionally smaller. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-16: Connected the newly added imported building FBX models. Gas Station, Furniture Factory, Kiosk, Kindergarten, Primary School, Secondary School, Car Market, Labor Exchange, Cleaning Depot, Docks, and all five Personal House variants now load from `Assets/Resources/Buildings/` through the shared imported-building fitting/material/night-light/interaction pipeline with procedural decorations left as fallback. Missing FBX `.meta` files were added, new imported-model metadata disables cameras/lights/animation where applicable, and imported building runtime setup now also disables any imported cameras/lights/animators defensively. Tutorial scenario was checked; this is a visual/model hookup only and does not alter the Tutorial flow. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, missing FBX meta scan, resource-path existence scan, targeted mojibake scan.

- 2026-05-16: Increased Secondary School footprint from `5x3` to `6x3`. Placement, build preview, Build catalog copy, and procedural school decoration now use the wider footprint with five-window rows and a wider yard while education capacity rules stay unchanged. Tutorial scenario was checked; the required Tutorial building flow does not include Secondary School, so no scenario update was needed. Verification: JSON parse, `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted old-size/mojibake scan.

- 2026-05-16: Increased Primary School footprint from `4x3` to `5x3`. Placement, build preview, Build catalog copy, and the procedural school decoration now use the wider footprint while education capacity rules stay unchanged. Tutorial scenario was checked; the required Tutorial building flow does not include Primary School, so no scenario update was needed. Verification: JSON parse, `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-16: Connected the imported Parking building model. `Assets/Resources/Buildings/parking.fbx` now has stable Unity model import metadata and loads as `Resources.Load<GameObject>("Buildings/parking")`; Parking uses the shared imported-building fitting/material/night-light pipeline with the procedural 5x4 truck-yard visual kept as fallback. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-16: Enlarged Parking footprint to `5x4`. Player placement, preview, generated/fallback layout placement, and Build catalog copy now use the shared `WorldLayoutGenerator.ParkingFootprintWidth/Height` constants; Parking's procedural base and yard details scale to the larger footprint as a flat truck-yard surface. Tutorial scenario was checked; core-building goal flow is unchanged. Verification: JSON parse, `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-16: Warmed runtime light sources and strengthened resident flashlights. City/building/road/service/vehicle/bar-interior/racing lights now use warmer amber tones through the night-light update path and direct light styles. Walking residents now keep flashlights on during busy walks and idle wandering, with a stronger spot beam plus a small point-light halo around the resident; forest ambient workers received matching warmer flashlight/halo behavior. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-16: Slightly enlarged imported Motel and Canteen visuals and widened normal building walk buffers. Imported Motel footprint fill moved to `3.10`, Canteen to `2.35`; regular buildings now reserve a 2-cell non-walkable buffer like the already-expanded City Hall/Motel, while City Park and stops remain unbuffered. Build-mode footprint previews now draw the same buffer radius used by runtime placement/pathing instead of a fixed 1-cell ring. Tutorial scenario was checked; build goals/flow are unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-16: Added race/gender height variation for imported resident world models. Imported Iskrian citizens now use the tallest base height, Rovians the middle height, and Zelens the shortest height; women are slightly shorter within each race, and each resident gets a small stable per-name/id height jitter. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-16: Reduced all imported resident world-model visuals again. The shared `ImportedDriverTargetHeight` for Iskrian/Zelen/Rovian male and female FBX citizens moved from `1.38` to `1.18` world units, keeping the imported set equalized but closer to the procedural resident footprint. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-16: Connected the imported Canteen building model. `Assets/Resources/Buildings/canteen.fbx` now loads as `Resources.Load<GameObject>("Buildings/canteen")`, uses import metadata with cameras/lights/animation disabled, grounds against `GroundBase` / `GroundPatch` renderers, registers imported interaction/night-light metadata, and keeps the old procedural Canteen as fallback. Tutorial scenario was checked; the service-building tutorial flow is unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-16: Fixed another Motel-built resident stranding case from `debug.log`. Pedestrian pathing now treats all location entrance cells as walkable, while road/vehicle access still only uses road-required anchors, so Motel remains roadless but reachable on foot. Starter residents and arrival-bus residents also no longer remain deferred at/near the Intercity Stop when no safe walk path can be built; they are placed into a safe Motel idle point instead. Added `GameBootstrap.DriverMotelRelocation.cs` and kept `Assembly-CSharp.csproj` / `ai/systems-map.md` in sync. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-16: Connected the imported Sawmill building model. `Assets/Resources/Buildings/sawmill.fbx` now loads as `Resources.Load<GameObject>("Buildings/sawmill")`, uses import metadata with cameras/lights/animation disabled, grounds against `GroundBase` / `GroundPatch` renderers, hides the procedural Sawmill body when the import is present, and keeps the old procedural Sawmill as fallback. Tutorial scenario was checked; production/tutorial flow is unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-16: Expanded the building walk-buffer radius for City Hall and Motel from one surrounding cell to two surrounding cells. Build-site preparation now uses the same per-building buffer radius when clearing misc objects, footpaths, and street litter, so the widened non-walkable edge is cleaned consistently. Tutorial scenario was checked; the build sequence and goals are unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-16: Fixed starter residents getting stranded at the Intercity Stop after Motel construction when the generated Motel idle target landed inside the building walk buffer. Motel/intercity idle-stand selection now rejects road, location, water, edge-highway, and building-walk-buffer cells, and falls back to the nearest safe walk cell if frontage is blocked. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-16: Connected the imported Logging Camp building model for the Lumberjack Camp / Forest location. `Assets/Resources/Buildings/loggingcamp.fbx` now loads through the shared imported-building pipeline as `Resources.Load<GameObject>("Buildings/loggingcamp")`, uses the same camera/light/animation import hardening as other building FBX assets, grounds against its ground-base/ground-patch meshes, hides the procedural forest camp when the import is present, and preserves the runtime stored-log stack. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-16: Fixed a starter resident getting stranded near the Intercity Stop after Motel placement. The Motel relocation pass now commits a new Motel idle target only after a safe walk path is built; failed attempts restore the previous idle state and are retried after successful player road construction for residents still using the Intercity fallback idle area. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-16: Stabilized imported resident world-model display. Blender inspection showed the FBX meshes were clean, but the runtime rig was assigning imported mesh groups to the same body/head/arm/leg anchors used for procedural cube residents, so normal walk/idle pose rotations could pull imported heads, limbs, and clothes apart. Imported citizens now keep one `ImportedDriverPoseRoot` for whole-model pose lean while hair/cloth/glow flow pivots remain separate, and the two Iskrian model `.meta` files now keep camera/light import settings in the same `meshes` block as the other citizen FBX assets. Verification: Blender FBX mesh/rig-name inspection, `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-space/replacement-character scans.

- 2026-05-16: Retuned diorama camera wheel zoom. Mouse-wheel scroll now updates a target camera offset and eases the live offset toward it, with a smaller per-tick step for smoother zooming; the ground-level pitch transition now starts only in the final `0.055` normalized close-zoom range instead of beginning early. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-space/replacement-character scans.

- 2026-05-16: Enlarged imported vehicles again and changed wheel animation to spin relative to each wheel's default pose. Imported truck target size is now `1.38 x 1.10 x 1.84`, imported bus target size is now `2.45 x 1.16 x 0.92`, and wheel animation now stores each wheel pivot's base local rotation plus a local spin axis; imported wheel axes are inferred from the thinnest renderer-bounds dimension so wheels rotate around their own axle instead of being forced onto a global guessed axis. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-space/mojibake scans.

- 2026-05-16: Increased imported vehicle scale again and corrected wheel spin axes. Imported truck target size is now `1.20 x 0.96 x 1.60`, imported bus target size is now `2.08 x 0.98 x 0.78`, and truck/bus wheel animation now spins imported wheel pivots around local forward/Z while procedural cylinders spin around their own cylinder axis after the proper base orientation. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-space/mojibake scans.

- 2026-05-16: Retuned imported resident and vehicle visual scale/animation. Imported citizens now normalize to `1.38` world-unit height instead of `1.45`; imported trucks fit to `1.02 x 0.83 x 1.36`, imported buses fit to `1.74 x 0.82 x 0.64`, and vehicle visuals gained stronger suspension/tilt plus imported/procedural bus wheel spin through a dedicated visual motion root. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-space/mojibake scans.

- 2026-05-16: Normalized imported resident model size. All imported Iskrian/Zelen/Rovian race+gender FBX citizens now scale by visible model height to the same `1.45` world-unit target instead of fitting the smallest width/height/depth axis, so the full imported set is larger and equalized in height. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-space/mojibake scans.

- 2026-05-16: Connected the remaining imported resident race models. `ZeleninMale/Female` and `RovianMale/Female` now load through the shared imported citizen pipeline by race+gender, with cleaned import metadata, generic body/head/limb rig matching, race-specific hair/cloth/accent flow pivots, and procedural fallback if a resource is missing. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-space/mojibake scans.

- 2026-05-16: Increased imported Iskrian resident model size. The shared imported resident fit target moved from `0.46 x 1.10 x 0.34` to `0.54 x 1.30 x 0.40`, making both male and female Iskrian FBX world models about 18% larger while leaving procedural resident models unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-space/mojibake scans.

- 2026-05-16: Hid stray rectangular visuals around imported Iskrian residents. Imported resident loading now disables helper/marker renderers by name, the always-created procedural flashlight cube renderer is hidden for imported Iskrian bodies while its light remains available, and female collarbone skin-line strips are no longer included in glow scaling pivots. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-space/mojibake scans.

- 2026-05-16: Added gender-specific secondary animation for imported Iskrian resident models. Imported male/female citizens now store animation hooks for model root, hair, cloth, and glow flow pivots; the FBX runtime rig creates those pivots from each model's actual part names, and all existing resident pose paths now layer imported-specific motion over body/head/arm/leg anchors for walking, neutral idle, conversations, sitting, park idle variants, smoking, phone calls, trash-can meals, cat petting, cleaner sweeping, lumber chopping, and planting. Male motion is heavier/coat-led; female motion emphasizes braid/cloak/tunic sway. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-space/mojibake scans.

- 2026-05-16: Connected the imported female Iskrian resident model. `Assets/Resources/Citizens/IskryaninFemale.fbx` now loads as `Resources.Load<GameObject>("Citizens/IskryaninFemale")` for female Iskrian world citizens through the same imported resident pipeline as the male model, with upright fitting, material normalization, disabled imported cameras/lights/animations/colliders, shared body/head/limb runtime pivots, and procedural fallback if the resource is missing. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-space/mojibake scans.

- 2026-05-16: Connected the imported male Iskrian resident model. `Assets/Resources/Citizens/IskryaninMale.fbx` now loads as `Resources.Load<GameObject>("Citizens/IskryaninMale")` for male Iskrian world citizens, auto-selects an upright orientation, scales to the existing resident footprint, disables imported cameras/lights/animations/colliders, normalizes materials, builds simple runtime pivots for current body/head/limb animations, and keeps procedural male Iskrians as fallback if the resource is missing. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-space/mojibake scans.

- 2026-05-16: Increased the imported Motel visual scale. `MotelImportedModelFootprintFill` moved from `2.45f` to `2.95f`, making only the imported Motel model about 20% larger inside its existing 2x2 placement while leaving fallback visuals and other imported buildings unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-16: Rebuilt imported ambient cat animation around a runtime rig after the FBX parts separated under the first primitive-style animation pass. Head, tail, and four legs now get grouped under generated pivots with their related mesh parts before animation; the imported model scales as one body for squash/stretch, uses smaller tail/head motions, and gets alternating cartoon leg swings for walk/idle/petting. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-16: Connected the imported ambient cat misc model. `Assets/Resources/Misc/cat.fbx` now loads as `Resources.Load<GameObject>("Misc/cat")` for ambient cats, has stable folder/model import metadata with cameras/lights/animation disabled, auto-fits to the existing cat footprint, disables imported colliders/helpers, keeps procedural cats as fallback, and preserves the existing roam/petting state machine with imported head/tail/body transform animation hooks. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-16: Hardened imported building materials after City Hall/Motel wall transparency reports. Imported building renderers now normalize non-window materials to opaque alpha, opaque render state, geometry queue, z-write on, and double-sided culling off, while window/glow materials remain available for night-light handling. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-16: Connected the imported City Hall building model. `Assets/Resources/Buildings/cityhall.fbx` now loads as `Resources.Load<GameObject>("Buildings/cityhall")`, has import metadata with cameras/lights/animation disabled, keeps its Y-up geometry without extra pitch correction, grounds against `MainBuilding_Foundation`, hides the procedural City Hall body when the import is present, and keeps the old procedural City Hall as fallback. City Hall gameplay/UI flow was unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-16: Connected the imported Motel building model. `Assets/Resources/Buildings/motel.fbx` now loads as `Resources.Load<GameObject>("Buildings/motel")`, has import metadata with cameras/lights/animation disabled, keeps the model's Y-up geometry without an extra pitch correction, grounds against `MainBuilding_Foundation` / `OfficeBlock_Foundation`, hides the procedural Motel body/window overlays when the import is present, and keeps the old procedural Motel as fallback. Tutorial flow and Motel gameplay logic were unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-15: Fixed construction reveal movement for rotated imported building models. Construction animation parts now compute their hidden start point by converting the building-root world-down vector into each part parent's local space, so imported Warehouse pieces rise vertically like the imported Bar instead of sliding along the Warehouse FBX's rotated local plane. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-15: Fixed the imported Warehouse model orientation/grounding. Warehouse imported-building placement now applies a Warehouse-only `-90` degree X rotation before fitting, matching the FBX's Z-up visual plane to Unity's Y-up world, and grounds against the model's concrete foundation renderers (`MainBuilding_ConcretePlinth` / `OfficeBlock_ConcreteBase`) instead of the full visible bounds. Bar/Gambling Hall keep their previous placement behavior. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-15: Connected the imported Warehouse building model. `Assets/Resources/Buildings/warehouse.fbx` now loads through the existing imported building pipeline as `Resources.Load<GameObject>("Buildings/warehouse")`, uses import metadata with cameras/lights/animation disabled, fits into the Warehouse footprint, registers the imported-building visual root, hides the old procedural base/window overlays when the import is present, and keeps the procedural Warehouse decoration as fallback. Tutorial flow and Warehouse gameplay logic were unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-15: Connected the imported bus model for shared bus visuals. `Assets/Resources/Vehicles/Bus/Bus.fbx` now has import metadata with cameras/lights/animation disabled, and `GameBootstrap.EdgeHighway.BusModels.cs` loads `Resources.Load<GameObject>("Vehicles/Bus/Bus")`, strips runtime camera/light/animation/collider components, hides helper camera/view renderers, fits visible bounds to the shared bus footprint target `1.52x0.72x0.56`, and preserves procedural fallback plus headlight/shadow runtime hooks. Local Parking buses, ambient edge-highway buses, and hiring-arrival buses now use the same shared visual builder. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-15: Hardened imported truck spawn after a freeze report. `Truck.fbx` import metadata now skips cameras/lights/animation, runtime truck loading strips camera/light/animation components, hides helper/view renderers, rotates the X-forward asset into the game's Z-forward vehicle axis, fits visible bounds to the truck footprint target `0.88x0.72x1.18`, logs raw/fitted bounds, and avoids registering every imported renderer in the runtime shadow LOD list. Repeated active-thought refreshes now apply explicit thought influence only once per pending formation, preserving the first influence state without emitting the same `THOUGHT_INFLUENCE` line every tick. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-15: Connected the imported city truck model. `Truck.fbx` now lives under `Assets/Resources/Vehicles/Trucks/`, with folder/model `.meta` files, and `GameBootstrap.Actors.TruckModels.cs` loads it through `Resources.Load<GameObject>("Vehicles/Trucks/Truck")`, auto-fits it to the existing truck footprint, preserves working cargo visuals/headlights/shadow, and falls back to the old procedural truck if the asset is missing. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-15: Added race-specific procedural world models for residents. `SetupDriver()` now delegates 3D resident assembly to `GameBootstrap.Actors.WorkerModels.cs`, which gives each `WorkerRaceKind` two stable visual variants with distinct clothing silhouettes, accents, headwear, and accessories while preserving the existing body/head/limb/fuel-can/flashlight animation anchors. `Assembly-CSharp.csproj` now includes the new runtime partial. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-15: Reworked the Noosphere HUD into switchable tabs. `City Memory` now has Overview, Knowledge, Signals, Journal, and Map tabs; the top summary is compact, the event stream is isolated in Journal, Knowledge shows current pinned/forming/contested city knowledge state, Signals summarizes public social-signal topics, and the animated Noosphere map only runs on its Map tab. Added `GameBootstrap.FleetCanvas.NoosphereScreen.Tabs.cs` and `GameBootstrap.FleetCanvas.NoosphereScreen.TabData.cs`, with `GameBootstrap.NoosphereVisuals.cs` gated to the active Map tab. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-15: Temporarily gated City Hall requests to Social Introduction only. Non-social request generators (`ServiceMissing`, grouped worker complaints, and social-signal `PublicConcern`s) now return early, and any already active non-social City Hall requests are silently resolved without trust penalties while the temporary gate is enabled. Added `GameBootstrap.CityComplaints.TemporaryGate.cs`; `SocialIntroduction` remains active. Verification: `tools/check-line-count.ps1`, `dotnet build Assembly-CSharp.csproj -v:minimal`, `git diff --check`.

- 2026-05-15: Fixed money-pressure City Hall public concerns ignoring existing income paths. `Money:TEXT:MONEY` public concerns are now suppressed/resolved when every active signer either already has assigned work or has a suitable work opportunity covered by Labor Exchange/open vacancies, while the underlying `low_money` and `affect_financial_pressure` thoughts still remain available for resident state/social signal visibility. Added `GameBootstrap.CityComplaints.MoneyPressure.cs` for the focused public-concern filter. Verification: `tools/check-line-count.ps1`, `dotnet build Assembly-CSharp.csproj -v:minimal`, `git diff --check`.

- 2026-05-15: Fixed arrival-bus residents getting stuck after unsafe-cell rescue. Rescuing a worker from `ToMotelFromBusStop` now completes the stale arrival state by clearing `IsArrivingByBus`, and a no-path halt for the same phase also clears the arrival blocker so idle life, vacancies, and migration counts can see the resident again. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-15: Fixed duplicate City Hall litter public concerns. Street-litter daily signals and city/affect litter thought signals now share the canonical public-concern key `social_cluster:litter:street_litter`, use the same `street litter` / `мусор на улицах` title, and active duplicate public concerns are merged/resolved without trust side effects. Added `GameBootstrap.CityComplaints.PublicConcernDedup.cs` for semantic duplicate handling. Verification: `tools/check-line-count.ps1`, `dotnet build Assembly-CSharp.csproj -v:minimal`, `git diff --check`.

- 2026-05-15: Fixed City Hall work public concerns ignoring available jobs. `Work:city_work` social-signal public concerns are now suppressed/resolved when open Labor Exchange postings and unposted staffing vacancies can cover the unassigned residents, and direct `NoJob` complaints also skip workers who have an available matching work opportunity. Added `GameBootstrap.LaborExchange.WorkAvailability.cs` to centralize vacancy/posting availability checks with education and professional requirements. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-15: Split the edge-highway bus visual/runtime helpers out of `GameBootstrap.EdgeHighway.cs` into `GameBootstrap.EdgeHighway.Buses.cs` so the edge-highway partial is back under the 900-line class/file limit without changing behavior. Updated `Assembly-CSharp.csproj` and owner memory for the new runtime partial. Verification: `tools/check-line-count.ps1`, `dotnet build Assembly-CSharp.csproj -v:minimal`, `git diff --check`.

- 2026-05-14: Fixed City Hall public concerns filing service-building complaints for already built need services. Public-concern social clusters with `Need` + `BuildingType:<service>` are now suppressed when the targeted need-service building already exists, and existing matching public concerns are treated as resolved by the same rule. This keeps pre-build `service_missing` and later known-place hunger signals visible in social/Noosphere data without turning an existing Canteen/Motel/Bar/Kiosk/Gambling Hall/City Park into a repeat construction complaint. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `git diff --check`.

- 2026-05-14: Fixed stale day-end recovered warnings creating City Hall public concerns. `BuildWorkerDailyOpinion` now skips resolved same-day active negative thoughts when their end-of-day condition has recovered: job/no-job warnings, need warnings/critical needs, low money, and financial pressure. Residents who still have the problem keep contributing real pressure signals. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `git diff --check`.

- 2026-05-14: Fixed build-site cleanup after the expanded building edge zone. `PrepareBuildSiteForLocation` now removes misc decorations from both the footprint and the building walk-buffer edge cells, matching the existing footpath/litter cleanup so trees, bushes, and flower patches no longer remain on newly blocked building-edge cells. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `git diff --check`, touched-file line count. `tools/check-line-count.ps1` still reports the pre-existing `Assets/Scripts/Runtime/Core/GameBootstrap.EdgeHighway.cs` 900-line violation.

- 2026-05-14: Added current social leadership to the citywide Social Graph HUD. City links now compute a `SocialLeader` status and social-leadership score/rank/link count on residents; the citywide graph hides residents with no visible social links, keeps the social leader centered and largest, and scales/places other residents by social significance. The per-resident Social Graph tab opened from Workers is unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `git diff --check`, targeted touched-file line counts, replacement-character scan. `tools/check-line-count.ps1` still reports the pre-existing `Assets/Scripts/Runtime/Core/GameBootstrap.EdgeHighway.cs` 900-line violation.

- 2026-05-14: Rebalanced public City Hall concerns around positive social signals. Negative social clusters now subtract same-topic positive signals before becoming `PublicConcern`s, log suppressed balanced clusters, use net strength/active signer count for severity, and accepted/recent concerns also re-check this balance. Successful service visits now keep their building opinion but also add a positive same-topic need signal/opinion, so `meal_service_good` can balance `Need:Meal` pressure instead of only praising `Canteen`. Existing service-building requests auto-resolve and can grant promise completion even if the player built the requested building before accepting the request. Verification: `dotnet build Assembly-CSharp.csproj --no-restore`, `git diff --check`.

- 2026-05-14: Tightened road access logic for production/service buildings. Required road access now means the entrance is on a road/highway cell and that road network eventually reaches the edge highway; quick HUD warnings distinguish missing road from road-not-linked-to-highway. Edge-highway setup refreshes warnings after generated highway cells exist, and road corner masks now use road asphalt material instead of ground material so turn cells no longer expose grass/soil slivers. Verification: `dotnet build Assembly-CSharp.csproj --no-restore`, `git diff --check`.

- 2026-05-14: Cleaned up Trade HUD policy defaults and trade feedback. Resource policy targets now default to `0` for every resource and switching to Sell Surplus/Buy Up To no longer silently raises the target to `5`; the smoke test now asserts that mode selection preserves a zero target. Land trade feed messages include the actual buy cost or export revenue and only emit after the trade really completes. Verification: `dotnet build Assembly-CSharp.csproj --no-restore`, `git diff --check`.

- 2026-05-14: Allowed multiple production buildings. Warehouse, Lumberjack Camp, Sawmill, Furniture Factory, and Docks can now be placed repeatedly without overwriting the primary `locations[type]`; extra instances are stored in the shared location instance list, get instance labels, road-access checks, selected-building HUD data, Docks ship runtime, per-instance sawmill/factory processing timers, and per-instance Warehouse/Labor Exchange vacancy support. Verification: `dotnet build Assembly-CSharp.csproj --no-restore`.

- 2026-05-14: Fixed building microHUD worker presence after `debug.log` review showed the Labor Exchange clerk could appear as still on shift after leaving for another service. Building quick HUD worker rows now resolve against the selected `LocationData`/instance instead of only `LocationType`, inside-building rows require the worker to be inside that exact building, Warehouse quick HUD height uses the same filtered entries, and "on shift" counters require `IsOnActiveShift`. Verification: `dotnet build Assembly-CSharp.csproj --no-restore`.

- 2026-05-14: Made layered resident portrait animation the standard portrait behavior and added state-aware cartoon expressions. `WorkerPortraitAnimator.cs` now runs on every portrait root created through `DrawWorkerPortraitScaled`, with texture slots and procedural fallback parts tagged through `WorkerPortraitLayerMarker`; head/face layers breathe subtly, hair back/front sway independently, eyes blink, occasionally glance sideways/up/down, and fatigue/anxiety/relief/hangover/financial pressure affect stronger brow/eye/mouth squash-stretch. Procedural left/right eyes and brows also get asymmetric acting beats for more readable facial motion. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-14: Raised texture-backed portrait hair placement after visual review showed the hairline sitting too low on the forehead. `GameBootstrap.WorkerPortraitTextures.cs` now applies a shared upward UI offset to both `hair_back` and `hair_front` slots, leaving head/face/clothes on the original canvas alignment. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-13: Adjusted the day-1 game-start cinematic timing. `Day 1` / `День 1` now fades out during the same visual scene fade-in and is gone exactly when the black overlay finishes, instead of holding and fading after the scene is already visible. Tutorial scenario memory now matches the new timing. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted added-line mojibake scan.

- 2026-05-13: Corrected texture-backed resident portrait hair layering. `GameBootstrap.WorkerPortraitTextures.cs` now treats `hair_back` and `hair_front` as separate selected slots, pairs them by the same implicit `variant_##`, renders `hair_back` before ears/head and `hair_front` after face accessories through the required layer order, and safely skips missing male `hair_back` assets. Manifest audit confirmed no generic `hair` slot, no Hair files outside `Back`/`Front`, complete female back/front pairs for every race, and no male back assets expected. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, portrait hair manifest audit.

- 2026-05-13: Rechecked resident portrait texture loading after the manifest v7 microchange. `GameBootstrap.WorkerPortraitTextures.cs` now treats `variant_##` from a part `id` or file stem as an implicit variant tag, so manifests with empty `tags` still keep coherent variant selection across portrait layers; female `hair_back` can also follow the same variant. Confirmed `portrait_parts_manifest.json` has 252 parts, 0 missing files, and required `background`/`clothes`/`head` coverage for every race/gender pair. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, manifest resource-path scan.

- 2026-05-13: Added the resident heritage/narod cultural-lens layer on top of the existing visual race identity. `WorkerHeritageKind` is synchronized from `WorkerRaceKind`, player-facing UI now presents Heritage/Origin, and soft heritage hooks bias already-triggered thoughts, knowledge formation/confidence, and social-signal strength for Rovian work/transport/money, Zelen home/family/litter/nature, and Iskrian social/knowledge/noosphere meaning. No direct work speed, salary, production, or staffing bonuses were added. Noosphere day-start snapshots now copy heritage. Added `ai/Design/worker-heritage-system.md` and linked it from the worker thought tree. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted added-line mojibake scan.

- 2026-05-13: Switched resident portrait rendering to use the new texture-part library in `Assets/Resources/Art/Workers/Portraits/`. `GameBootstrap.WorkerPortraitTextures.cs` now loads `portrait_parts_manifest.json`, picks stable race/gender/profile-compatible layers for each worker, supports the manifest v3 `<Category>/<Gender>/<Race>/` structure, keeps one coherent `variant_*` across portrait layers, respects manifest layer order and canvas aspect, creates cached sprites from Resources textures, and falls back to the old procedural UI-rectangle portrait if the manifest or critical assets are missing. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, manifest asset-path scan.

- 2026-05-13: Fixed the Workers list row layout after adding race badges. Resident rows now stack name, race badge, and current job/status vertically in the info column, with money in a separate right column so race/job text no longer collides with balance values. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-13: Added the first resident race identity layer. Workers now receive one stable deterministic race (`Rovian`, `Zelen`, or `Iskrian`) at creation/runtime normalization, with a centralized race catalog for names, descriptions, colors, icon keys, and inline badges. Race is visible in the selected worker quick HUD, Workers list, Workers personality card/tooltips, Noosphere Vision resident tooltip, and day-start Noosphere snapshots; it does not change work speed, salary, production, or money logic. Updated system map/tree and Tutorial scenario notes. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted added-line mojibake scan.

- 2026-05-13: Reworked the day-1 game-start title into a staged cinematic. Starting a new game now begins on a black screen with `Day 1` / `День 1` already visible, fades the whole scene in first with a longer smootherstep transition, then fades the title away; startup audio ramps more slowly through `AudioListener.volume` instead of appearing instantly. Later day rollover titles keep the existing non-blocking title-only behavior. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-13: Removed the visible cell lighting overlay after further in-game review showed any ground lightmap still read as a texture laid over the town. `GameBootstrap.World.CellLighting.cs` now keeps the six-level per-cell lighting map as internal data only; visual night lighting comes from real building/lantern Unity lights and glow materials. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted added-line mojibake scan.

- 2026-05-13: Softened the new cell lighting overlay after in-game review showed it reading as a yellow texture stretched over grid cells. The overlay texture is now 4x denser per cell, samples bilinearly between cell lighting values, uses a lower alpha cap, and desaturates toward a neutral night glow so realtime lights remain the primary visible light. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted added-line mojibake scan.

- 2026-05-13: Fixed the expanded night lighting showing yellow light patches during daytime/early morning. Location lights, road lanterns, and the cell lighting overlay now share darker activation thresholds (`NightLightActivationDarkness`/`NightLightFullDarkness`) so the 06:00 start and normal day view do not show night-light halos. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted added-line mojibake scan.

- 2026-05-13: Expanded the night lighting system. Added `GameBootstrap.World.CellLighting.cs` with a six-level per-cell lighting map (`Dark` through `Landmark`) and a single transparent night lightmap overlay driven by road lanterns and building lights. Road lanterns and service/building lights now use wider profiles, imported building night point lights are expanded too, cell lighting is dirtied by road lantern/location changes and demolition, and the overlay mesh refreshes after terrain flattening. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted added-line mojibake scan.

- 2026-05-13: Fixed Bus Driver vacancy availability so Labor Exchange/manual staffing no longer offers or fills Bus Driver roles unless there is a working local bus route: Parking can reach a local stop and that stop can reach a second stop. Existing stale Labor Exchange Bus Driver postings are now treated as unavailable/filled when the route disappears, Staffing hides open Bus Driver vacancies without a working route, and direct bus-slot assignment is blocked until the route exists. `ai/tutorial-scenario.md` now records the route prerequisite for bus-driver staffing. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted added-line mojibake scan.

- 2026-05-13: Enabled the final `Stability / Recovery` explicit worker thought influence package. `GameBootstrap.WorkerThoughtInfluence.cs` now contains all 119 described rules, adding 9 final rules for `salary_paid`, `job_found`, `meal_service_good`, `home_sleep_good`, `stable_life`, `affect_family_anxiety`, `affect_litter_irritation`, and `affect_stable_routine`. The full worker thought influence matrix is now implemented in code. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted whitespace/mojibake scan.

- 2026-05-13: Enabled the `Gambling contradiction` explicit worker thought influence package. `GameBootstrap.WorkerThoughtInfluence.cs` now contains 110 enabled rules total, adding 10 rules for `low_money`, `affect_gambling_regret`, and `affect_gambling_excitement` with Gambling weakness and Frugal/Impulsive/Cautious trait modifiers. Remaining matrix branch is Stability/Recovery. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted whitespace/mojibake scan.

- 2026-05-13: Enabled the `Transport` explicit worker thought influence package. `GameBootstrap.WorkerThoughtInfluence.cs` now contains 100 enabled rules total, adding 5 rules for `bus_chosen` and `bus_unavailable` with Cautious/Trusting/Dutiful/Anxious trait modifiers. Remaining matrix branches are Gambling contradiction and remaining Stability/Recovery. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted whitespace/mojibake scan.

- 2026-05-13: Enabled the `Social` explicit worker thought influence package. `GameBootstrap.WorkerThoughtInfluence.cs` now contains 95 enabled rules total, adding 7 rules for `social_talk_good`, `social_shared_place`, and `social_learned_new_topic` with Sociable/Reserved/Trusting/Skeptical/Curious trait modifiers. Remaining matrix branches are Transport, Gambling contradiction, and remaining Stability/Recovery. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted whitespace/mojibake scan.

- 2026-05-13: Enabled the `City/Litter` explicit worker thought influence package. `GameBootstrap.WorkerThoughtInfluence.cs` now contains 88 enabled rules total, adding 21 rules for `worker_arrived`, `stable_life`, `street_litter_low/medium/high`, `affect_litter_irritation`, and `affect_stable_routine` with Meticulous/Anxious/Trusting/Skeptical/Sociable/Adaptable/Stubborn/Dutiful trait modifiers. Remaining matrix branches are Social, Transport, Gambling contradiction, and remaining Stability/Recovery. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted whitespace/mojibake scan.

- 2026-05-13: Enabled the `Family` explicit worker thought influence package. `GameBootstrap.WorkerThoughtInfluence.cs` now contains 67 enabled rules total, adding 14 long-window family rules for `house_bought`, `family_formed`, `child_born`, and `affect_family_anxiety` with Dutiful/Anxious/Frugal/Adaptable trait modifiers. Remaining matrix branches are City/Litter, Social, Transport, Gambling contradiction, and remaining Stability/Recovery. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted whitespace/mojibake scan.

- 2026-05-13: Enabled the `Needs` explicit worker thought influence package. `GameBootstrap.WorkerThoughtInfluence.cs` now contains 53 enabled rules total, adding 30 Meal/Sleep/Leisure rules for critical needs, service relief, snack/coffee fallbacks, home sleep, leisure relief, and hangover/rest contradictions with Alcoholism/Gambling weakness modifiers where relevant. Remaining matrix branches are Family, City/Litter, Social, Transport, Gambling contradiction, and remaining Stability/Recovery. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted whitespace/mojibake scan.

- 2026-05-13: Enabled the first explicit worker thought influence package: `Money + Work + Paid Services`. `GameBootstrap.WorkerThoughtInfluence.cs` now contains 23 rules for `low_money`, `service_unaffordable`, `salary_paid`, `affect_financial_pressure`, `no_job_warning`, `starter_job_suggestion`, and `job_found`, with trait modifiers, caps, optional wording for strong links, and `THOUGHT_INFLUENCE` logging. Remaining matrix branches are not implemented yet. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted whitespace/mojibake scan.

- 2026-05-13: Started implementation of the explicit worker thought influence matrix. Added `GameBootstrap.WorkerThoughtInfluence.cs` with the rule data shape, source-thought lookup, target-key normalization, trait/weakness modifiers, caps, optional wording suffix, and `THOUGHT_INFLUENCE` logging hook wired after generic opinion bias in pending thought formation. The concrete rule catalog is intentionally empty for this step; next step is the first enabled package `Money + Work + Paid Services`. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted whitespace/mojibake scan.

- 2026-05-13: Added the worker thought influence matrix to project workflow rules. `AGENTS.md`, `ai/README.md`, and `ai/prompt-templates.md` now require future agents to read `ai/Design/worker-thought-influence-matrix.md` before changing explicit thought influence rules, `WorkerOpinion` feedback loops, source/target thought links, influence windows/caps, or human-logic links, and to update it when those rules change. Verification: `git diff --check`, targeted diff mojibake scan.

- 2026-05-13: Added `ai/Design/worker-thought-influence-matrix.md` as a design matrix for future explicit thought influence rules. It documents the intended `source thought -> WorkerOpinion -> target thought bias` layer, covers 36 source thoughts and 119 candidate influence rules, names high-detail human-logic cases, safety caps, a future data shape, and the recommended first implementation slice `Money + Work + Paid Services`. `worker-thought-tree.md` now links to the matrix from Feedback Loops. Verification: markdown whitespace scan, `git diff --check`, targeted mojibake scan.

- 2026-05-13: Added a controlled `WorkerOpinion` feedback loop for resident thoughts. Pending thoughts now look up exact/fallback opinions, apply confidence-gated bias to tone/intensity/priority/formation time, add an optional wording suffix for strong bias, decay old opinion confidence for bias purposes, and log `THOUGHT_BIAS`; opinions still never create thoughts by themselves and `starter_job_resolved` remains excluded. `ai/Design/worker-thought-tree.md` now documents the feedback-loop rules. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-13: Promoted `ai/Design/worker-thought-tree.md` from a linked design map into an explicit project workflow rule. `AGENTS.md`, `ai/README.md`, and `ai/prompt-templates.md` now require future agents to read it before changing worker thoughts/affects/weakness-driven interpretation/knowledge-opinion links/Noosphere thought-state chains/Workers-F9 thought UI, and to update it when those chains change. Verification: `git diff --check`, targeted diff mojibake scan.

- 2026-05-13: Added `ai/Design/worker-thought-tree.md` as the current design map for resident thoughts. The document records the cause -> status/affect -> thought -> opinion/knowledge -> social signal -> Noosphere/UI chain, includes cards for every current thought key/template variant, documents affect states and weaknesses, and captures known gaps around inactive need warning thoughts, `no_job_today`, hidden `starter_job_resolved`, and litter thought UI. `ai/README.md` now points future agents to the design map. Verification: markdown/source scan, `git diff --check`, targeted mojibake scan.

- 2026-05-13: Clarified worker thought status coverage. F9/States reference now marks `need_*_warning` as template/UI-ready but inactive under current active rules, marks `no_job_today` as a legacy alias for `no_job_warning`, and notes that `street_litter_low/medium/high` are active. Workers UI now has dedicated display cases for low/medium/high street-litter thoughts instead of falling back to generic thought rendering. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-13: Tightened Workers personality card UI after the traits/weaknesses refactor. The `States` row now uses the yellow accent like the other section labels, and the `Main thought` row was removed from the `Personality` block. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `git diff --check`, targeted mojibake scan.

- 2026-05-13: Reworked worker personality into `Traits + Weakness + Affects`. Each worker now rolls 3 non-conflicting character traits and a 33% chance of one weakness (`Alcoholism` or `Gambling`) instead of 4 of 5 legacy perks or a separate visible "досуг" personality layer. Legacy `Socialite`/`Frugal`/`Quicklearner` migrate to `Sociable`/`Frugal`/`Curious`; legacy `Alcoholism`/`Gambler` migrate to at most one weakness. Weaknesses steer Bar/Gambling Hall choice, gambling risk, thoughts, knowledge bias, Noosphere Vision chains, and Workers/F9/Tutorial copy; affects still do not modify work speed, salary, or production. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, old leisure/personality API scan, targeted mojibake scan.

- 2026-05-13: Added silent Noosphere day-start snapshots and day-1 title display. `GameBootstrap.NoosphereSnapshots*.cs` now stores copied internal state for knowledge events, social signals/insights, city experience, canon, conversation topics, resident cognition, dive meanings, vision insights, and visual-node state at game start/day 1 and each later day start. `FinishGameStart()` now shows the existing `Day 1` cinematic title, day rollover records the snapshot before showing the next day title, and Noosphere Vision builder/UI code is split into focused partials for the line-count rule. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-13: Cleaned AI memory consistency after an `ai/` review. `system-tree.md` no longer uses outdated production-chain wording for the Tutorial build path, `README.md` separates release memory from volatile work memory, and `work-log.md` Active Queue now reflects that no queued memory items are active. Verification: `git diff --check`, targeted mojibake scan.

- 2026-05-13: Reduced likely day-2 morning performance spikes found in `debug.log`. Runtime debug logging now flushes buffered lines by interval/larger batches instead of small synchronous bursts, shift commute path starts are capped to two expensive starts per frame so 08:00 worker commutes are spread across frames, construction misc-object removal is verbose-only while the existing build-site summary remains visible, and citywide lived-experience factor counts now say signals instead of residents. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Added MVP child education/schools. Build menu and placement now include Primary School and Secondary School with Teacher staffing, capacities are calculated as base seats plus assigned teachers, children now map Toddler -> Kindergarten, Child -> Primary School, Teen -> Secondary School, and New Game unlocks education buildings as families reach the relevant stages. Family happiness/readiness/pressure, personal-house HUD rows, education quick HUDs, and family-stress complaints now react to missing school seats. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, JSON parse, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Reworked worker families around child slots instead of a single-child cap. Personal houses now support up to 2 active children, next-child readiness uses work stability, relationship, family happiness, savings, unpaid upkeep, critical needs, child-care capacity, and per-stage child load, and births enforce an 8-12 day spacing/cooldown. Children now progress through Baby/Toddler/Child/Teen stages with different upkeep/child-care pressure, then can grow into new adult residents or leave town. Personal-house HUD rows show each child stage/age/next-stage timing, family stress complaints can be driven by aggregate child pressure, and the system tree records the new family child-slot/growth responsibilities. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Added the `Assets/Resources/GeneratedAudio/ASMRMustHave` sound set to runtime audio. New clips are loaded from Resources/Editor fallback, exposed in Sound Options, and hooked into build tool start, build complete, road build wave, build denial, daily tax collection, and truck cargo load/unload events. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Fixed persistent white ground grid lines outside Build mode. The always-created `GridLines` world mesh now starts inactive, uses a transparent hidden material state, and is only enabled while the Build panel/tool is active; far-zoom LOD no longer re-enables it during normal gameplay. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Tightened Build menu construction cost badges. Build item cards now give the price badge an explicit top-right RectTransform size so it does not spread over the center icon, render costs as a small gold `$` + amount badge, and keep unaffordable items dimmed while showing the red price instead of replacing it with a generic no-funds label. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Fixed Unity tree OBJ import normal warnings. All six generated `Assets/Resources/Nature/Trees/tree_*.obj` files now include explicit `vn` normals and every face references `v/vt/vn`, so Unity no longer needs to recalculate missing normals during import. Verification: OBJ structure scan, `dotnet build Assembly-CSharp.csproj -v:minimal`, `dotnet build Assembly-CSharp-Editor.csproj -v:minimal`.

- 2026-05-12: Rebalanced world readability after the texture/tree pass. Grid lines now use a very low-alpha calm colour in normal play and switch to bright amber only while the Build panel/tool is active. Ground cells now use world-space macro UVs, larger texture scale, calmer green/sage tints, lower per-cell contrast, forest-edge mixed ground, shoreline transition tinting, softer water-edge wash/foam, extra sparse shoreline reeds/pebbles, and muted imported/procedural tree leaf tones. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `dotnet build Assembly-CSharp-Editor.csproj -v:minimal`.

- 2026-05-12: Applied tree texture PNGs from `Assets/Resources/Nature/Trees/Textures/` to generated tree assets. `procedural_trees.mtl` now maps bark/leaf materials to the matching low-poly textures, all six OBJ tree models have generated UV coordinates for those texture maps, and `ProceduralTreePrefabGenerator` assigns the same textures when generating Unity prefab/material variants. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `dotnet build Assembly-CSharp-Editor.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Added generated/imported tree asset support for world misc trees. Six low-poly OBJ tree models now live in `Assets/Resources/Nature/Trees/`, and `CreateMiscTree` loads `Resources.LoadAll<GameObject>("Nature/Trees")`, instantiates a variant, auto-fits it to the existing tree root height/base, enables shadow LOD registration, and preserves the same root for lumber growth/felling, tree sway, cleanup, and ambient perch points. `Assets/Editor/ProceduralTreePrefabGenerator.cs` can generate prefab/material/mesh variants from Unity's Tools menu, and the old procedural primitive trees remain the fallback when no assets are present. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `dotnet build Assembly-CSharp-Editor.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Integrated imported low-poly world texture PNGs from `Assets/Resources/Art/Textures/` into runtime materials through build-safe `Resources.Load` lookup. Fresh grass, dry grass, forest floor, dirt footpaths, sandy riverbanks, town asphalt, highway asphalt, gravel shoulders, river water, and lake water now replace/fill the procedural material slots with per-cell tint/offset variation; missing assets still fall back to generated textures. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`.

- 2026-05-12: Completed the rest of the social-simulation backlog. Noosphere now aggregates `SocialSignal` into city meaning insights with tone, topic split, recurring reasons, and tension/source summaries. City trust now has formula helpers fed by daily social signals and public promises. Negative signal clusters can generate City Hall public-concern requests, and accepted requests become visible public promises with success/failure trust effects. Money ledger entries now carry account owner/reason fields for city budget, resident wallets, building cashboxes, external, and debug money. Workers gained a citizen/profession layer mapped from contracts. Street litter now tracks dominant sources and residents react to accumulated exposure. Upgrade cards can highlight currently relevant city problems. City Hall request/upgrade UI now routes decisions through runtime command methods instead of owning the simulation logic. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-12: Kept the expanded social/economy/litter work under the project line-count rule by splitting local-bus passenger handling, street-litter cleanup/source helpers, and social runtime models into focused partial files. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`.

- 2026-05-12: Started executing the social-simulation backlog. Runtime models now separate cognition kind as Fact/Experience/Opinion/Rumor: building memories remain factual/neutral, conversation-topic memories are rumors, topic stances are opinions, and daily lived experience/social signals are experiences. End-of-day lived experience now emits capped `DailyExperience` social signals for Noosphere and later topic opinion formation. Added a `ConversationTopic` registry and first topic-association aliases for litter, food, gambling/bar, City Hall/governance, and safety so arbitrary player words can connect to relevant `SocialSignal` categories. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`.

- 2026-05-12: Added the first structured `SocialSignal` layer. Important resident/city events now record topic, tone, strength, confidence, source, place, reason, and day facts through `GameBootstrap.SocialSignals.cs`; thoughts emit non-duplicating signals, street-litter daily exposure creates a litter signal used by "Пережитый опыт", topic opinions record topic signals and can read recent related personal signals, and City Hall complaints/accept/reject/complete/expire decisions emit governance/request signals. The Noosphere HUD and 3D dive now include daily social-signal topic summaries/meaning words. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`.

- 2026-05-12: Added `ai/system-tree.md` as the stable informational hierarchy of project systems, subsystems, feature leaves, and cross-system links. `AGENTS.md`, `ai/README.md`, and `ai/prompt-templates.md` now make keeping this tree up to date an explicit project rule whenever systems, subsystem responsibilities, feature leaves, UI surfaces, or cross-system dependencies change. Verification: `git diff --check`, targeted mojibake scan.

- 2026-05-12: Fixed the new City Hall Requests/Upgrades tabs being visible but effectively unclickable. The tab buttons now get explicit layout height/flexible width, raycast-ready target graphics, non-raycast labels, no navigation capture, and immediate `UpdateCityHallScreenUi()` refresh on click, matching the safer tab setup used by other FleetCanvas screens. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Added MVP city upgrades purchasable from the City Hall. `GameBootstrap.CityUpgrades.cs` defines six trust-gated upgrade cards across Cleanliness, Economy, and Trust branches, tracks purchased upgrades for the current world, records Treasury spending in the ledger/feed, and applies first gameplay effects: lower street-litter growth, larger cleaner coverage radius, construction discounts, longer accepted request deadlines, and softer trust penalties. The City Hall canvas now has Requests/Upgrades tabs; `GameBootstrap.FleetCanvas.CityHallUpgrades.cs` renders the upgrade tree as cards with costs, trust gates, prerequisites, statuses, and buy buttons. Tutorial scenario was checked; this does not change the Tutorial-mode flow yet. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Added graded resident reactions to street litter. Each worker now samples visible street litter in a small radius while outside, accumulates daily litter exposure/peak values, and forms negative active thoughts only when the nearby mess becomes noticeable; every visible exposure still contributes a negative but scaled City factor to the end-of-day "Пережитый опыт" so one dirty cell is a small annoyance while sustained/multi-cell litter can become a major reason. Active cleaner-duty phases are excluded from litter perception while cleaning. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-12: Changed player road construction to an always-on start/end segment flow. Single-lane roads no longer build immediately on the first click or require Shift for path mode: the first left click selects the start and the second left click builds the segment with the existing road construction wave. Road previews now show the pending segment after the start click, two-way road tutorial goals now track start/end selection, and road Build descriptions/tutorial copy no longer teach Shift/R road-building legends. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, JSON parse, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-12: Added construction costs for buildable buildings. `build-catalog.json` now carries per-building `cost` values while roads remain free; the Build menu shows larger/high-contrast cost badges, disables unaffordable buildings, appends cost details to descriptions, and rejects hotkey/world placement attempts when Treasury is too low. Successful building placement now spends Treasury after the placement succeeds, records a money ledger entry/feed event, shows a negative Treasury popup, and spawns the same world spend-float animation used by worker service payments over the built location. Tutorial notes were updated: the required building chain currently costs `$3740` against `StartingTreasury = $5000`. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, JSON parse, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Fixed a resident crowd-separation rescue loop found at the end of `debug.log`. `Doru Muren` was repeatedly pushed from safe cell `(61,20)` into unsafe cell `(61,21)`, then rescued back every frame during idle life updates; pedestrian separation nudges now only apply when the proposed position remains on a safe walk cell and the small segment does not cross blocked walk cells. Inside-building residents are no longer considered nudgeable. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Added a cinematic start-of-day title. Each day rollover now triggers a large centered white `Day N` / `День N` IMGUI overlay that stays visible for 3 real seconds and then fades out without blocking gameplay or changing Tutorial flow. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Fixed mirrored words in the fullscreen Noosphere dive scene. The 3D meaning `TextMesh` billboards now use the camera's forward/up rotation so the readable side faces the viewer instead of rendering the back side. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Fixed Noosphere dive compile wiring after the new fullscreen scene split. The meanings rebuild/apply methods now live in `GameBootstrap.NoosphereDive.Meanings.cs` with the file restored under `Assets/`, and `Assembly-CSharp.csproj` explicitly includes both Noosphere dive partials so `RebuildNoosphereDiveMeanings` resolves during local builds. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Added a fullscreen 3D Noosphere dive mode. Clicking the animated Noosphere panel now fades the regular HUD into a RenderTexture-driven 3D scene with its own camera, pulsing core, orbit rings, connection spokes, and flying word-meanings built from the Noosphere knowledge log, resident topic opinions, and citywide lived experience. Escape, right click, or the close button returns to the regular Noosphere HUD. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`.

- 2026-05-12: Added citywide shared lived experience aggregation for the Noosphere HUD. After resident end-of-day opinions are finalized, `GameBootstrap.CityDailyExperience.cs` builds a capped city-level daily verdict with score, confidence, consensus, tension, dominant reason, and counterpoint from all active residents. The Noosphere summary/visual core now expose the latest city experience, and future arbitrary topic opinions receive a small background mood signal from it. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-12: Added per-resident `WorkerTopicOpinion` for arbitrary player conversation topics. Conversation-topic knowledge now forms a positive/negative stance by combining rumor connotation, source trust, repeated or contradictory versions, deterministic resident disposition, and the latest "Пережитый опыт"; the result is stored by rumor root/original topic, copied back into memory/opinion metadata, propagated through later rumor iterations, and used by topic reflection thoughts. Workers/Noosphere opinion labels now say positive/negative instead of useful/doubtful for these opinions. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

## Recent Summary

- 2026-05-11: Added street litter and Cleaning Depot/Cleaner gameplay, then tuned litter visibility, cleaner target selection, and runtime performance. Building walk buffers, safe exits, stop/intercity-stop handling, worker idle pathing, and Parking shift commutes were hardened after `debug.log` path/rescue loops.

- 2026-05-11: Reworked road/build presentation and controls: road construction wave, flattened road/highway overlays, stronger road-access warnings, right-click/Escape Build cancellation, numeric Build hotkeys, and less noisy local-bus empty-network logging.

- 2026-05-11 to 2026-05-10: Integrated imported Bar/Gambling Hall visuals and interactions. Bar/casino Resources models, imported door hinges, night-light metadata, visible seating, safer service exits, construction pop-in animation, hidden helper markers, and larger visual fitting replaced or extended the procedural fallbacks.

- 2026-05-10 to 2026-05-09: Expanded resident knowledge/Noosphere from simple topic memory into staged knowledge formation, timed building/topic memories, rumor iterations, knowledge sharing through social contexts, citywide canon knowledge, Noosphere timers, and the Residents `Knowledge` tab. Project version and Patch Notes baseline were bumped to `v.0.0.4`.

- 2026-05-08: Reworked compact building and resident microHUDs. City Hall, Motel, Warehouse, worker quick HUDs, guest submenu overlays, map tether lines, Event Feed layering, scroll/click behavior, and close/focus handling were tightened for the current UI style.

- 2026-05-08: Rebuilt the Build experience around the bottom dock. The Build menu now uses category icons and an animated tray, supports outside-click closing and category unfocus, keeps tray/category containers transparent where needed, and New Game unlocks buildings in staged layers instead of exposing everything after tutorial skip.

- 2026-05-08: Added and polished the large Bar interior scene. The Bar quick HUD can enter a separate paused interior with fade transitions, room ambience, patron idle/talk/drink/dance animation, readable TextMesh signs/bubbles, spatial vocalizer audio, and safer teardown/audio listener handling.

- 2026-05-08: Hardened worker idle placement and dialogue from `debug.log` review. Idle wandering, conversations, Motel idle slots, stationary idle actions, and overlap checks now reserve planned/current positions more carefully and avoid same-cell stacking.

- 2026-05-08: Updated Tutorial and staffing memory around the current player path. Normal play no longer has a permanent Vacancies top-HUD entry; Tutorial uses contextual Staffing/Labor Exchange flow, and `ai/tutorial-scenario.md` records the current `Обучение` sequence.

- 2026-05-08 to 2026-05-06: Expanded resident simulation. City Hall requests/trust, social-introduction requests, idle dialogue, Residents HUD, thoughts/opinions, social graph, inventory/Kiosk, families/children, Kindergarten, worker migration, contracts, needs, money, and worker focus were added or tuned.

- 2026-05-06 to 2026-05-04: Continued transport/economy/system work. Trade services, schedule helpers, Docks/regional trade, Labor Exchange automation, service staffing, personal cars, resource simplification, audio/options, Event Feed, demolition, and runtime workflow rules were implemented or hardened.

- 2026-05-03: Completed broad 900-line runtime partial cleanup. Oversized `GameBootstrap` partials were split by concern, owner map and architecture notes were refreshed, line-count tooling/CI default to 900 lines, and editor smoke tests cover road build, transport/trade, vacancies, and tutorial goals.

- 2026-05-03: Started moving data/text out of large UI C# files. `Assets/Scripts/Runtime/Data/` loaders and JSON catalogs now back Patch Notes and Build menu content first, with C# fallbacks kept.

- 2026-05-02 to 2026-04-06: The project evolved from a runtime-generated grid logistics prototype into the current town-sim slice with tutorial, workers/needs, production, local buses, regional trade, racing, water/ambient life, audio/options, and many extracted service seams.

## Active Notes / Watchouts

- Keep `ai/work-log.md` short. If it grows beyond roughly 120-160 lines, collapse older completed items into this summary format again.

- Code remains source of truth. The project has many partial `GameBootstrap.*.cs` files; memory is only a navigation aid.

- Use `ai/systems-map.md` -> `System Owner Map` as the first navigation pass for implementation, bugfix, refactor, and investigation tasks.

- When changing files listed in an owner card, update `ai/systems-map.md` only if paths, ownership, or responsibilities changed.

- Prefer `./tools/check-all.ps1` before commits or after risky code edits. Use `-SkipSmokeTests` for a fast local pass when Unity is already open or unavailable.

- For memory-only edits, a text/diff check is enough; no Unity build is needed unless code changed.

- Avoid unsafe shell rewrites for localized files. Prefer `apply_patch`, keep text UTF-8, and scan touched localized UI/HUD/tutorial files for mojibake markers after edits.

- Avoid broad Canvas localization passes immediately after dynamic HUD redraws unless necessary; several HUDs intentionally use direct localized strings to avoid mixed-language output.

- Tutorial flow is sensitive to timing, overlays, highlights, goals, and camera state. When changing tutorial steps, verify OK click behavior, skip behavior, menu highlight cleanup, and whether regular HUDs should be hidden or preserved.

- Quick HUDs should not open automatically after building placement and should close when larger HUD windows open. Fleet details are an exception: preserve Fleet's internal selected-truck state while Fleet is open.

- After adding or changing HUD/Canvas buttons, explicitly verify clickability: Canvas has an EventSystem/GraphicRaycaster, every `Button` has a `targetGraphic` and listener path, text/decorative images do not block raycasts unless they are the button target, overlay close buttons are last-sibling but small, and the new panel is included in `CloseAllMenus`/blocking-HUD state.
