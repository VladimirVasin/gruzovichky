# Work Log

Last updated: 2026-04-29

Purpose: compact active memory for recent work. Older detailed history was intentionally collapsed on 2026-04-20 to keep agent startup light. Use git history for exact old implementation details.

## Recent Detailed Work

- 2026-04-30: Bumped the manual main-menu version label to `Lo-fi Delivery Co. v.0.0.3`. Added a new `v.0.0.3` in-development section to `ai/release-notes.md` that explicitly treats `v.0.0.2` as the previous public baseline, so future Patch Notes updates can compare recent work-log/code changes against that release and record only player-facing changes.

- 2026-04-29: Removed the superseded legacy tutorial mode after the rebuilt User tutorial replaced it. The tutorial trigger enum now keeps only the active User-mode sequence plus the bee easter egg; Debug mode no longer schedules the old startup tutorial, legacy Fleet/Forest/Sawmill tutorial chains were removed, old red-outline hooks were disconnected, and the unused ProductionOrbit tutorial partial was deleted. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, legacy tutorial symbol scan, and touched-file mojibake scan.

- 2026-04-29: Rewrote the in-game `v.0.0.2` Patch Notes and `ai/release-notes.md` baseline to focus on player-facing mechanics only. The changelog now groups the release around Workers, Production, Transport, Economy/Trade, Town Services, and World/Atmosphere, while removing tutorial/debug/internal implementation details from the visible notes.

- 2026-04-29: Made rebuilt User tutorial goal HUD instructions more explicit. Goal subtitles and individual objective labels now name the exact top HUD menu, key, tab, button, or assignment path the player should use (for example Build/B, Vacancies -> Shift -> Worker, Economy -> Taxes, Economy -> Trade), and the goals panel was widened/tallened slightly so longer actionable labels remain readable.

- 2026-04-29: Added the current final rebuilt User tutorial window after successful Join-the-Race completion. When the race finish sequence returns to city mode, the tutorial schedules a non-pausing `Demo Complete` screen explaining that the demo is over for now, suggesting `New Game Clear`, reminding about the F9 Debug window, and thanking the player. The new screen is counted as step 28/28 and resets with the rest of the User tutorial state.

- 2026-04-29: Extended the rebuilt User tutorial from Economy/Taxes into Trade/Race onboarding. Completing the 15% tax objective now opens a Trade tutorial, injects one free parked tutorial truck if no unassigned truck is available, then starts goals for assigning an Intercity driver and creating a Buy Alcohol order from Economy -> Trade. Completing those goals auto-kicks trade dispatch, focuses the camera on the trade truck without selecting it/opening quick HUD, shows a non-pausing Join-the-Race explanation, and adds a final objective completed by starting the race minigame. Split tutorial truck camera helpers into `GameBootstrap.Tutorial.CameraFocus.cs` to keep line-count checks green. Verified build, line-count, `git diff --check`, and added-line mojibake scan.

- 2026-04-29: Added the next rebuilt User tutorial step for Economy/Taxes after the local bus route explanation. The new non-pausing tutorial explains that service-building banks are taxed daily at 00:00 into the global Treasury, then starts a goals HUD objective to set the tax rate to 15%. Economy tax `+/-` buttons now notify the tutorial goal checker immediately, with progress logged under `TUTORIAL`. Verified line-count check and added-line mojibake scan.

- 2026-04-29: Fixed Intercity vacancy assignment so it now includes the same explicit truck-selection step required by trade dispatch. The Vacancies flow shows `Shift -> Truck -> Worker` for Intercity, records the assigned truck on the occupied vacancy, assigns the selected worker to the truck roster before switching them to `Intercity`, and rejects direct intercity assignment without a truck to avoid the later `Intercity driver needs an assigned truck` dispatch block. Intercity removal now also frees the truck roster slot, and the F9 auto-assign helper reserves a truck slot before assigning intercity duty. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and touched-file mojibake scan.

- 2026-04-29: Added the next User tutorial local-transport step after Warehouse loaders. Completing the 3-loader goal now schedules a transport explanation, unlocks local bus stops, and starts a new objective requiring exactly 2 city bus stops plus 3 assigned bus drivers. Stop placement and bus-driver assignment notify the tutorial goal checker, progress is logged under `TUTORIAL`, and completion shows a final bus-route explanation covering stop numbering, sequential routes, passengers, `$1` fares, and Parking bank transfer. Split tutorial panel-open helpers into `GameBootstrap.Tutorial.PanelActions.cs` to keep line-count checks green. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and added-line mojibake scan.

- 2026-04-29: Extended the post-hiring User tutorial flow. Tutorial worker-wave buses now bypass ambient lane waiting so the 7-worker arrival appears immediately, and the tutorial schedules a new Resources/Warehouse explanation after that wave disembarks. Added a new Warehouse Loaders goal that completes when 3 workers are assigned to Warehouse production slots, with progress logged through `TUTORIAL`. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and added-line mojibake scan.

- 2026-04-29: Extended the rebuilt User tutorial worker step. The final Workers objective now requires both opening Workers and hiring a new worker; tutorial-only hiring closes the Workers HUD, completes the objective, follows the arrival bus with a non-pausing tutorial window, brings a one-time wave of 7 workers, and unlocks all build tools/vacancies after that bus explanation. Hiring arrival buses now support multiple workers while preserving normal one-worker hiring outside that tutorial wave. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and added-line mojibake scan.

- 2026-04-29: Fixed two-lane road build preview drift on corners. Shift-drag preview for `BuildTool.Road` now uses the same continuing-footprint offset resolution as actual placement and also previews turn-fill cells, so curved/two-segment road previews no longer appear one cell away from the final built result. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, and `git diff --check`.

- 2026-04-29: Added a tiny one-per-world squirrel memorial easter egg sign near the first eligible forest/lumberyard or City Park decoration. The sign reads `В память о белке, остановившей смену.` and resets between world generations so it remains a rare map detail rather than a repeated prop. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and changed-line mojibake scan.

- 2026-04-29: Added Gas Station to the rebuilt User tutorial service-building objective. Gas Station is now a buildable service tool unlocked alongside Bar / Gambling Hall / Canteen / City Park, has manual placement/preview support, completes a new `BuildGasStation` tutorial goal, and shows its own non-pausing tutorial HUD that frames it as servicing truck needs/fuel. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and changed-line mojibake scan.

- 2026-04-29: Fixed an early User/tutorial refuel crash when `GasStation` is not built yet. Refuel runtime now uses guarded `TryGetValue` access for Gas Station/Parking, cancels impossible refuel orders with a `FUEL` debug entry instead of throwing `KeyNotFoundException`, and both auto/manual refuel entry points now refuse to queue refuel before a Gas Station exists. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and a direct scan for unsafe `locations[LocationType.GasStation]` access.

- 2026-04-29: Fixed weather/squirrel regressions after the new weather pass. Foggy weather is now much lighter (`FogMult` no longer collapses view distance into a wall, and runtime fog distances are clamped to safe minimums). Day/night music now re-unpauses the active layer if it was left paused, preserving crossfades after weather/time transitions. `StartSquirrelClimbDown` now recovers from invalid squirrel roam indices instead of throwing `ArgumentOutOfRangeException`, and squirrel roam helpers were split into `GameBootstrap.AmbientLife.Squirrels.cs` to keep line-count checks green. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and touched-file mojibake scan.

- 2026-04-28: Extended the rebuilt User tutorial after the first truck-driver assignment. Completing the first freight goals now focuses the camera on the assigned truck and shows a non-pausing/no-dim freight explanation, then teaches worker leisure, unlocks Bar / Gambling Hall / Canteen / City Park for the next build objective, shows purpose popups for each built service building, and finishes with a Workers overview goal that completes when the Workers HUD is opened. Also split runtime model classes into `GameBootstrap.RuntimeModels.cs` and grid mesh visuals into `GameBootstrap.GridVisuals.cs` to keep CI line-count checks green. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and touched-file mojibake scan.

- 2026-04-28: Added ambient falling leaves as a lightweight misc animation. A 30-object pooled `AmbientFallingLeaves` system now spawns small flat orange/yellow/red leaf cubes near registered misc-tree crown/perch points, spirals them downward with drift and rotation, then respawns them above another tree when they reach the ground. The effect is initialized with wildlife setup and disabled under far-zoom visual LOD with other small ambient particles. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, and `git diff --check`.

- 2026-04-28: Removed extra truck side-movement caused by isolated turn-fill cells. `physical-right-lane` targeting now requires the neighboring right-lane road cell to be continuous with the previous or next right-lane cell, so single road cells at corners/intersections no longer make the truck jump sideways before continuing along the logical route. Verified `dotnet build .\Assembly-CSharp.csproj -nologo` and line-count check.

- 2026-04-28: Fixed truck visual lane targeting when the pathfinder chooses the opposite physical cell of a two-lane road pair. Ordinary-road lane offset now checks for an actual road cell on the physical right side of travel; when present, the truck target shifts into that neighboring right-lane cell plus the small in-cell offset, logged as `physical-right-lane`. This prevents destinations across the road from making trucks visually drive the left lane until the driveway approach. Verified `dotnet build .\Assembly-CSharp.csproj -nologo` and line-count check.

- 2026-04-28: Refined truck lane targeting near building access anchors. Corner-aware lane targeting is now disabled within one road cell of an anchor, so a destination on the opposite side of a two-lane road no longer forces an early lane swap before the explicit driveway/anchor segment. Trucks keep the ordinary right-lane offset until the final anchor approach. Verified `dotnet build .\Assembly-CSharp.csproj -nologo` and line-count check.

- 2026-04-28: Fixed truck lane targeting on ordinary two-lane road corners. `BeginNextTruckSegment()` now uses a corner-aware lane target: if the next cell is a turn, the truck aims at the outgoing segment's right-lane offset before entering the corner, preventing the visible lane jump/rebuild at L-shaped road joins. `TRUCK_LANE` now reports `corner-outgoing-*` reasons for these cases. Verified `dotnet build .\Assembly-CSharp.csproj -nologo` and line-count check.

- 2026-04-28: Added per-segment truck lane telemetry for ordinary roads. Each `BeginNextTruckSegment()` now writes a `TRUCK_LANE` debug entry with from/to cells, normalized direction, lane-offset reason, offset vector, base target, final target, and anchor/road flags, making it possible to distinguish wrong-side offset bugs from path/anchor/dead-end zero-offset cases. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`.

- 2026-04-28: Fixed ordinary two-lane road right-hand movement for trucks/local buses. Runtime lane offset now matches `TwoLaneRoadGeometry.GetRightLaneOffset`: travel `+X` uses the south lane and travel `+Y` uses the east lane, correcting the previous left-hand-looking offset on generated/player-built roads.

- 2026-04-28: Fixed Debug-mode build unlock separation from the rebuilt User tutorial gates. `InitUnlockedBuildTools()` now gives `New Game User` only the tutorial road tools, while non-User modes such as Debug immediately unlock every build tool. Verified the Vacancies gate already returns unlocked for any non-User mode, so Debug vacancies are not restricted by tutorial progression.

- 2026-04-28: Fixed the rebuilt User tutorial truck-driver completion flow. When a worker is assigned to a Truck Driver vacancy during the active Buy Truck tutorial goals, the Vacancies HUD now auto-closes after completing `AssignTruckDriverShift`, matching the Lumberjack Camp tutorial-only close behavior without changing normal assignment UX. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and touched-file mojibake scan.

- 2026-04-28: Added a fallback local freight route for early User-mode logistics. Trucks can now haul `Logs` from `Lumberyard/Forest` to `Warehouse` when the higher-priority `Lumberyard -> Sawmill` route is unavailable, so buying/assigning the first truck before building a Sawmill no longer leaves auto mode with no possible trip. Warehouse unloading now stores cargo-aware `Logs` or `Boards` instead of treating every generic warehouse unload as boards. Also trimmed `GameBootstrap.AmbientLife.cs` back under the 1500-line CI limit without changing behavior. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and touched-file mojibake scan.

- 2026-04-28: Fixed the rebuilt User tutorial truck-driver assignment objective. Successful assignment through the Vacancies Truck Driver flow now completes `AssignTruckDriverShift` immediately after a worker is assigned to a truck and shift, so the goal follows the actual assignment result rather than an older Fleet-specific tutorial path. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and touched-file mojibake scan.

- 2026-04-28: Refined the first purchased-truck tutorial presentation. The arrival tutorial now follows the truck with the camera without selecting it or opening the Truck quick HUD, uses a transparent overlay instead of dimming the screen, and positions the tutorial card slightly up/right so the incoming truck remains visible. Verified `dotnet build Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and touched-file mojibake scan.

- 2026-04-28: Fixed the rebuilt User tutorial freight step so the player can actually find the first-truck purchase path. Closing the `Set Up Freight` tutorial now unlocks the `Truck Driver` vacancy (without unlocking all later vacancies), and that tutorial text now explicitly explains that the first truck is bought from `Vacancies -> Truck Driver -> Transport Park` for `$300`. Reset logic clears the truck-driver vacancy gate on new tutorial starts, while skipping tutorial still unlocks all vacancies. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, `powershell -ExecutionPolicy Bypass -File .\tools\check-line-count.ps1 -MaxLines 1500`, `git diff --check`, and a changed-line mojibake scan.

- 2026-04-28: Moved truck buying out of the Vacancies assignment option chain into a dedicated `Transport Park` card that appears only while assigning a Truck Driver vacancy. The Truck Driver vacancy still remains visible even when no free trucks exist; after selecting a shift, the truck step now shows a disabled explanatory row when a free truck is required, and the new card shows current fleet capacity plus a clickable buy button/status. Also repaired mojibake in the Vacancies runtime file while touching localized vacancy UI strings. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, `powershell -ExecutionPolicy Bypass -File .\tools\check-line-count.ps1 -MaxLines 1500`, `git diff --check`, and a changed-line mojibake scan.

- 2026-04-28: Extended the rebuilt User tutorial after the Lumberjack Camp assignment goals. Completing the lumberjack goals now schedules two new tutorial HUDs: one explaining production shifts and end-of-shift wages, then one explaining that production resources must be moved to the Warehouse by freight. Closing the freight explanation starts a new goals HUD with two display-only objectives for now: buy the first truck and assign a truck driver to a shift. Verified `dotnet build .\Assembly-CSharp.csproj -nologo`, `powershell -ExecutionPolicy Bypass -File .\tools\check-line-count.ps1 -MaxLines 1500`, and a changed-line mojibake scan.

- 2026-04-28: Refined the rebuilt User tutorial lumberjack assignment step. After a successful `LocationType.Forest` production assignment while the Lumberjack Camp tutorial goals are active, the Vacancies HUD now closes immediately so the completed objective and next tutorial flow are visible without manual close. Verified `dotnet build .\Assembly-CSharp.csproj -nologo` and `powershell -ExecutionPolicy Bypass -File .\tools\check-line-count.ps1 -MaxLines 1500`.

- 2026-04-28: Fixed the rebuilt User tutorial lumberjack assignment objective not completing after assigning a worker through the new Vacancies HUD. The central production assignment helper now marks `AssignLumberjackWorker` complete whenever a worker is actually assigned to `LocationType.Forest`, so the goal follows the real assignment result rather than a specific UI flow. Verified `dotnet build .\Assembly-CSharp.csproj -nologo` and `powershell -ExecutionPolicy Bypass -File .\tools\check-line-count.ps1 -MaxLines 1500`.

- 2026-04-28: Stabilized Shift-drag two-lane road construction across straight path continuations. Player-built road paths now preserve the previous segment's chosen side-lane offset on same-direction segments before falling back to generic footprint resolution, preventing late-path cells from snapping to an unrelated adjacent road side and leaving inconsistent two-lane seams. Added fixed-offset `BUILD_ROAD` logging for this path. Verified `dotnet build .\Assembly-CSharp.csproj -nologo` and `powershell -ExecutionPolicy Bypass -File .\tools\check-line-count.ps1 -MaxLines 1500`.

- 2026-04-28: Fixed single-shift vacancy progression in the Vacancies HUD. Single-shift vacancies use the sentinel `SingleShiftVacancySelection = -2`, but the flow still treated any negative `selectedVacancyShiftIndex` as "no shift selected"; added `HasSelectedVacancyShift(...)` so production/intercity single-shift selections advance to the worker step while truck/bus vacancies still require a real shift index. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `powershell -ExecutionPolicy Bypass -File .\tools\check-line-count.ps1 -MaxLines 1500`, `git diff --check`, and touched-file mojibake scan.

- 2026-04-28: Fixed the Vacancies assignment flow option rows so the single-shift selection card is clickable. Vacancy option rows now treat the row background as the `Button.targetGraphic`, while the nested title/subtitle/badge graphics opt out of raycasts; this keeps the whole card as one click target instead of allowing decorative child UI to swallow pointer hits. Verified `dotnet build Assembly-CSharp.csproj -nologo` and `powershell -ExecutionPolicy Bypass -File .\tools\check-line-count.ps1 -MaxLines 1500`; touched-file mojibake scan returned no matches, while `git diff --check` could not be completed in the current shell wrapper because `git -C` was still invoked outside the repo context.

- 2026-04-28: Updated rebuilt User tutorial copy for early construction. The first road tutorial now explicitly says the road must connect to the Highway, and the first core-buildings tutorial now explains that every building needs road access for workers and vehicles to use it properly. Added matching Russian localization entries. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, `git diff --check`, and a mojibake scan on touched tutorial/localization files.

- 2026-04-28: Tightened the rebuilt User tutorial UI gates. `TutorialGoalsCanvas` now renders below normal HUD panels/quick HUDs, the User-mode vacancy list is tutorial-gated so only the Lumberjack Camp production vacancy is visible until the tutorial is skipped/unlocked, and checking `Skip tutorial` now unlocks all build tools and vacancies immediately. The Lumberjack Camp assignment prompt no longer auto-opens the Vacancies menu after OK; players must open it manually as instructed. Also fixed the build-mode legend text color by resetting `GUI.color` to white after drawing the dark IMGUI background, so `R - rotate` and `Shift - drag road` no longer inherit the black panel tint. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, `git diff --check`, and a mojibake scan on touched tutorial/vacancy/build-legend files.

- 2026-04-28: Reworked the old `Roles` screen into a first-pass `Vacancies` HUD. The top menu label is now `Vacancies` / `Вакансии`; the screen renders open and occupied vacancies on the left, then uses a staged right-side flow: choose vacancy, choose shift where needed, choose truck for truck-driver work, then choose an available worker. Production, Intercity, Bus Driver, and Truck Driver assignments reuse the existing assignment/removal helpers; truck buying is still available inside the truck-driver vacancy flow. The new vacancy runtime lives in `GameBootstrap.FleetCanvas.VacanciesRuntime.cs` to keep `ShiftsRuntime` under the 1500-line CI limit. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, `git diff --check`, and a UTF-8/mojibake marker check on touched vacancy/localization files.

- 2026-04-28: Extended the rebuilt `New Game User` onboarding into truck logistics. After the Lumberjack Camp assignment explanation, the tutorial now explains that trucks move resources, opens Fleet, and starts a `Buy first truck` goal. Buying a truck in any mode now creates it off-map on the edge highway, drives it along the highway, then routes it into the Parking slot instead of spawning instantly inside Parking. In User tutorial mode, the Fleet panel closes, camera follow locks onto the purchased truck, and a non-pausing tutorial HUD explains that the new truck is arriving. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on touched tutorial/Fleet/truck-arrival files.

- 2026-04-28: Fixed mixed-language strings in the rebuilt User tutorial truck steps. Tutorial steps 10/11 now branch explicitly on current language for both title and body instead of combining localized labels with English copy. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake/mixed-title scan on `GameBootstrap.Tutorial.cs`.

- 2026-04-28: Adjusted the User tutorial camera after first truck purchase. The purchase tutorial no longer uses the normal close behind-the-truck follow camera; it immediately starts a dedicated tutorial camera follow on the purchased truck with a higher RTS-style top-down offset, then releases back toward the diorama camera when the non-pausing arrival tutorial is closed. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on touched tutorial camera files.

- 2026-04-28: Disabled the legacy red top-menu Fleet outline during the rebuilt User tutorial's `Buy the first truck` step. The step now relies on text + goals HUD instead of the old persistent tutorial button outline. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on `GameBootstrap.Tutorial.cs`.

- 2026-04-28: Fixed purchased-truck arrival drifting one cell beside player-built two-lane roads. The arrival waypoint planner now chooses the nearest actual `roadCells` cell to Parking as the approach point before entering the Parking slot, instead of driving through `Parking.Anchor`; the debug log now records `approachRoad=(x,y)` and `parkingAnchor=(x,y)` for future route diagnosis. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on `GameBootstrap.TruckPurchaseArrival.cs`.

- 2026-04-28: Continued the rebuilt `New Game User` onboarding into first production. After the core-building goals complete, the tutorial now unlocks `Lumberjack Camp`, explains that it should be built near dense forest, and starts goals to build the camp and assign a worker there. Building the camp marks the build goal and opens a localized explanation to use `Roles > Production`; assigning any worker to the camp marks the assignment goal, closes the Roles panel, and shows a localized explanation of the worker's logging loop. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on touched tutorial/localization/world files.

- 2026-04-28: Extended the rebuilt `New Game User` onboarding after road construction. Completing the road-building goals now schedules a tutorial step that unlocks the three core building tools (`Warehouse`, `Motel`, `Parking`), then starts a new goals HUD requiring all three to be built. Successful placement of each core building marks its goal and shows a short localized tutorial explanation of that building's role. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on touched tutorial/localization/world files.

- 2026-04-28: Updated Build mode placement rules. Misc decorations are no longer structural blockers for player construction: roads and building footprints remove misc props on occupied cells instead of rejecting placement. Player-built buildings now prepare their footprint by flattening terrain under the building pad and refreshing nearby terrain meshes, while water/beach cells and the edge highway remain hard blockers. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on touched build/world files.

- 2026-04-27: Fixed the rebuilt User tutorial objectives HUD so each goal screen only accepts goals from its own active objective set. Camera inputs now cannot complete the road-building objectives; road goals only complete from actual single-cell road placement and Shift-drag road placement. Also changed the first User tutorial bus-stop camera focus from an instant jump to a smooth tutorial camera move with its own close stop-focused offset. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on touched tutorial files.

- 2026-04-27: Extended the rebuilt User tutorial road step. Tutorial 2 now explicitly tells the player to open Build from the top HUD or press `B`; after OK it starts a second goals HUD mode for road construction with two checklist goals: place a single road cell via left click, and build a longer segment via Shift-drag. Successful road placement marks these goals from the actual road build path. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on touched tutorial/build files.

- 2026-04-27: Simplified the first two rebuilt User tutorial texts into direct instructional copy in both English and Russian. Also fixed Build menu tutorial gating: locked build cards are now explicitly deactivated before category rendering, so buildings no longer appear as available without categories when only road tools are unlocked. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on touched tutorial/build/localization files.

- 2026-04-27: Adjusted the rebuilt `New Game User` tutorial opening. User mode now starts on the default camera first, delays the start-stop focus and welcome tutorial by 2 seconds, starts the camera-control goals only after the player presses OK on tutorial step 1, then shows a new step 2/2 road-building tutorial after the goals success animation and an additional delay. The first tutorial text now includes a wry camera-control line, and the goals HUD Russian text was cleaned from mojibake. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on touched tutorial/localization files.

- 2026-04-27: Fixed the build-mode control legend readability. The bottom-right `R - rotate` legend now forces white text through both `GUI.contentColor` and all GUIStyle text states, and road tools (`SingleRoad` and `Road`) show an extra `Shift - drag road` hint in the same panel. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on touched HUD/localization files.

- 2026-04-27: Added the first tutorial goals HUD for `New Game User`. A non-blocking left-side `TutorialGoalsCanvas` now appears under the top HUD buttons with camera-control goals (`Zoom In`, `Zoom Out`, map pan, `Q/E` rotate), marks checkbox rows as the player performs the inputs, logs goal completion under `TUTORIAL`, and plays a green success flash/pulse using the Gambling Hall win sound/style before hiding. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a mojibake scan on touched HUD/input files.

- 2026-04-27: Changed Build menu default unlocks to road-only. `InitUnlockedBuildTools()` now unlocks only `SingleRoad` and `Road`, all building tools stay hidden until a script/tutorial explicitly calls `UnlockBuildTool(...)`, and the fallback unlock check no longer exposes every tool if the unlock set has not been initialized yet. Verified `dotnet build Assembly-CSharp.csproj -nologo` and `./tools/check-line-count.ps1 -MaxLines 1500`.

- 2026-04-27: Started the rebuilt `New Game User` tutorial flow. User mode now opens with a new `UserWelcome` tutorial screen numbered from `1/1`, focuses the camera on the generated Intercity Bus Stop with a closer start-stop composition, and keeps legacy tutorial triggers disabled for User mode until the new onboarding chain is rebuilt. Added Russian localization for the new welcome body. Verified `dotnet build Assembly-CSharp.csproj -nologo` and `./tools/check-line-count.ps1 -MaxLines 1500`.

- 2026-04-27: Retuned the city-builder camera zoom curve. The RTS/diarama pitch now stays unchanged until the closest 20% of zoom range, then drops much harder toward a low near-ground angle (`8°`) with lower close-zoom height/distance so the horizon becomes visible at maximum zoom-in. Verified `dotnet build Assembly-CSharp.csproj -nologo` and `./tools/check-line-count.ps1 -MaxLines 1500`.

- 2026-04-27: Restored the CI line-count gate after `GameBootstrap.World.cs` reached 1560 lines. Moved the Craftsman and Split-Level personal-house visual variants into `GameBootstrap.World.PersonalHouseVariants.cs`, keeping behavior unchanged while bringing `GameBootstrap.World.cs` down to 1463 lines. Verified `./tools/check-line-count.ps1 -MaxLines 1500` and `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Fixed ambient cats teleporting from the Intercity Bus Stop to Motel when the first Motel is built. Motel placement now retargets existing cat roam points and forces current cats to walk to the new home points before returning to normal idle/sleep behavior, including at night; cats are only respawned as a fallback if none exist. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Increased world vegetation density after the larger map made misc props feel too sparse. `MiscTreePlanner` now targets a much higher density with a grid-scaled cap of roughly `area / 13` instead of `area / 48`, and the minimum misc count was raised for smaller starts. Berry bushes are now more common while flowers are slightly rarer, giving the map more trees and low shrub mass without allowing props on roads, buildings, water, or beach cells. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Fixed the remaining far-zoom LOD regressions reported after the first pass. Road/building lanterns now update directly from the day-night cycle instead of piggybacking on truck headlight updates, so they still light at night when `New Game User` starts with 0 trucks. Dense-forest/lumber trees now still register misc sway and perch points, while falling/felled lumber trees are skipped by wind sway, restoring visible tree animation and wildlife spawn anchors for birds/squirrels. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Restored the one-lane road as a separate Build menu tool. `SingleRoad` now appears in Infrastructure beside `Two-Way Road`, builds exactly one road cell, supports Shift-drag path building with a one-cell preview/start marker, and shares the road removal/hover flow without changing the two-cell two-way road tool. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Narrowed far-zoom visual LOD after it froze too much of the living world. Water effects now update at the normal animation cadence even when zoomed out, water body meshes keep wave-height animation under water LOD, and tree sway plus ambient birds/cats/squirrels/bees/moths/fish continue updating instead of being root-disabled. Far LOD still culls grid lines, ambient air, exhaust/dirt dust particles, and shadows for performance. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Fixed night lights being unintentionally disabled by far-zoom visual LOD. The optimization pass still culls ambient roots/particles at distant camera heights, but road lanterns and building/location night lights now remain controlled only by the normal day/night darkness logic so they keep glowing at night. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Removed the generated central starter road loop from `New Game User`. User mode now skips initial city-road generation entirely, leaving road construction to the player while preserving Debug-mode generated road chains and the edge/highway systems. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Made `New Game User` a true empty-build start for core logistics buildings. User generation now creates only the system Intercity Bus Stop and the starter road loop; Parking and Warehouse are skipped from world generation and must be placed from Build menu. Added `Warehouse` as a build tool, wired both Parking and Warehouse placement/previews, and changed startup truck spawning so User mode defaults to `0` trucks when Parking is absent while still spawning the starter workers at the bus stop. Fleet truck purchase is now guarded until Parking exists. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Fixed Shift-drag two-way road preview frustration. The road path start marker now has a second side-lane highlight object and both start tiles are positioned/oriented together, so holding Shift and choosing the start of a two-cell road shows the full two-lane footprint instead of only one highlighted cell. Build logic/pathfinding is unchanged. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Added a first safe visual pass for smoother road corners while keeping road logic grid-based. Unified road rebuild now detects corner cells and adds terrain-colored curved corner masks plus a thin curved shoulder arc over the square road tile, so L-turns read as rounded bends instead of purely blocky cell corners. Pathfinding, roadCells, building access, and transport movement remain unchanged for safety. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Reduced build-mode road placement hitches by batching manual road mutations. Single road placement and Shift-drag road paths now suppress repeated unified-road mesh rebuilds and roadside-prop refreshes while cells are being added, then flush affected road visuals, lanterns, benches, signs, moths, and the unified road mesh once at the end of the operation. This keeps placement behavior unchanged while avoiding per-cell full visual refresh spikes on the larger map. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Reopened `New Game User` as a lean build-first start and disabled the old User tutorial flow while onboarding is redesigned. User mode now starts with only Parking, Warehouse, one Intercity Bus Stop, one truck, three workers idling around the stop, and a small central two-lane road loop; Gas Station, Lumberjack Camp/Forest, Motel, and Sawmill are no longer generated up front. Starter cats prefer the stop while there is no Motel, and building a Motel rebuilds cat idle points and moves starter idle workers toward the Motel idle area. Also fixed a pre-existing mojibake dash in the Workers car-detail fallback that was blocking compilation. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Added a stricter starter-road anti-3-lane guard after Warehouse-area road artifacts still appeared visually despite the previous access taper. Generated road cells now refuse to add a new lane cell when two parallel road cells already exist on the same width side for the current road direction, preventing patterns like an existing two-lane pair `75/76` being widened into `75/76/77` by a later generated route. The guard logs skipped cells under `ROAD` for future debugging. Verified `dotnet build Assembly-CSharp.csproj` with 0 errors and 0 warnings.

- 2026-04-27: Reduced Warehouse-side starter-road triple-cell artifacts by tapering generated building driveways. Starter road generation now treats terminal `RoadAccess` cells as one-cell driveway endpoints instead of always adding the paired second lane directly against the building frontage. This keeps the main two-lane road intact while preventing Warehouse-style hubs from accumulating a third parallel road cell when several generated connections branch out from the same access point. Verified `dotnet build Assembly-CSharp.csproj` with 0 errors and 0 warnings.

- 2026-04-27: Fixed the remaining one-cell two-lane road shift beside the generated Intercity Stop. The bus stop road access now sits two cells to the side of the stop anchor instead of one, so the stop no longer occupies the candidate side lane used by the two-lane starter-road footprint. This keeps the final two road cells aligned beside the stop instead of forcing the last tile to jump outward by one cell. Verified `dotnet build Assembly-CSharp.csproj` with 0 errors and 0 warnings.

- 2026-04-27: Corrected the generated Intercity Stop frontage placement. The stop no longer uses the generic south-facing building placement that put its visual cells one row away from the two-lane road access. It now has a dedicated 2x1 side-of-road placement with the stop cells immediately to the left of the road access row, matching the intended pattern where the stop sits beside the road and the two-lane road occupies the adjacent cells. Verified `dotnet build Assembly-CSharp.csproj` with 0 errors and 0 warnings.

- 2026-04-27: Adjusted generated road access placement for service/frontage buildings after `debug.log` still showed ugly road formation beside Gas Station and the intercity stop. Generated building `RoadAccess` cells now sit two cells outward from the anchor by default, giving two-lane starter roads room to form without touching the building frontage. The intercity bus stop now uses a side access cell on the city side instead of an access cell on the highway lane, preventing regular starter roads from being generated directly across the edge highway. Verified `dotnet build Assembly-CSharp.csproj` with 0 errors and 0 warnings.

- 2026-04-27: Fixed another road-generation cause around service buildings such as Gas Station and Motel. Debug logs showed starter-road pathfinding could route through building anchor cells as intermediate cells because `IsAnchorCell` was treated as always build-passable; the road builder then skipped those blocked anchor cells, producing broken/ugly approach footprints. Road-build pathfinding and generated-layout road validation now only allow `start`, `goal`, or existing road cells directly, while normal blocked-location rules handle intermediate building anchors. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Reduced malformed starter-road branching beside Motel. Debug logs showed the layout built `Warehouse -> Motel` and then immediately built a second `Motel -> Intercity Stop` wide-road route from the same motel access, creating an ugly extra branch around the motel frontage. Starter road generation now connects `Intercity Stop` from `Warehouse` instead; debug layout validation was updated to require `Warehouse -> BusStop` feasibility while Motel remains connected through `Warehouse -> Motel`. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Added generated natural zones to the 128x128 world. World boot now creates several forest, hill, and lake zones before layout generation; lake cells are merged into `waterCells` so generated roads/buildings avoid them, lake shore cells are treated as beach/no-misc cells, forest zones force grass and raise misc-tree priority, and hill zones add strong smooth terrain height boosts before flattening building pads. `debug.log` now records natural zone counts and lake/beach cell counts. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Fixed the remaining malformed two-lane road corners near starter buildings. Straight starter-road segments already preserved their chosen lane side, but corner fill still used the canonical right-lane offset and could draw extra cells on the opposite side of a bend, e.g. a road running on rows `13/14` suddenly filling row `12`. Road turn fill now derives its bounds from the actually existing side lane at both sides of the turn. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Fixed a starter-road generation artifact around building entrances such as Parking, Gas Station, and Motel. The two-lane road builder now preserves the previously chosen side-lane offset along straight starter-road segments, so a road that was forced away from a building anchor does not immediately flip to the opposite side on the next cell and create a jagged/triple-looking frontage. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Increased far-distance city-builder zoom-out acceleration. Mouse-wheel zoom-out now uses a stronger distance-weighted curve (`0.55` to `6.2`) so pulling back from a large map gets progressively faster the farther the camera already is, while zoom-in keeps the previous smoother curve for close-control precision. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-26: Added the first pass of `Car Market` and personal worker cars. `CarMarket` is now a buildable 5x5 service location in the debug Build menu with a procedural lot/office/three display cars, and workers with at least `$100` can choose a `BuyCar` life goal, travel to the market, pay into the building bank, receive a random Sedan/Pickup/Hatchback, and spawn the owned car visually near Parking. Workers HUD and Driver Quick HUD now show the owned car, and Car Market quick HUD shows its bank. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-26: Tuned the Cities: Skylines-style camera pitch curve so ground-hugging only starts during the closest 30% of zoom-in. For the outer 70% of zoom range the camera keeps the normal RTS/diorama pitch, then smoothly blends from `52` to `20` degrees only when `zoomT < 0.3`. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-26: Made city-builder zoom speed scale with current camera distance. Mouse-wheel zoom now uses a smooth distance-based multiplier, so zoom steps are small and precise near the ground and become progressively larger when the camera is already far from the map, for both zoom-in and zoom-out directions. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-26: Added Cities: Skylines-style zoom pitch behavior to the city-builder camera. The camera now uses a low near-ground pitch at maximum zoom-in (`20` degrees) and smoothly blends back to the normal diorama pitch (`52` degrees) as the player zooms out. The minimum camera height was lowered from `6` to `3.6` so close zoom feels grounded instead of just cropped overhead. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-26: Fixed the first visible camera/framing regression after doubling the world to `128x128`. The default diorama camera was no longer mechanically doubled with the grid: pitch was increased from `42` to `52` degrees and the default offset was brought closer (`-24, 30, -24`) so the world reads at a playable scale instead of showing a tiny map with excessive sky. The camera far clip was raised to `220` to keep the larger map safe at high zoom. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-26: Audited the doubled `128x128` world-generation size for scale-related regressions. Synced `WorldGenerationSmokeTests` from the old `68x68` fixture to the runtime grid size so generated layout/road-chain smoke coverage exercises the real map scale. Expanded roadside bench occupancy capacity from a fixed `64` slots to a grid-derived capacity (`GridWidth * GridHeight / 16`) so larger maps with more benches do not create unreservable bench indexes. Verified runtime/editor `dotnet build` with 0 errors and 0 warnings. Remaining noted risk: ground/water still instantiate per-cell GameObjects, so a future chunked mesh pass is recommended for performance on 128x128.

- 2026-04-26: Added `Lumberjack Camp` to the Build menu as the buildable entry for the existing `Forest`/lumberyard production system. The Production category now exposes a 3x3 camp card with its own icon, placement preview, build-mode action, already-built gating, and Russian localization label. It reuses `LocationType.Forest`, so assigned workers continue to use the current tree-chopping/log-carrying/sapling loop. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-26: Made City Park a real leisure destination instead of a last-resort fallback. Worker leisure selection now uses weighted choices across City Park, Gambling Hall, and Bar while preserving Alcoholism's strong Bar preference. City Park visits choose the nearest reachable entrance from four sides, the park fence visuals now expose four entrance gaps, and visitors visibly promenade inside the park or sit on dedicated park benches before satisfying Leisure. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-26: Added an F9 debug `AUTO ASSIGN ALL` action. The button fills vacant truck roster slots with available workers, assigns truck drivers to unique logistics shifts per truck, fills open production slots (Forest/Sawmill/Furniture Factory/Warehouse), then fills Bus Driver shift slots and Intercity if workers remain. The action skips busy/arriving/resting/active workers and writes a detailed `DEBUG_ASSIGN` summary plus per-slot notes to `debug.log`. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-26: Expanded debug telemetry for worker behavior and economy. Added `GameBootstrap.DebugTelemetry.cs` with deduplicated `WORKER_DECISION` snapshots, `ECON` ledger logging, and `BUILDING_BANK` service-fee logging. Worker life-cycle decisions now log why a worker starts/skips meal, leisure, sleep, shift/idle handling, bus-assisted service visits, and idle activities. Economy now logs every ledger movement, per-building tax details, service-bank transfers, trade order queue creation/removal, trade auto-dispatch attempts, and trade dispatch block reasons. Also cleaned mojibake in touched debug-facing log strings. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-26: Fixed lingering worker smoking particles. `UpdateDriverVisualAnimation()` now immediately disables a worker's smoke-puff pool when the worker is hidden or no longer in `IdleSmoking`, and idle activity completion explicitly stops smoke before moving the worker back to `None`, preventing detached smoke clouds from hanging in the world after the animation ends.

- 2026-04-26: Fixed initial worker clustering near Motel after debug-log analysis showed several debug-start workers repeatedly spawning/idle-walking from the same tiny `(21..22, 20..23)` Motel frontage cells and then only pausing due to overlap avoidance. `GetDriverIdleMotelPosition()` now searches for a non-overlapping motel idle spawn point and the Motel idle point set was expanded with a wider second ring, so debug-mode starter workers should begin spread out instead of stacked.

- 2026-04-26: Continued the remaining non-critical refactor backlog after the larger architecture split. Added `WarehouseBusDeliveryService` as a pure decision seam for bus-assisted warehouse deliveries and wired warehouse delivery logging through it so debug output explains why the worker chose bus vs walking. Added `ServiceDecorationStyleService` for shared Bar/Canteen/Gambling Hall light identity, extended `FleetCanvasUiFactory` with a reusable spacer helper, and routed the Economy tax controls through it. Expanded `ui.ru.json` with more Workers/Roles/Trade/Map resource and HUD labels. Added editor smoke tests for warehouse bus-delivery decisions and service decoration light identity. Verified runtime/editor `dotnet build`, line-count, `git diff --check` (CRLF warnings only), and a targeted mojibake scan with 0 matches.

- 2026-04-26: Continued the remaining refactor backlog. Split misc-tree/bush/flower visual constructors from `GameBootstrap.World.cs` into `GameBootstrap.World.MiscDecorations.cs`, keeping the same partial-class behavior while shrinking the world file. Extended `FleetCanvasUiFactory` with a reusable vertical scrollbar helper and routed the Roles/Shifts scrollbar wrapper through it. Expanded `ui.ru.json` with more Roles/Map/Trade screen labels while retaining built-in localization fallback. Added an editor smoke test for build-mode two-lane road path-preview footprint continuity. Verified runtime/editor `dotnet build`, line-count, and targeted localized/mojibake scan with 0 errors.

- 2026-04-25: Continued the post-CityPark refactor pass. Added `TradeOrderQueueService` for active trade-order creation/peek/removal, `LocalBusPassengerService` for passenger boarding/fare/fallback decisions, and `MiscDecorationSpawnService` for misc world-prop spawn bucket selection. `FleetCanvasUiFactory` now also owns reusable tab rows and vertical scroll panels; Workers and Economy/Roles tab setup started migrating to those helpers. Expanded `ui.ru.json` with MainMenu/PatchNotes/Economy/Trade/Workers/Roles keys while keeping the built-in dictionary as fallback. Added editor smoke tests for trade-order lifecycle, local-bus passenger boarding, and misc decoration bucket selection. Verified runtime/editor `dotnet build`, line-count, and targeted mojibake scan with 0 errors.

- 2026-04-25: Extended the architecture hardening pass around the remaining refactor backlog. Added `RoadBuildPlacementService` and wired build-mode two-lane road footprint resolution/structural checks through it; added `BusStopOrderingService` and wired local-stop ordering through it; added `TradeDispatchPreconditionService` and wired trade dispatch blocking reasons through it. Expanded editor smoke tests for build-mode road placement, third-lane prevention, bus-stop ordering, and trade dispatch preconditions. Cleaned old mojibake comment markers in the touched locations file. Verified runtime/editor `dotnet build`, line-count, and targeted mojibake scan with 0 errors after one transient antivirus file-lock retry.

- 2026-04-25: Finished the remaining truck/trade runtime refactor seam. Regular trip phase decisions now go through `TruckTripRuntimeService`, refuel order decisions go through `TruckRefuelRuntimeService`, and intercity trade-run phase decisions/timing now go through `TradeRunRuntimeService`; `GameBootstrap` keeps concrete Unity side effects such as path commands, interactions, cargo mutation, money ledger, feed, audio, and rest handoff. Promoted the needed nested phase enums to public for typed service boundaries. Added smoke tests for the new phase services and fixed old mojibake in touched trade ETA/offer/debug strings. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors.

- 2026-04-25: Continued the larger refactor pass. Physically split `GameBootstrap.Transport.Roads.cs` into focused partials: core road/path helpers, `RoadGeneration`, `RoadVisuals`, `RoadMarkings`, and `RoadsideProps`; the original road file is now ~210 lines instead of >1300. Added first-pass service seams for truck/trade/local-bus runtime decisions: `TruckRuntimeGuardService` for assigned-trip/refuel update guards, `TradeAutoDispatchService` for retry/weekend dispatch timing, and `LocalBusRuntimeService` for dwell/movement stepping. `FleetCanvasUiFactory` now also owns section-card creation, and `LocalizationJsonLoader` can merge `Assets/Resources/Localization/ui.ru.json` over the built-in Russian dictionary as the first JSON-table migration path. Expanded editor smoke tests for truck/trade/local-bus/localization seams. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `dotnet build Assembly-CSharp-Editor.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and a targeted localization mojibake scan with 0 errors.

- 2026-04-25: Continued the architecture refactor without touching the in-progress CityPark implementation. `BuildingPlacementService` is now wired into `Assembly-CSharp.csproj` and covered by editor smoke tests for rotated footprints and blocked placement cells. Added `LocalBusRoutePlanner` as the first pure service seam for the local-bus out-and-back state machine, and added `RoadMarkingPlanner` for two-lane road axis/center-dash decisions. Expanded `WorldLayoutRoadValidator` with a path-appending mode so smoke tests can build a deterministic starter wide-road network and verify required destinations are actually connected, not just individually feasible. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `dotnet build Assembly-CSharp-Editor.csproj -nologo`, and `./tools/check-line-count.ps1 -MaxLines 1500` with 0 errors.

- 2026-04-25: Reworked the Racing manual-gear HUD meter semantics. The speedometer bar is now an `ACCEL`/shift-load indicator that fills as the current gear approaches its top speed, turns red near the limit, and shows `SHIFT!` as the cue to shift up. The underlying manual gear torque model remains unchanged. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Added manual gear shifting to Racing mode. The existing 3D shifter can now be dragged with the mouse upward/downward to shift between forward gears `1..4`; auto up/down shifting was removed from forward driving, while reverse remains controlled by braking/backing up. Racing acceleration and top speed now depend on the selected gear, the truck is faster overall, and the speedometer gained a `POWER` bar showing current acceleration efficiency. The Racing control hint was expanded with localized gearbox instructions. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Applied a first racing-mode visual polish pass. The race truck now has brake lights, speed-reactive road dust/exhaust motes, stronger warm headlights/lanterns, speed-line HUD accents, subtle speed/corner camera shake, dynamic FOV, darker road material with dusty shoulders, and a warmer bloom/vignette post-process profile. Racing HUD mojibake in the main objective panel was replaced with clean localized RU/EN text, and the finish overlay is now localized. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Fixed generated building road-access targets after debug-log analysis of malformed roads near Motel/Gas Station. `WorldLayoutGenerator.GetRoadAccessCell()` no longer returns the building `Anchor`; it now assigns a separate external road-access cell one tile outward from the building entrance, while the anchor remains the non-road driveway/entrance cell. Layout validation now also checks `RoadAccess` bounds, bottom-highway clearance, and blocked cells, and fallback layout access cells were updated accordingly. This prevents starter road generation from aiming at a blocked anchor and producing odd final road footprints around service buildings. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Extended height-aware cell visuals from roads to the base world grid. Ground and beach cells now render as mesh boxes with top corners sampled from the terrain height map, so neighboring cells share sloped edges instead of looking like independent flat cubes. Water body cells now use mesh boxes, and the three transparent water surface layers are quad meshes whose corner heights update from shared wave samples, making river waves connect across cell boundaries. Ground mesh colliders are preserved for cell clicking/debug HUD; water visual layers remain non-colliding. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Made the unified regular-road top surface height-aware. The long visual road overlays are now procedural mesh strips whose edge vertices sample terrain height at each road-cell boundary, so connected road cells slope into each other instead of rendering as one flat cube at an averaged height. Isolated road caps also use terrain-fitted quad meshes. Logical road cells, pathfinding, and lower tile backing remain unchanged. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Fixed 2-cell road geometry gaps at bends. When a generated or player-dragged wide road changes direction, the road builder now fills the missing corner rectangle between the previous segment footprint and the current segment footprint, preventing holes such as `(15,11)/(16,11)` after a horizontal-to-vertical turn. Added `ROAD_TURN` debug logging with previous/current segment directions and filled cells. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Added a first-pass visual corner treatment for regular roads. Road tiles with one horizontal and one vertical connection now render as full corner tiles instead of shrunken straight segments and receive a segmented curved yellow divider so 2-cell roads bend more naturally. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Fixed missing lane-divider rendering on regular 2-cell roads. Road visual axis detection now treats the adjacent paired lane as width rather than as a perpendicular intersection, so straight generated/player roads keep their center seam dash instead of suppressing markings. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Fixed regular two-cell road lane-divider markings. The yellow divider is no longer drawn inside every individual road tile; it now appears on the seam between paired parallel road cells, matching the visual idea of the edge highway center line. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Updated automatic/starter road generation to use the same 2-cell-wide `Two-Way Road` footprint as player Build mode. Generated guaranteed road connections now validate wide-road clearance against future layout placements, skip building over location anchor endpoints, and add the paired lane cell for each intermediate path segment. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Changed player-built `Two-Way Road` placement from a single-cell road tile into a real 2-cell-wide road footprint. Road build mode now supports `R` rotation, previews both occupied cells, validates both cells before placement, and Shift-drag road paths add the second lane cell along each path segment direction. Build HUD copy now states that the road occupies 2 cells of width. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Increased the full day-night cycle from 360s to 440s so each time-of-day band (`Night`, `Morning`, `Day`, `Evening`) now lasts 110 real-time seconds instead of 90. Because worker/service durations are derived from `DayNightCycleDuration`, this pass keeps the whole simulation rhythm internally consistent rather than changing only HUD labels.

- 2026-04-23: Started a first-pass right-side Event Feed HUD overlay. Added a localized timed feed canvas for gameplay events and wired initial event sources for worker hiring, truck roster assignment, role assignment, trade order creation, local trip completion, and trade completion. This pass is intentionally high-signal only to avoid spam. Build verification pending after subsequent world-visual edits in the same session.

- 2026-04-23: Visually separated `Bar`, `Canteen`, and `Gambling Hall`, which had drifted too close to the same silhouette. `Bar` stayed compact but gained a false-front facade, porch, and barrels; `Canteen` was expanded to a 3x2 footprint with a brighter diner-like body, awning, and outdoor seating; `Gambling Hall` was expanded to a 3x3 footprint with a taller tower, marquee canopy, neon pillars, and a stronger purple/pink visual identity. Build-mode placement hints were updated to the new `3x2` / `3x3` sizes. Build verification pending.

- 2026-04-23: Added distinct night-light identity profiles for `Bar`, `Canteen`, and `Gambling Hall`. Location night lights now support per-light off/on colors, range, and max intensity instead of one shared warm profile. `Bar` uses warmer amber entrance glow, `Canteen` uses broader neutral cafе-style light, and `Gambling Hall` uses stronger colored magenta/cyan glow so the three service buildings stay readable after dark. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-23: Fixed the first Event Feed readability regression. Feed cards were collapsing horizontally and letting text spill into a narrow unreadable column; the layout was rebuilt around a fixed card width, content min width, vertical content fitting, and no child-width collapse in the row layout. The feed now behaves like a stable right-side HUD card stack instead of a shrinking debug column. Build verification pending.

- 2026-04-23: Polished Event Feed card presentation so it reads like a proper HUD notification instead of raw log text. Added a colored top badge by event type, cleaner time/meta row, softer message shadow, and a fixed compact body area to keep notifications visually uniform and scannable.

- 2026-04-23: Implemented the first working local-bus-driver gameplay loop. A worker assigned to the `Bus Driver / Водитель автобуса` slot now keeps their normal logistics shift, walks to `Parking` before the shift, boards a dedicated local route bus, and drives between buildable local stops in stop-number order (`1..N`, then back down) using real road pathfinding between stop anchors. The bus waits 5 in-game minutes at each stop, returns to `Parking` at shift end, and the worker disembarks into the normal after-work life cycle. The route bus reuses the same low-poly bus model/headlights style as the highway decorative buses. Added a dedicated `GameBootstrap.LocalBus.cs` runtime partial plus UI/status integration and included the new file in `Assembly-CSharp.csproj`. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-23: Fixed a Russian HUD mojibake regression in shift time ranges. `GetShiftRangeLabel()` in `GameBootstrap.cs` had a broken literal separator inside the returned string, which rendered as garbage in `Assignments / Logistics` headers. Replaced it with a UTF-safe escaped en dash (`\u2013`) so the range now renders correctly regardless of file encoding. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Added a placeholder `Bus Driver` / `Водитель автобуса` role to `Assignments -> Logistics`. This is intentionally only a reservation slot for upcoming local bus-route gameplay: one worker can now be assigned/removed as the future city bus driver, the profession appears correctly in Workers/Assignments/quick HUDs, and truck roster assignment now excludes that reserved worker until they are reassigned. Normal logistics/production/intercity reassignments clear the placeholder bus-driver reservation safely. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Tightened local bus-stop numbering so stop numbers now always live in the compact range `1..N`, where `N` is the current number of buildable local stops. Added normalization for legacy/skewed numbers and changed the quick-HUD `- / +` logic to swap positions within that bounded range instead of letting arbitrary values like `4` survive when only one stop exists. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Fixed the buildable local `Bus Stop` system so stops are no longer blocked after the first placement. Local stops were moved off the singleton `locations[LocationType.Stop]` path into their own `localStops` list, while the highway-generated `Intercity Stop` remains a separate special singleton. World placement, terrain application, occupancy checks, selection highlights/labels, Build placement rules, and building quick HUD stop-number editing were updated to work with multiple local stops safely. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Fixed the stop-number microHUD controls after the first pass rendered `- / +` almost non-clickable. The stop-number buttons now get explicit `LayoutElement` width/height and the center label also has a stable height, so the row has a real hit area instead of collapsing to text-sized controls. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Added a first-pass buildable `Stop` / `Остановка` MVP using the same visual language as the existing highway `Bus Stop`. Build mode now includes a `Stop` infrastructure card with rotated 2x1 placement, the new stop is treated as a service building, and both `Bus Stop` and `Stop` now carry an auto-assigned stop number in `LocationData`. Building quick HUD gained a compact stop-number row with `- / +` controls so the number can be edited after placement. Current architecture still keeps stops singleton-per-type (`Bus Stop` and one buildable `Stop`) rather than a true multi-instance stop network. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Exposed worker gender in the Workers HUD detail card. The right-side profile block now appends localized gender (`Male/Female`, `Мужчина/Женщина`) to the occupation line, so the existing `DriverAgent.Gender` field is visible in normal gameplay UI without expanding the left worker list. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Updated the `v.0.0.2` patch notes wording to include a separate all-caps release bullet `ДОБАВЛЕНЫ ЖЕНЩИНЫ!!!` in the new-version section. Mirrored the same line into `ai/release-notes.md` so future patch-note updates keep the documented `0.0.2` release text consistent.

- 2026-04-22: Fixed a Lumberyard field-work regression where a Forest worker teleported straight back inside the building immediately after planting a sapling. Planting now transitions into a dedicated `LumberReturnToBuilding` walk phase, so the worker visibly walks back to the Lumberyard entrance before being hidden inside again. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Bumped the documented game version from `Lo-fi Delivery Co. v.0.0.1` to `Lo-fi Delivery Co. v.0.0.2`. Updated the main-menu version label, rewrote the in-game Patch Notes to add a new `v.0.0.2` section describing the delta since `v.0.0.1`, and updated `ai/release-notes.md` so future patch-note work has a stable `0.0.2` baseline. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Extended the `F9` debug resource controls beyond service buildings. The same panel now has a `Production / storage` block for `Forest`, `Sawmill`, `Warehouse`, and `Furniture Factory`, with `-1 / +1` controls for `Logs`, `Boards`, `Textile`, and `Furniture`. Runtime-cap storage uses existing building limits where available; loose debug-only storage rows use a safe debug cap of `99`. All adjustments are logged to `debug.log`. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Added a distinct dense tree/grass zone to the generated town. Ground visuals now force a separate forest hotspot with a broken organic edge, and misc-tree planning can prioritize cells inside that hotspot, so one part of the map reads as a noticeably denser wooded patch instead of grass and trees being spread with the same uniform noise everywhere. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Increased the initial workforce from 1 to 3 workers on world start. Startup now uses a shared `InitialWorkerCount` constant and spawns three unassigned idle workers during the normal Motel-based bootstrap instead of only one. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Rebalanced ambient misc world dressing for the larger town map. `MiscTreePlanner` no longer uses the old tiny hard cap; it now scales misc-prop target count with map area and candidate density, so larger generated maps receive noticeably more trees/bushes/flower patches. All world `misc` trees were also doubled in overall scale during spawn, making the decorative tree line read much larger against the expanded map. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Repeated the `F9` debug service-panel sizing fix after the previous pass still felt cramped in use. The panel itself is now larger (`380x820`), the worker list got a bit more vertical room, and the service-resource scroll area was expanded again so the `Canteen` row and its `+ / -` controls are fully visible during gameplay testing. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Followed up the new `F9` debug resource controls by enlarging the debug service window and increasing the service-resource scroll area height, so the full service list (including `Canteen`) is visible without the last control row being clipped. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Expanded the `F9` debug service panel for `Debug` mode with direct service-resource controls. The panel now includes `-1 / +1` test buttons for `Gas Station` fuel, `Bar` alcohol, and `Canteen` food, each clamped to the real in-game storage caps and logged to `debug.log`. This gives a quick gameplay-testing path for service supply states without touching normal player HUD flows. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Fixed the last major `Assignments / Назначения` overflow bug by making the tab work area height derive from the actual free space inside the right column instead of an outdated fixed constant. This keeps the `Intercity` block inside the panel bounds and makes scrolling responsible for overflow instead of letting the content extend below the window. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Rebuilt scrolling in `Assignments / Назначения` so the right-side tab content is finally contained by a real viewport instead of spilling past the window bounds. Both `Logistics` and `Production` now use explicit masked viewports plus visible vertical scrollbars, and the selected-worker summary card was restored to show the worker name/status above the tabs instead of being effectively collapsed by earlier fixes. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Simplified the `Assignments / Назначения` selected-worker summary card to stop text collisions for good. The card now uses only two active lines inside its info block (`name` + one status line), the extra middle line was disabled, and both active texts were switched to `VerticalWrapMode.Truncate` with fixed heights so they cannot overlap even when localized strings change. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Applied a systemic HUD upscale pass to give the main management windows more breathing room. `Fleet`, `Workers`, `Assignments`, `Build`, `Resources`, `Trade`, `States`, and the main-menu Patch Notes window were all enlarged, with matching panel-height updates where those screens relied on fixed content areas. This was done as a coordinated UI-capacity increase rather than a one-off fix so growing Russian text and denser cards have more space across the whole interface. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Simplified `Assignments / Назначения` production cards by removing the extra `Role:` line entirely. The building already communicates the purpose of the assignment, so this row only added visual noise. Production and warehouse cards were shortened accordingly, reducing vertical clutter in the right panel. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Fixed an `Assignments / Назначения` layout regression introduced during the HUD redesign. The selected-worker summary card no longer tries to act as a long explanatory block: its no-selection state was shortened, hint copy was reduced, dynamic colors were corrected, and the internal row heights were rebalanced so wrapped Russian text no longer visually collides. Warehouse assignment rows also stopped trying to render the selected worker name inside a narrow button; the warehouse action button now uses a short explicit label (`Assign` / `Назначить`) with a wider fixed width so the row remains readable. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Continued the `Assignments / Назначения` HUD redesign with a structure pass focused on tab clarity. Both tabs now start with their own short explanatory card so the screen reads as two distinct assignment modes: `Logistics` explains local shifts plus intercity reservation, while `Production` explains direct building assignment on the 08:00-18:00 schedule. The intercity card now has its own summary/context line, and shift summaries were tightened to read like local delivery staffing status rather than abstract slots. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Continued HUD redesign for `Assignments / Назначения` with a second readability pass. The right panel now starts with a selected-worker summary card (`name`, `profession`, `current status`, `context hint`) so the screen reads from the chosen worker outward instead of looking like two unrelated tables. Logistics shift cards gained a short per-shift summary, and Production cards were clarified with explicit role context plus cleaner assignment wording (`Assigned: Name`, clearer warehouse slot labels, localized building titles). Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Reframed the old `Shifts` screen as `Assignments` on the player-facing HUD. The top menu button now opens `Assignments / Назначения` while internal runtime state still uses the existing `Shifts` plumbing for safety. The canvas title was renamed, worker row statuses were clarified so production/logistics/intercity read more consistently, and several assignment/intercity button labels were localized/cleaned up to better match the current gameplay model where this screen is about assigning workers, not only scheduling time slots. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Continued HUD redesign with pass 1 for `Fleet` / `Автопарк`. The Fleet right panel was restructured from one dense truck overview into clearer grouped sections: truck profile, crew, resources, and route. Active driver and crew-slot summary were moved into the top profile card, roster management stayed functional in a dedicated crew section, and lower cards now read more like operational summaries instead of debug leftovers. Runtime text setup was also normalized for Russian/English labels in the refreshed layout. To avoid breaking the tutorial chain, compatibility refs for `InfoCardLayout` and `AssignDriverPickerLayout` were preserved even though the visual composition changed. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-22: Started HUD redesign pass 1 for `Workers`. The right-side worker detail view was split into clearer cards: `Profile`, `Skills`, `Effects`, `Needs`, `Perks`, `Work`, and `Contract`, replacing the previous dense mixed layout. The old needs/perks shared-card artifact path was removed by separating them into two proper panels, worker list status badges were widened, and status wording was unified between the left list and the selected worker card (`Idle`, `Production`, `Logistics`, `Intercity`, `Trade Run`, etc.). This pass also restored build stability after perk cleanup by keeping the old perk/effect reference data needed by `States`/gambling, while worker runtime perk assignment was reduced back to MVP behavior: only random `Alcoholism` is currently assigned. Verified with `dotnet build Assembly-CSharp.csproj -nologo` (0 errors, 0 warnings).

- 2026-04-20 (session 2): Added new HUD menu button "States" (Состояния). New file `GameBootstrap.FleetCanvas.StatesScreen.cs` creates a 700×580 scrollable reference panel listing all 4 Skills (Driving/Stamina/Production/Logistics with descriptions), all 17 Effects grouped as Activities (Rested, Well Fed, Forest Air, Sawdust, Warehouse Flow, Craft Focus, Worked Hard, Drunk, Road Focus, Road Fatigue, Race Rush) and Needs (Hungry, Starving, Sleep Deprived, Exhausted, Bored, Burned Out) with colored modifier tags (+N green / −N red) and source/cause hints, plus 1 Perk (Alcoholism). Panel localizes to Russian/English on language change. `isStatesPanelOpen` added to `GameBootstrap.cs`; button added to DrawMenuBar in `Fleet.cs`; close logic wired in ToggleMenuPanel, Input.cs (Escape/CloseAllMenus/scroll-block), BuildingQuickHud blocking check, and Localization.cs dirty flag. Build not verified in this session.

- 2026-04-20 (session 2): Fixed Workers HUD detail panel — Перки section. `needsCardLayout.childForceExpandWidth` was `true`, causing Unity to expand the 2px divider to ~1/3 of the card width (gray-blue "square" artefact) and squish the perks column to near-zero width (hiding Алкоголизм text). Changed to `false` so the divider stays at `minWidth = 2f` and both columns share the remaining space equally. One-line fix in `ManagementScreens.cs:589`.

- 2026-04-20: Fixed standalone Build And Run freeze when pressing `Join the race`. `Player.log` showed `ArgumentNullException: shader` in `CreateRacingHeadlight`; racing headlights/skydome now use packaged `ShaderRefs` fallbacks instead of `Shader.Find("Standard")`, and racing bootstrap is wrapped in exception logging plus cleanup so future startup errors restore the city instead of leaving `Time.timeScale = 0`. `Time.fixedDeltaTime` is also kept valid during racing pause. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Updated Racing controls HUD and steering input. Racing steering is now mouse-wheel-drag only; keyboard `A/D` and left/right arrow steering fallback were removed while `W`/up, `S`/down, and `ESC` remain active. Added a readable top-right Russian control legend covering mouse steering, throttle, brake/reverse, and exit. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Removed the old worker/driver Energy attribute after the needs-system rewrite. DriverAgent no longer stores Energy/SleepStartEnergy/rest-queued state, runtime no longer drains/restores energy during shifts or motel sleep, Fleet/Workers/Truck/Driver quick HUDs no longer display energy rows, and Stamina tooltip copy now refers to tiredness/needs instead of energy. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Tightened Patch Notes scroll content spacing. Removed premature fixed preferred heights from paragraph rows, added per-text `ContentSizeFitter`, reduced content spacing, and kept compact section header minimum heights so the changelog no longer creates huge gaps between sections. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Added `ai/release-notes.md` as stable release memory for in-game Patch Notes. It documents the player-facing contents of `Lo-fi Delivery Co. v.0.0.1` and the workflow for comparing that baseline against current code/work-log when preparing future patch notes. `ai/README.md` now points agents to this file for version/changelog tasks.

- 2026-04-20: Improved the main-menu Patch Notes window. The changelog body is now a scrollable `ScrollRect`, section headers are rendered as separate accent-colored bold rows, and the content/title localizes between English and Russian using UTF-safe escaped strings. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Added a main-menu `Patch Notes` button and modal changelog window. The window currently documents `Lo-fi Delivery Co. v.0.0.1` with version-grouped sections for the core loop, workers, production/resources, logistics/trade, and world ambience, creating an in-game place for future patch notes. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Added a manual main-menu version label. `GameBootstrap.MainMenuHud.cs` now has `MainMenuVersionLabel = "Lo-fi Delivery Co. v.0.0.1"` and renders it in the bottom-right corner of the main menu. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Expanded worker needs diagnostics in `debug.log`. Needs threshold changes, satisfied needs, lifecycle day-rollover queue/reset, skipped services, service entry, sleep completion, and unemployed work-skip now include timer/status snapshots like `Meal=7.2h/Ok, Sleep=18.0h/Warning, Leisure=3.1h/Ok` plus service failure reasons where relevant. `GameBootstrap.WorkerNeeds.cs` was rewritten with UTF-safe Russian UI strings. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Redesigned Resources HUD with two tabs. "Warehouse" tab (На складе) shows the 8 existing resource rows sourced from Warehouse only. "Production" tab (Производство) shows per-building sections (Forest → Logs, Sawmill → Logs+Boards, FurnitureFactory → Boards+Textile+Furniture, GasStation → Fuel); sections for unbuilt buildings show a "(not built)" label. Window widened from 400→480px. All labels set directly via IsRussianLanguage(). Verified 0 errors; one pre-existing legacy warning remains.

- 2026-04-20: Added MVP worker needs timers. Workers now track game-hours since Meal, Sleep, and Leisure; Canteen/Bar/fallback leisure/Motel completion resets the relevant timer, need status thresholds are logged once on change, and Workers HUD shows a compact Needs block with localized status and hours. The existing life-chain now uses need timers for seeking service goals and only resets its daily helper flags at a safe idle moment after day rollover. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-20: Temporarily disabled the main-menu User mode entry. `New Game User` now renders as a non-interactive `Work in progress` button with disabled visuals while Debug mode remains available. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Added a first pass worker daily life-cycle. After a completed work block, workers now proceed through goal-based Eat/Canteen, Leisure/Bar or idle fallback, Sleep/Motel, then Idle; unemployed workers skip Work and start the same evening chain. Canteen/Bar/Motel goals are skipped for the day when the needed building/resource/money is unavailable, Bar/Canteen are no longer picked by random idle wandering, and lifestyle durations now use game-hour based timing (1h canteen, 2h leisure, 1-3h idle fallback, 8h sleep). Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Added hover explanations for Workers HUD skills. Driving, Stamina, Production, and Logistics now render as accent-colored interactive rows in the selected worker card, and hovering each row opens a compact tooltip describing what that skill affects in English/Russian. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Added MVP worker base stats: Driving, Stamina, Production, and Logistics. Stats are generated deterministically on worker creation, stored on `DriverAgent`, and displayed beside the portrait in the selected worker's Workers HUD detail card; they are informational only for now and do not affect gameplay yet. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Added procedural low-poly worker portraits to the Workers HUD detail panel. Each `DriverAgent` now gets deterministic portrait traits on creation, and the selected worker card renders a UI-primitive portrait directly under the worker name in the right-side detail view. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors; one pre-existing legacy `DriverCardUi.DriverId` warning remains.

- 2026-04-20: Fixed tutorial/HUD polish issues from the latest playtest. Tutorial step 9 now clears Forest/building and debug-cell selections before showing, does not open quick HUDs, and continues simulation while blocking player input behind the tutorial window. Tutorial step 15 no longer highlights or auto-picks the first driver; the player must choose any free driver, and any valid truck assignment completes the tutorial. Shifts HUD now labels the roster as Workers, shows each worker's profession on the right side of the row, and marks truck-assigned workers as Logistics with Truck Driver profession. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-20: Fixed two tutorial/assignment regressions. Tutorial step 9 (`ForestWorkerStarted`) no longer pauses the simulation while its HUD is open; other tutorial windows keep the existing pause behavior. Production assignment from Shifts > Productions now auto-unassigns a worker from their truck roster before switching them to building work, and blocks the production assignment if the truck is actively using that driver and cannot safely release them. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-20: Rewrote all active tutorial HUD copy to be concise and gameplay-informative in both English and Russian. Updated the 17-step User tutorial flow, both OrbitHUD messages, removed the hidden Forest text override, and cleaned old literary tutorial strings out of localization so Russian mode uses the new instructional copy. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-20: Compressed this work log. Kept recent onboarding/Fleet/Trade/quick-HUD notes in useful detail and collapsed older multi-session history into thematic summaries. Stable memory files were not changed.

- 2026-04-21: Simplified the Workers HUD Effects panel. The visible list now shows only each active effect name plus remaining time, while descriptions and skill modifiers moved into the shared hover tooltip; the tooltip expands for Effects and returns to the compact size for Skills. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-21: Expanded worker effects into the current gameplay loop. Motel sleep now applies `Rested`; need warning/critical states apply and clear live debuffs for hunger, sleep deprivation, and leisure burnout; completed work applies `Worked Hard` plus building-specific effects for Forest/Sawmill/Warehouse/Furniture Factory; normal route completion applies `Road Focus`; intercity trade completion applies `Road Fatigue`; race bonuses apply `Race Rush`. Effect refresh logging was adjusted to avoid per-frame spam. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-21: Added the second active worker effect, `Well Fed` / `Сытость`, after successful Canteen visits. Consuming Food now applies/refreshed a 6-game-hour effect with Stamina +2, Production +1, and Logistics +1; the existing Effects panel and effective skill display pick it up automatically. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-21: Added the first active worker effect, `Drunk` / `Опьянение`, after successful Bar visits. Consuming Alcohol now applies/refreshed a 4-game-hour effect with Driving -5, Production +1, and Logistics +1; the Workers HUD skill lines show effective values with base-plus-delta notation while the Effects panel lists the timed modifier. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-20: Added a worker Effects foundation beside Skills in the Workers HUD. Workers now have active temporary effect state with duration and skill modifier deltas, effect timers tick with game time, debug logs record effect activation/expiry, and the selected worker card shows a separated localized Effects panel with an empty state. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-21: Added the first worker perk system. Workers now roll random perks during stat generation; the first available perk is Alcoholism, internally typed as a negative perk. Workers HUD now shows a Perks column beside Needs with a divider, localized labels, and per-perk hover details, but does not print perk type labels in the HUD. Alcoholism strengthens and extends the Bar/Drunk effect instead of being only a label. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-21: Changed Workers HUD effects hover from a whole-table tooltip to per-effect rows. Each visible effect row now has its own hover target and shows only that effect's remaining time, modifiers, and description. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-19: Fixed Fleet tutorial step 13 Buy Truck panel hiding. The Buy Truck panel is now hidden every visible Fleet update during `FleetSelectTruck`, even if the list is not rebuilt that frame. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-19: Added typewriter reveal to every regular tutorial HUD. `ShowTutorialWindow` stores localized body text separately and `UpdateTutorialUi` reveals it at the same default speed as orbit tutorial HUDs. Bee/flower easter egg popups use the same path. Verified build clean.

- 2026-04-19: Added a day-only bee/flower easter egg in both Debug and User modes. Clicking a flower patch cell during Day opens a tutorial-style popup saying `Дурачок, не мешай пчёлкам`; it takes priority over debug cell inspection. Verified build clean.

- 2026-04-19: Extended the User tutorial after Fleet driver assignment. Workers HUD dynamic labels are localized, Fleet step 13 hides the Buy Truck panel, assigning a worker to Truck 1 closes Fleet and queues step 16, step 16 sends the player to assign someone to Sawmill, and assigning a Sawmill worker queues step 17 placeholder. Current tutorial total: 17. Verified build clean.

- 2026-04-19: Fixed tutorial step 15 side-card placement so the Fleet driver-picker card remains visible left of the Fleet HUD instead of drifting off-screen. Verified build clean.

- 2026-04-19: Fixed OrbitHUD profession labels. Step 12 explicitly renders `(for now) unemployed` / `(пока ещё) безработный`; generic fallback no longer calls unassigned local workers drivers unless they have truck assignment. Verified build clean.

- 2026-04-19: Fixed tutorial step 8 Forest assignment regression. The forced tutorial commute now moves time into production hours and clears stale rest/walk state so the assigned worker reaches Forest and starts working instead of immediately going to Motel/sleep. Verified build clean.

- 2026-04-19: Polished orbit/typewriter tutorial identity and gating. Forest worker-start step 9 waits until step 8 is dismissed, OrbitHUD speaker text is highlighted separately from typewritten body, and descriptor/profession localization was added for assignable jobs/services/truck/intercity/default workers. Verified build clean.

- 2026-04-19: Added OrbitHUD detach behavior. When the attached character reaches their destination or leaves the walk phase, the HUD stops orbiting, smoothly returns to the screen center, keeps typing, and triggers smooth camera return to the default diorama view. Verified build clean.

- 2026-04-19: Hardened standalone Build And Run `Join the race` clicks by adding a direct mouse-rect fallback after showing the Canvas button. Verified build clean.

- 2026-04-19: Rewrote User tutorial step 12 as first-person worker thought text with matching Russian localization. Verified build clean.

- 2026-04-19: Fixed compile break in `GameBootstrap.FleetCanvas.UtilityScreens.cs` caused by smart quotes in C# code and an accessibility mismatch on `UnlockBuildTool(BuildTool tool)`. Verified build clean.

- 2026-04-17: Fixed truck microHUD layout for long trade/status text. Truck quick HUD is wider/taller, uses one-column resource/route rows, avoids broad post-update localization, and uses compact active trade phrases. Verified build clean.

- 2026-04-17: Cleaned worker quick HUD readability and trade cargo display. Worker quick HUD uses wider one-column rows and direct localization. Truck cargo formatting is shared as `amount/capacity (Resource)` across quick HUD, Fleet list/details, and legacy HUD. Buy trade runs load purchased cargo before the truck reappears at the return edge. Verified build clean.

- 2026-04-17: Fixed a Fleet regression from quick-HUD cleanup. Fleet internal selected-truck state is preserved while Fleet is open; only compact quick-HUD canvases are hidden. Verified build clean.

- 2026-04-17: Added automatic quick-HUD cleanup when larger HUD windows or build mode are open. Truck, Driver, Building, and Cell quick HUD selections/canvases plus debug-cell highlights are cleared before large HUD updates. Verified build clean.

- 2026-04-17: Rebuilt Trade HUD into an explicit order-creation panel. Trade now has Resource dropdown, Action dropdown, amount stepper, PLACE ORDER, Active Orders list, BUY/SELL tags, and remove buttons. Resources HUD Buy/Sell threshold controls and hidden threshold auto-dispatch were removed/disabled. Verified build clean.

- 2026-04-17: Fixed Fleet driver assignment filtering after Intercity assignment. Local and Intercity drivers/workers can be truck candidates if otherwise free; production-assigned, already truck-assigned, arriving, active trade-run, resting, and busy walking workers are excluded. Verified build clean.

- 2026-04-17: Simplified Warehouse quick HUD by removing inline stock counts; detailed inventory remains in Resources HUD. Verified build clean.

- 2026-04-17: Fixed Shifts HUD tab/layout presentation. Logistics and Productions now enforce fixed panel dimensions; Productions active tab text receives accent styling. Diagnostics log `SHIFTS_HUD` state for future UI debugging. Verified build clean.

## Older Summary

- 2026-04-16 to 2026-04-17: User-mode tutorial system grew from a welcome popup into a multi-step onboarding chain covering Motel construction, Workers panel, hiring, Forest production assignment, Sawmill construction, worker arrival cinematic, Fleet selection, truck driver assignment, and follow-up Sawmill assignment. It includes localized Russian/English text, side-card tutorial windows, red UI highlights, OrbitHUD/world-space typewriter panels, camera focus/zoom, skip tutorial, and cleanup of tutorial overlays/highlights.

- 2026-04-16: Added separate New Game Debug/User modes. Debug keeps fuller starter generation; User starts leaner and expects Motel/Sawmill to be built manually. Restarting either mode from the Escape menu reloads the scene and starts the selected mode.

- 2026-04-16: Added buildable service/production buildings and build-preview improvements. Canteen was added as a service building with fee/inside counters, Motel/Sawmill became buildable in User mode, Furniture Factory produces Furniture from Boards + Textile, and build mode now previews full footprints, driveway cells, rotation with R, and post-placement road connection flow.

- 2026-04-16: Production assignments were split from logistics shifts. Logistics still uses driver shifts/intercity role; Productions assigns workers directly to production buildings with fixed 08:00-18:00 working hours.

- 2026-04-16: Localization system was introduced with Eng/Rus main-menu selector and runtime label helpers. There was a past mojibake incident in `GameBootstrap.Localization.cs`; avoid non-UTF-safe file writes and prefer `apply_patch`.

- 2026-04-12: Regional Map was added and iterated into a clickable 3x3 region HUD. The current region has a schematic preview with river/highway/town/forest cues; neighboring regions are placeholders with known resource roles like Textile/Cotton/route.

- 2026-04-11: Intercity trade highway routing and ambient bus lane behavior were heavily debugged. Current intent: trade trucks use explicit edge-highway lane paths, disappear/reappear at the highway edge, and bus spawning is suppressed only on relevant active trade lanes to reduce overlap. Debug logs include `TRADE_PHASE`, `TRADE_PATH`, and `BUS_SPAWN`.

- 2026-04-10 to 2026-04-11: Edge-highway lane mapping, bus direction rows, highway lantern suppression near local-road junctions, and trade-truck lane usage were repeatedly corrected. Treat current code as source of truth, not old notes.

- 2026-04-09 to 2026-04-14: World polish added/iterated edge highway, bus stop, ambient buses, water/river visuals, beach/water height, fish, bees, birds, cats, benches, flowers, roadside/forest particles, dust, night moth swarms, and audio toggles. Some ambient sounds were fully disabled by request; truck loop audio should only run when a truck is active/occupied.

- 2026-04-08 to 2026-04-09: Major runtime/gameplay split from the early prototype into partial `GameBootstrap` systems. Added Fleet Canvas, driver extraction from truck, early rest handling, shifts, manual routes, building quick HUDs, debug cell HUD, generated world layout, and broader resource/economy UI.

- 2026-04-06: Project began as a runtime-generated transport prototype in `SampleScene`: grid, road placement, Parking/Forest/Warehouse/Town, truck pathing, cargo service, basic resource loop, fuel/gas station, diorama presentation, procedural audio, and AI memory files.

- 2026-04-15: Intercity racing minigame was added and polished. Trade trucks can expose Join the Race, launch a separate road scene with truck controls, dashboard objects, finish sequence, music/SFX, skybox, collisions, road extension, and payout bonus.

## Active Notes / Watchouts

- 2026-04-25: Improved misc tree visual grounding. Procedural misc trees now get a transparent contact-shadow blob under the canopy, tree trunks/crowns use `ConfigureShadowVisual()` so they can cast/receive shadows, and the three tree variants gained darker underside foliage plus deterministic leaf-color variation to reduce copy-paste repetition. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Stabilized generated map layout selection after road/building generation fell back to an outdated hardcoded layout. `WorldLayoutGenerator` now tries more candidates, tracks the best road-valid candidate separately from the highest-score candidate, includes Motel in distribution scoring, and builds the emergency fallback through the same placement helper used by normal generated buildings so footprints, anchors, and outward road-access cells stay consistent with two-cell roads. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Added a landmark/hero readability pass on top of the lighting and surface work. `Bar`, `Canteen`, and `Gambling Hall` now have soft translucent glow panels around their key signage/window accents, buses use a richer shared body stack with lower body/roof stripe/shadow blob, trucks gained a clearer stripe/windshield/shadow silhouette, and workers now read better against the ground thanks to contact shadow blobs plus more varied clothing palettes. The goal was to improve top-down readability of the important moving pieces and service landmarks without breaking the low-poly style.

- 2026-04-24: Followed the lighting pass with a surface pass. Ground and grass textures were enriched with extra dry/moss variation, roads and the edge highway now use their own stylized asphalt material stack instead of flat colors, and water/shore colors were pushed toward a deeper layered river look with softer top transparency and less harsh foam. This keeps the low-poly style intact while making the most screen-dominant surfaces feel less flat.

- 2026-04-24: Applied a first real graphics-upgrade pass focused on city-mode lighting and post-processing. The world now uses stronger tri-light ambient colors instead of a single flat ambient term, more expressive day/night sun colors and intensities, richer haze/fog blending, and a runtime-driven diorama post-processing profile. `ColorAdjustments`, `Bloom`, `DepthOfField`, and `Vignette` now all respond to time of day, so mornings/evenings feel warmer, nights cooler and moodier, and daytime scenes brighter/crisper without heavy visual noise.

- 2026-04-24: Expanded the documented `v.0.0.2` Patch Notes to catch up with the actual shipped prototype state. The in-game changelog and `ai/release-notes.md` now mention the local bus loop, bus fares/Parking revenue, Event Feed, Economy taxes/tabs, stronger service-building visual separation, deeper Alcoholism behavior, randomized perk loadouts, and improved female portraits so the release notes better match the current build.

- 2026-04-23: Upgraded female worker portrait generation so women read more distinctly from men in the low-poly HUD portraits. Female portraits now draw from their own hair-style pool (including bob/ponytail variants), can receive a dedicated earring/hair-clip accessory, and use slightly different face proportions plus softer cheek/eye/mouth treatment in both full-size and scaled portrait renderers. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Restored full random worker-perk generation. Workers now receive exactly `1` random negative perk and `4` random positive perks on stat assignment, chosen deterministically from the existing perk pools without duplicates. `EnsureWorkerStats()` now repairs older workers that still had an incomplete perk set, and the Workers HUD perk list was expanded to 5 rows so the full loadout is visible. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Fixed Event Feed top-right placement. The feed root is now anchored lower so notifications begin clearly below the compact `Speed / Day / Treasury` HUD strip instead of overlapping it. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Fixed Event Feed canvas anchoring. `EventFeedCanvas` now explicitly stretches to the full screen before anchoring the feed root, preventing notifications from drifting toward the center/left when other HUD overlays are already open. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Added daily building-tax MVP. At each day rollover (`00:00`), the game now collects `10%` from each positive `BuildingBank`, subtracts that tax from the building bank, adds the aggregate amount to global Treasury, writes one aggregated `moneyLedger` entry (`Building Taxes -> Treasury`), and logs one aggregated `TAX` debug line with optional per-building breakdown. Economy HUD now includes a compact `Taxes` summary card showing tax rate, current taxable bank total, last collected amount, taxed building count, next collection, and current day. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Tightened the first tax-HUD polish pass. Non-interactive UI texts created through the shared Fleet canvas helpers now disable `raycastTarget`, which prevents labels from stealing clicks from nearby buttons (including the tax-rate `+ / -` controls). The top HUD menu label for the economy panel was also switched from `Trade` to `Economy`, and the tax summary rows were widened and forced to stay single-line to avoid wrapped, broken horizontal layouts. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Expanded the `Taxes` section in Economy HUD into a taller, clearer 4-row layout. Tax rate controls now sit on their own top row; current taxable bank, last collected amount, taxed building count, and next collection time each render in separate dedicated rows/value fields, and the dynamic tax values are assigned after `LocalizeCanvas()` so runtime numbers are no longer mangled by late string localization. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-26: Added a first-pass far zoom visual LOD for the enlarged city map. When the camera is pulled far out, the runtime now hides grid lines and tiny decorative particle/animal roots, skips their visual update loops, disables procedural shadow casting for registered low-poly objects, and suppresses real point lights from building/road lanterns while preserving glow materials and all gameplay simulation. The LOD uses height hysteresis (`72` enter / `64` exit) so it does not flicker around the threshold, and resets cleanly when a new world is generated. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Reworked Economy HUD into a tabbed window. The panel now has separate `Taxes` and `Trade` tabs (using the same tab-button visual pattern as `Roles/Shifts`), with the tax UI isolated into its own panel and the trade-order UI isolated into a separate panel. This removes the earlier shared-layout contention between tax summary rows and the trade-order controls. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Simplified the Taxes tab rendering after repeated layout instability. Instead of several narrow tax value fields competing inside one card, the Taxes tab now uses a small dedicated top row for `Tax rate -/+` and one larger multiline summary text block for the current taxable bank, last collected amount, taxed building count, next collection, and current day. The tab row itself was also constrained to a real 40px control strip so the tab buttons no longer balloon vertically. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Rebuilt the Taxes tab into three explicit cards inside the Economy window: `Tax rate`, `Income`, and `Timer`. The rate card now uses centered square `- / +` controls around the percentage value, while income/timer values render in their own dedicated cards instead of sharing one oversized empty block. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Applied a `final polish` graphics pass on top of the previous lighting/surface/landmark work. Water and shoreline were refined with softer multi-layer transparency, a broader foam band stack, and a third near-edge wash ring so the river reads more like moving water against wet sand instead of flat tiles. The atmosphere pass deepened horizon/background blending with a gentler distance haze and slightly richer sky/fog separation over the existing day-night lighting. Building night identity was also extended beyond the three service landmarks: Parking, Gas Station, Lumberyard, Warehouse, Motel, Sawmill, and Furniture Factory now get dedicated animated window/glow strips driven by the same night-light system, so the rest of the town also reads as “lit from within” after dark. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Warmed the practical light language across the game. Road lanterns now glow noticeably more amber, truck and bus headlights use a hotter warm beam/material tint, and handheld worker flashlights (including forest ambient workers) were shifted from pale neutral white toward a stronger tungsten-like orange. Racing-mode headlights and track lights were also brought into the same warmer palette so night scenes feel more cohesive between city-builder and racing contexts. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Exposed the already-implemented two-way road behavior in Build mode by repurposing the existing road build entry as an explicit `Two-Way Road` tool. The Build card now describes the yellow center divider / automatic lane-keeping behavior, and the legacy IMGUI build button was also relabeled from generic `Road` to `2-WAY / Two-Way Road` so the feature is surfaced consistently in both build UIs. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Added a stronger Build-card identity for `Two-Way Road`. The infrastructure card no longer relies only on text: its accent panel now renders a miniature asphalt icon with white shoulder lines and a yellow center divider, plus a smaller `2W` label underneath, so the new road type is visually recognizable at a glance in the Build menu. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Made regular two-cell roads more usable for transport. Starter road generation now falls back to the opposite side for the second lane when the preferred right-side footprint is blocked by a building footprint, preventing one-cell gaps near buildings such as Gas Station; wide-road validation was relaxed to match that tolerant turn-fill behavior. Local route buses now use the same right-hand lane world offset as trucks, so forward and return travel visually occupy opposite lanes on normal two-way roads. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Fixed a regression where overlapping generated road routes could inflate a two-cell road into three parallel cells near buildings. Road footprint resolution now reuses an existing paired side lane when the primary road cell already belongs to a two-cell road, and turn-fill skips cells that would create a third parallel lane. This targets artifacts like `(44,17)/(45,17)/(46,17)`, `(49,41)/(50,41)/(51,41)`, and `(19,11)/(19,10)/(19,9)`. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Added a first-pass unified visual road layer. `UnifiedRoadVisuals` now rebuilds long horizontal/vertical surface runs plus overlay dashes/side stripes on top of the logical road cells, so roads read as connected strips instead of only separate per-cell cubes. The old per-cell road objects remain as the logic/refresh backing layer, and startup generation suppresses repeated unified rebuilds until terrain heights are applied. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Fixed generated building driveway/access cells pulling two-lane roads sideways. Generated `RoadAccess` now stays on the building `Anchor` rather than one tile outward, while road generation still skips terminal building cells, so roads approach the entrance without shifting the two-cell road pair toward the building. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Fixed roadside prop placement for ordinary two-cell roads. Road lanterns and benches no longer rely on the old one-cell straight-road test; they now detect the outside edge of a two-lane road pair and place props only on the external side, avoiding the middle lane and blocked side cells. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Fixed another source of generated 3-cell road width. `TryResolveRoadFootprintOffset()` now snaps a new road footprint to an already-existing adjacent lane before trying to place a fresh side lane, so repeated guaranteed building connections should merge into existing two-cell roads instead of growing a third parallel lane. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Matched ordinary two-cell road visuals to the edge-highway style. Regular road tiles now use the same dark highway asphalt/shoulder materials, beige side stripes, and light center dashes so the buildable two-way road reads visually like the highway instead of a separate dusty road style. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Fixed the ordinary two-cell road lane offset. `GetRightHandLaneOffset()` now uses the corrected map-space perpendicular so trucks and local buses travel on the visual right-hand lane instead of the opposite lane; road generation was not changed. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Reworked generated building road access for the two-cell road system. Generated locations now store a separate `RoadAccess` cell one tile outward from the building anchor, starter road validation checks those access cells instead of routing straight into building anchors, and guaranteed road connections now log `ROAD_ACCESS` plus a `ROAD_VALIDATE` summary with possible 3-wide lane warnings. Terminal endpoints that are actual building/highway cells are skipped safely so the intercity stop can still connect through the edge highway. Player-built buildings still default their road access to the existing anchor behavior. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Added the first Racing gameplay-result pass, excluding the previously deferred gearbox-depth and route-type priorities. Racing now tracks elapsed time, truck damage, cargo damage, collision count, and bus-impact cooldown; collisions with lanterns, trees, and buses write structured `RACING` entries to `debug.log`; the finish overlay now shows a reward breakdown instead of a flat `$50`; and the stored `racingBonusEarned` is calculated from base reward + speed bonus - damage/collision penalties while still paying through the existing intercity trade completion flow. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Continued world-generation service extraction. Added `WorldLayoutRoadValidator` as a pure helper for required wide-road feasibility across generated `RoadAccess` pairs, delegated `CanBuildWideRoadPath()` to it, and reused its path-direction helper from build-road preview. Expanded `WorldGenerationSmokeTests` with deterministic Debug/User layout road-chain checks so generated worlds are tested for two-lane road feasibility before runtime placement. Verified `./tools/check-line-count.ps1 -MaxLines 1500` and mojibake scan. Runtime/editor `dotnet build` is currently blocked by another in-progress CityPark implementation (`TryPlaceCityParkAtAnchor`, `GetCityParkPlacementPreview`, `CreateCityParkDecoration` references are present but not all compiled methods are available yet); do not resolve this from unrelated refactor work unless the CityPark owner hands it over.

- 2026-04-25: Started the next architecture hardening pass after the partial split. Added `TwoLaneRoadGeometry` as a pure service for two-lane road direction/footprint/turn bounds and wired build-road input through it; added `FleetCanvasUiFactory` as the first real UI factory seam behind existing FleetCanvas helper wrappers; added `LocalizedStringTable` as the first table-style localization lookup seam; added `tools/check-line-count.ps1` plus `.github/workflows/project-sanity.yml`; added editor smoke tests for world layout placement validity and two-lane road invariants. Cleaned the remaining mojibake scan hits in source comments. Verified `dotnet build Assembly-CSharp.csproj -nologo`, `dotnet build Assembly-CSharp-Editor.csproj -nologo`, `./tools/check-line-count.ps1 -MaxLines 1500`, and mojibake scan.

- 2026-04-25: Completed a controlled size-focused runtime refactor pass. All C# scripts under `Assets/Scripts` are now below the approximate 1500-line ceiling; the largest remaining files are `GameBootstrap.Transport.Roads.cs` (~1386), `GameBootstrap.WorkerStats.cs` (~1317), and core `GameBootstrap.cs` (~1271). Split large partials into focused files for Racing (`Runtime/Racing/*`), FleetCanvas screens (`Runtime/UI/FleetCanvas/*`), runtime loop, ambient particles, driver life-cycle/hiring-shift logic, and build-road input. Fixed split-introduced brace/import issues while preserving behavior. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Fixed water appearing completely frozen after the first performance pass. Water effect updates now run outside the far-zoom ambient block, with separate update rates for near/normal (`24Hz`) and far zoom (`10Hz`), so visible surface motion continues while heavier far-zoom ambient systems stay disabled. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Added a first performance stabilization pass for the enlarged 128x128 city map. Far-zoom visual LOD now activates earlier (`46/40` height hysteresis), water animation updates were reduced from 30Hz to 20Hz, far water LOD now skips water effect animation, and medium/far water no longer animates the heavy water body meshes while keeping surface layers active near the camera. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Made the far zoom-out bloom pass intentionally stronger and more visible. The zoom boost now starts earlier, lowers bloom threshold much more, increases bloom intensity/scatter, and adds a small far-zoom exposure lift while preserving the base close-camera look. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Fixed ambient misc birds failing to recover when bird setup runs before enough tree perch points exist. `UpdateMiscBirds()` now lazily rebuilds the bird flock once at least two perch points are available, and bird flight animation was slowed by increasing travel duration and reducing flight bob/wing-flap speed. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-27: Added zoom-out visual polish for the city-builder camera. Diorama bloom now gains a smooth extra boost at high camera zoom, distant clouds were moved into a much higher sky layer, cloud rows now span the enlarged map depth, and cloud travel length was extended so they drift across the full scene instead of clustering over a small area. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-28: Started merging Fleet management into the Roles/Logistics HUD. The top-level Fleet/Autopark menu button is hidden, while Roles > Logistics now has an embedded Fleet card with truck purchase, truck list, cargo/crew summary, focus-on-truck row click, and selected-worker-to-truck assignment. The new User tutorial truck-buy step now opens Roles/Logistics instead of the old Fleet screen, and the legacy top-level Fleet tutorial outline is disabled. Verified `dotnet build Assembly-CSharp.csproj -nologo` and `./tools/check-line-count.ps1 -MaxLines 1500` with 0 errors.

- 2026-04-25: Reworked the in-world Build preview for `Two-Way Road`. The road tool no longer previews as a plain green square: hover preview, drag-path preview cells, and the Shift path start marker now render as dark asphalt tiles with white edge lines and a yellow center divider, while still tinting appropriately for valid/invalid/start states. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Added direction-aware orientation to the in-world `Two-Way Road` Build preview. Single-cell hover previews now align their road markings to a neighboring existing road when possible, and Shift drag-path previews orient each preview tile from the generated path direction so center dividers and shoulder lines no longer all face the same way. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Added a first-pass visible cargo load on truck beds. Each `TruckAgent` now owns a lightweight `CargoVisualRoot` that rebuilds only when cargo type/amount changes, showing distinct procedural primitive loads for logs, boards, cotton, textile, furniture, fuel, alcohol, and food. Existing truck cargo logic remains unchanged; the visual layer follows `TruckCargoType` / `TruckCargoAmount`. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Completed the regional trade-route UI pass already present in the map/trade code by adding a compact region tag to regular Trade HUD order rows. Orders created from the Regional Map still enter the same `activeTradeHudOrders` queue and now remain visually traceable in the Economy/Trade tab via `Region: <name>`, while generic Trade HUD orders stay untagged. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-25: Added first-pass truck dirt-dust particles. `SetupExhaustSmoke()` now also creates a small beige `TruckDirtDust` particle pool, and moving trucks emit low rear-wheel puffs only while traveling outside regular road/highway cells and away from water. The effect reuses the lightweight procedural particle style rather than Unity Particle System. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-24: Removed direct Treasury income from completed internal truck trips. The old `AwardMoney(...)` payout on normal local route completion is no longer called; trip completion now only logs/feeds that the route finished, without awarding cash or showing a money-style feed event. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Hardened `Gambling Hall` microHUD audio gating. Even though reel/result logic already stopped updating when the building quick HUD was hidden, `TryStopReel()` and `TriggerHudGamblingResult()` now also explicitly guard their reel-stop / win / lose sounds behind `IsGamblingHallQuickHudVisible()`, so slot-machine audio and HUD flash/shake cannot fire after the microHUD is closed or selection changes. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Tied the `Gambling Hall` microHUD slot-machine animation to simulation speed. Reel spin timing and result/show-bet pauses now advance with `gameSpeedMultiplier` instead of raw unscaled time, so the microHUD speeds up/slows down with `1x/2x/4x` and freezes on pause. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Fixed the last visible `Gambling Hall` quick-HUD mojibake in slot-machine reel symbols. The reel symbol set in `GameBootstrap.BuildingQuickHud.cs` now uses explicit Unicode escapes for star/suit icons instead of a previously corrupted literal sequence, so the spinning animation renders correctly regardless of source-file encoding. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Expanded the `Alcoholism` perk. Alcoholic workers now prefer `Bar` first for leisure if it is available, their `Drunk` effect already remains stronger/longer, and when `Drunk` expires they now receive a persistent `Hangover` debuff (`∞` duration in HUD) that only clears on the next successful Bar visit. Added the new effect to the `States` reference panel as well. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Renamed the old `Assignments` HUD to `Roles` / `Роли` in the top menu and management screen title so the label fits the button better. Also verified and preserved the cleaned Russian Racing controls HUD text (`Управление`, mouse/W/S/ESC legend) and checked remaining worker stats/perk descriptions for visible mojibake; the remaining corruption in those systems is now limited to source comments/internal notes, not player-facing text. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Stabilized truck refuel behavior after the debug-log auto-refuel loop. Reduced per-cell truck fuel drain from `1.0` to `0.5`, raised the proactive auto-refuel threshold from `30` to `50`, initialized `Gas Station` fuel to a permanent full `5/5`, and removed fuel depletion from both normal truck refuels and on-foot rescue refuels. Refuel trips now no longer abort because the station is empty, and Gas Station quick HUD explicitly shows `5 / 5` as the current temporary infinite-fuel gameplay rule. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Fixed city-builder day/night music leaking into Racing mode. Entering the race now pauses both the general city music source and the time-of-day music layers via `PauseDayNightMusic()`, and exiting the race resumes both via `ResumeDayNightMusic()`. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Added bus-assisted Warehouse worker deliveries. Warehouse logistics workers now store explicit delivery cargo state (`resource type`, `amount`, `carrying flag`) and may use the local city bus on both the outbound `Warehouse -> service building` leg and the return `service building -> Warehouse` leg when the route is long enough. Service delivery bus rides are fare-exempt (`service pass`) so workers do not pay from personal balance for job travel. Shift interruption while carrying cargo now restores the undelivered unit back to Warehouse and removes phantom bus passengers if the worker was already onboard. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Added local-bus fare handling and bus revenue handoff. Workers now pay `$1` when boarding the city bus; the fare is accumulated in a per-bus `Bank`, workers who cannot pay fall back to walking instead of getting stuck at the stop, and when the local bus returns to Parking at shift end it transfers its entire bank into Parking's `BuildingBank` treasury. Parking quick HUD now shows `Parking Treasury`, and the local-bus quick HUD shows the current bus bank for debugging. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Added explicit encoding-safety rules to both `AGENTS.md` and `ai/README.md`. The repo workflow now formally treats project text files as UTF-8, prefers `apply_patch` for localized edits, forbids unsafe shell rewrites without explicit UTF-8, and requires quick mojibake scans after UI/localization changes.

- 2026-04-23: Fixed visible mojibake/encoding regressions in several player-facing HUDs. Cleaned broken Russian strings in the top time/day HUD, the shared building quick HUD (including Motel/service activity labels and gambling slot text), and the States screen. Also restored the correct Russian display name for `Forest -> Лесозаготовка` in shared location-label helpers. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Expanded the local-bus quick HUD with richer route context. The selected city bus card is now taller and shows not only driver / passenger count / current stop / route leg, but also the next stop target and a compact passenger manifest (`Name -> Stop #N`, capped to a few visible entries with overflow summary). Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Added local-bus runtime selection and worker trip status polish. The active city bus is now a selectable world object with its own quick HUD (driver, phase, passenger count, stop, route leg), and Worker quick HUD now shows bus-trip states such as walking to a stop, waiting at a stop, and riding to a destination stop. `Assembly-CSharp.csproj` was updated to include the new `Assets/Scripts/Runtime/UI/GameBootstrap.LocalBusQuickHud.cs` partial for external `dotnet build` validation. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Added worker local-bus passenger MVP for long trips. Local workers can now choose the city bus for sufficiently distant commutes to production buildings, service visits, and Motel sleep: they walk to an origin stop, wait there, board during the bus stop dwell, ride invisibly inside the existing local route bus, exit at the chosen destination stop, and then resume the original final walk phase. Passenger transitions are logged under `BUS_PASSENGER`. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-28: Disabled the legacy red tutorial highlight system globally. Tutorial highlight predicates now always return false, all tutorial outline roots are force-hidden during tutorial UI updates, persistent highlight flags and assignments were removed, and the legacy `GameBootstrap.Tutorial.ProductionOrbit.cs` implementation was replaced by compatibility no-op stubs so old orbit-HUD/tutorial-production behavior no longer runs. Verified `dotnet build Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and touched-file mojibake scan.

- 2026-04-28: Added targeted `BUILD_ROAD` debug logging for player road construction. The log now records single-click attempts/results, single-road rejects with block reasons, two-lane footprint resolution/build/reject details, Shift path start/finish/cancel, path cell lists, skipped path cells, and turn-fill build/no-op summaries. Preview frames are intentionally not logged to avoid debug spam. Verified `dotnet build Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and touched-file mojibake scan.

- 2026-04-28: Fixed Vacancy Assignment step progress display so non-applicable steps are hidden from the progress row instead of merely greyed out. Non-truck vacancies now show Vacancy -> Shift -> Worker, while the Truck step is visible only for Truck Driver vacancies. Verified `dotnet build Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and touched-file mojibake scan.

- 2026-04-28: Refined the Vacancy Assignment flow so every vacancy now includes an explicit Shift step, including single-shift production/warehouse/intercity vacancies. The Truck step remains applicable only to Truck Driver vacancies, while single-shift vacancies show one confirmable shift option and then proceed to worker selection without changing assignment business logic. Verified `dotnet build Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and touched-file mojibake scan.

- 2026-04-28: Improved the Vacancy Assignment HUD as a clearer step-by-step flow. The right panel now shows a Vacancy / Shift / Truck / Worker progress indicator, a persistent assignment context card, compact success feedback after assignment, calmer close-button styling, badge-style availability labels, stronger selected-vacancy styling, and card-like selectable rows for shifts/trucks/workers. Worker education is now surfaced in worker options and a reusable education requirement check is wired into vacancy filtering, while current vacancies remain Basic to preserve existing gameplay. Verified `dotnet build Assembly-CSharp.csproj -nologo`, line-count check, `git diff --check`, and touched-file mojibake scan.

- 2026-04-23: Fixed premature idle/life-cycle release for local bus drivers during shift handoff. While `NeedsShiftEndReturn` is set, bus drivers are now excluded from idle wandering and idle conversations, so they stay logically busy until the bus actually completes `ReturningToParking` and `CompleteBusDriverShiftReturn()` starts post-work life-cycle. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Fixed a local-bus route startup race. Bus drivers no longer re-enter `ToParkingForShift` after already boarding the local bus: `UpdateDriverShiftPreparation()` now skips workers already controlling an active local bus route, and `StartBusDriverShiftCommute()` has a guard/log line to prevent accidental driver-object reactivation over an active bus. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Fixed bus-driver shift assignment availability. The grouped Bus Driver card now allows separate workers to be assigned to all three shift rows; UI availability no longer incorrectly blocks candidates just because they are idle-walking/resting, while a worker already assigned to one bus slot is not offered for another slot until removed. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Reworked the bus-driver assignment UI in Shifts/Assignments to match the Warehouse-style grouped layout. Bus Driver now renders as one logistics card with a shared title/summary and three compact shift rows (Morning / Evening / Night), each with its own assign/remove controls, while keeping the existing per-shift runtime logic intact. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Expanded bus action telemetry in `debug.log` for easier route debugging. Added `BUS_SHIFT` transition logs for local-bus boarding blocks (with reason and anti-spam dedup), successful boarding/awaiting shift window, route-cycle start from Parking, next-stop transitions (ascending/descending leg), and route-start failures due to missing stops/Parking. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Added local bus passenger attribute to runtime data (`PassengerCount` with max capacity `5`). The value is now surfaced only in bus micro HUD context (driver quick HUD while `On Bus Route`: `Passengers X/5`) and not shown in the Shifts assignment panels. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Fixed local bus visual orientation. Local-route bus rotation now uses model-axis-aware facing (`+X` as bus nose) via `Quaternion.FromToRotation(Vector3.right, direction)` instead of generic `LookRotation`, so buses move along the road instead of appearing sideways. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Reworked bus-driver assignments to match the intended 24h model. The old single `Bus Driver` slot was replaced by three dedicated bus-driver slots tied directly to Morning / Evening / Night, each slot auto-assigns its matching shift hour, and bus drivers are no longer mixed into the normal local-delivery shift cards. Runtime role tracking now uses three bus-driver slot ids instead of a single `busDriverId`. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Added clearer bus-driver UX for the reserved-but-no-shift case. Assigning a worker to the Bus Driver slot now writes an explicit `BUS_SHIFT` explanation to `debug.log` if no shift is assigned yet, the Bus Driver slot HUD tells the player that Morning/Evening/Night still must be chosen, and worker quick HUD shows `Bus Driver: no shift` / `Водитель автобуса: смена не назначена`. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Expanded local bus debug logging for route-end analysis. `debug.log` now records `BUS_SHIFT` entries when a bus driver is marked for finish-cycle return, when a stop performs the finish-cycle decision, when the route cycle is considered complete, and when the bus starts the actual return-to-parking segment. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Refined local bus shift-end behavior. When a bus driver's shift ends mid-route, the bus no longer aborts toward Parking from the next stop; it now finishes the current full route cycle, then returns to Parking and completes after-work flow. For the current MVP this means the bus parks only after reaching Stop #1 on the descending leg, or after the single stop if only one local stop exists. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-23: Fixed a bus-driver shift assignment regression. Assigning a logistics shift to the worker reserved in the Bus Driver slot no longer clears `busDriverId`, and if that shift is assigned during an active shift window the worker now starts the bus-specific commute to Parking immediately instead of falling back to normal truck commute logic or staying idle. Verified `dotnet build Assembly-CSharp.csproj -nologo` with 0 errors and 0 warnings.

- 2026-04-22: Forest production was reworked into an early Lumberyard MVP. Ambient fake forest workers were disabled; the Forest building now renders as a simple lumberyard/depot; dense-forest misc trees are registered as harvestable runtime trees with daily growth stages 0..5; assigned Forest workers now leave the building, walk to a real misc tree, perform a 5-hit chop loop, trigger a fall, spawn 1..3 world logs on the ground, carry logs back one by one into Lumberyard storage, then replant a sapling that grows over following days. Shift-end logic now also pulls an out-in-the-field lumber worker back into the normal after-work life cycle.

- 2026-04-22: `Assembly-CSharp.csproj` was updated to include the new `Assets/Scripts/Runtime/World/GameBootstrap.Lumberyard.cs` partial so `dotnet build Assembly-CSharp.csproj -nologo` continues to validate the repo outside Unity after adding the new runtime file.

- Keep `ai/work-log.md` short. If it grows beyond roughly 120-160 lines, collapse older completed items into this summary format again.

- Code remains source of truth. The project has many partial `GameBootstrap.*.cs` files; memory is only a navigation aid.

- Prefer `dotnet build Assembly-CSharp.csproj -nologo` after code edits when practical. For memory-only edits, a text/diff check is enough.

- Avoid broad Canvas localization passes immediately after dynamic HUD redraws unless necessary; prior fixes moved several HUDs toward direct localized strings to avoid mixed-language/mojibake-looking output.

- Tutorial flow is sensitive to timing, overlays, highlights, and camera state. When changing tutorial steps, verify OK click behavior, skip behavior, menu highlight cleanup, and whether regular HUDs should be hidden or preserved.

- Quick HUDs should not open automatically after building placement and should close when larger HUD windows open. Fleet details are an exception: preserve Fleet's internal selected-truck state while Fleet is open.
