# Work Log

Last updated: 2026-05-12

Purpose: compact active memory for recent work. Older detailed history was intentionally collapsed on 2026-04-20, 2026-05-03, 2026-05-08, and 2026-05-09 to keep agent startup light. Use git history for exact old implementation details.

## Active Queue

- 2026-05-12: Social simulation backlog items 2-15 are implemented in an MVP form. No active items remain in this queue; future work should harden balance, UI copy, tests, and edge cases rather than redoing the slice from scratch.

## Recent Work

- 2026-05-12: Added the `Assets/Resources/GeneratedAudio/ASMRMustHave` sound set to runtime audio. New clips are loaded from Resources/Editor fallback, exposed in Sound Options, and hooked into build tool start, build complete, road build wave, build denial, daily tax collection, and truck cargo load/unload events. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Fixed persistent white ground grid lines outside Build mode. The always-created `GridLines` world mesh now starts inactive, uses a transparent hidden material state, and is only enabled while the Build panel/tool is active; far-zoom LOD no longer re-enables it during normal gameplay. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Tightened Build menu construction cost badges. Build item cards now give the price badge an explicit top-right RectTransform size so it does not spread over the center icon, render costs as a small gold `$` + amount badge, and keep unaffordable items dimmed while showing the red price instead of replacing it with a generic no-funds label. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Fixed Unity tree OBJ import normal warnings. All six generated `Assets/Resources/Nature/Trees/tree_*.obj` files now include explicit `vn` normals and every face references `v/vt/vn`, so Unity no longer needs to recalculate missing normals during import. Verification: OBJ structure scan, `dotnet build Assembly-CSharp.csproj -v:minimal`, `dotnet build Assembly-CSharp-Editor.csproj -v:minimal`.

- 2026-05-12: Rebalanced world readability after the texture/tree pass. Grid lines now use a very low-alpha calm colour in normal play and switch to bright amber only while the Build panel/tool is active. Ground cells now use world-space macro UVs, larger texture scale, calmer green/sage tints, lower per-cell contrast, forest-edge mixed ground, shoreline transition tinting, softer water-edge wash/foam, extra sparse shoreline reeds/pebbles, and muted imported/procedural tree leaf tones. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `dotnet build Assembly-CSharp-Editor.csproj -v:minimal`.

- 2026-05-12: Applied tree texture PNGs from `Assets/Resources/Nature/Trees/Textures/` to generated tree assets. `procedural_trees.mtl` now maps bark/leaf materials to the matching low-poly textures, all six OBJ tree models have generated UV coordinates for those texture maps, and `ProceduralTreePrefabGenerator` assigns the same textures when generating Unity prefab/material variants. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `dotnet build Assembly-CSharp-Editor.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Added generated/imported tree asset support for world misc trees. Six low-poly OBJ tree models now live in `Assets/Resources/Nature/Trees/`, and `CreateMiscTree` loads `Resources.LoadAll<GameObject>("Nature/Trees")`, instantiates a variant, auto-fits it to the existing tree root height/base, enables shadow LOD registration, and preserves the same root for lumber growth/felling, tree sway, cleanup, and ambient perch points. `Assets/Editor/ProceduralTreePrefabGenerator.cs` can generate prefab/material/mesh variants from Unity's Tools menu, and the old procedural primitive trees remain the fallback when no assets are present. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `dotnet build Assembly-CSharp-Editor.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Integrated imported low-poly world texture PNGs from `Assets/Resources/Art/Textures/` into runtime materials through build-safe `Resources.Load` lookup. Fresh grass, dry grass, forest floor, dirt footpaths, sandy riverbanks, town asphalt, highway asphalt, gravel shoulders, river water, and lake water now replace/fill the procedural material slots with per-cell tint/offset variation; missing assets still fall back to generated textures. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`.

- 2026-05-12: Completed the rest of the social-simulation backlog. Noosphere now aggregates `SocialSignal` into city meaning insights with tone, topic split, recurring reasons, and tension/source summaries. City trust now has formula helpers fed by daily social signals and public promises. Negative signal clusters can generate City Hall public-concern requests, and accepted requests become visible public promises with success/failure trust effects. Money ledger entries now carry account owner/reason fields for city budget, resident wallets, building cashboxes, external, and debug money. Workers gained a citizen/profession layer mapped from contracts. Street litter now tracks dominant sources and residents react to accumulated exposure. Upgrade cards can highlight currently relevant city problems. City Hall request/upgrade UI now routes decisions through runtime command methods instead of owning the simulation logic. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-12: Kept the expanded social/economy/litter work under the project line-count rule by splitting local-bus passenger handling, street-litter cleanup/source helpers, and social runtime models into focused partial files. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`.

- 2026-05-12: Started executing the social-simulation backlog. Runtime models now separate cognition kind as Fact/Experience/Opinion/Rumor: building memories remain factual/neutral, conversation-topic memories are rumors, topic stances are opinions, and daily lived experience/social signals are experiences. End-of-day lived experience now emits capped `DailyExperience` social signals for Noosphere and later topic opinion formation. Added a `ConversationTopic` registry and first topic-association aliases for litter, food, gambling/bar, City Hall/governance, and safety so arbitrary player words can connect to relevant `SocialSignal` categories. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`.

- 2026-05-12: Added the first structured `SocialSignal` layer. Important resident/city events now record topic, tone, strength, confidence, source, place, reason, and day facts through `GameBootstrap.SocialSignals.cs`; thoughts emit non-duplicating signals, street-litter daily exposure creates a litter signal used by "РџРµСЂРµР¶РёС‚С‹Р№ РѕРїС‹С‚", topic opinions record topic signals and can read recent related personal signals, and City Hall complaints/accept/reject/complete/expire decisions emit governance/request signals. The Noosphere HUD and 3D dive now include daily social-signal topic summaries/meaning words. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`.

- 2026-05-12: Added `ai/system-tree.md` as the stable informational hierarchy of project systems, subsystems, feature leaves, and cross-system links. `AGENTS.md`, `ai/README.md`, and `ai/prompt-templates.md` now make keeping this tree up to date an explicit project rule whenever systems, subsystem responsibilities, feature leaves, UI surfaces, or cross-system dependencies change. Verification: `git diff --check`, targeted mojibake scan.

- 2026-05-12: Fixed the new City Hall Requests/Upgrades tabs being visible but effectively unclickable. The tab buttons now get explicit layout height/flexible width, raycast-ready target graphics, non-raycast labels, no navigation capture, and immediate `UpdateCityHallScreenUi()` refresh on click, matching the safer tab setup used by other FleetCanvas screens. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Added MVP city upgrades purchasable from the City Hall. `GameBootstrap.CityUpgrades.cs` defines six trust-gated upgrade cards across Cleanliness, Economy, and Trust branches, tracks purchased upgrades for the current world, records Treasury spending in the ledger/feed, and applies first gameplay effects: lower street-litter growth, larger cleaner coverage radius, construction discounts, longer accepted request deadlines, and softer trust penalties. The City Hall canvas now has Requests/Upgrades tabs; `GameBootstrap.FleetCanvas.CityHallUpgrades.cs` renders the upgrade tree as cards with costs, trust gates, prerequisites, statuses, and buy buttons. Tutorial scenario was checked; this does not change the Tutorial-mode flow yet. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Added graded resident reactions to street litter. Each worker now samples visible street litter in a small radius while outside, accumulates daily litter exposure/peak values, and forms negative active thoughts only when the nearby mess becomes noticeable; every visible exposure still contributes a negative but scaled City factor to the end-of-day "Пережитый опыт" so one dirty cell is a small annoyance while sustained/multi-cell litter can become a major reason. Active cleaner-duty phases are excluded from litter perception while cleaning. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-12: Changed player road construction to an always-on start/end segment flow. Single-lane roads no longer build immediately on the first click or require Shift for path mode: the first left click selects the start and the second left click builds the segment with the existing road construction wave. Road previews now show the pending segment after the start click, two-way road tutorial goals now track start/end selection, and road Build descriptions/tutorial copy no longer teach Shift/R road-building legends. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, JSON parse, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-12: Added construction costs for buildable buildings. `build-catalog.json` now carries per-building `cost` values while roads remain free; the Build menu shows larger/high-contrast cost badges, disables unaffordable buildings, appends cost details to descriptions, and rejects hotkey/world placement attempts when Treasury is too low. Successful building placement now spends Treasury after the placement succeeds, records a money ledger entry/feed event, shows a negative Treasury popup, and spawns the same world spend-float animation used by worker service payments over the built location. Tutorial notes were updated: the required building chain currently costs `$3740` against `StartingTreasury = $5000`. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, JSON parse, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Fixed a resident crowd-separation rescue loop found at the end of `debug.log`. `Doru Muren` was repeatedly pushed from safe cell `(61,20)` into unsafe cell `(61,21)`, then rescued back every frame during idle life updates; pedestrian separation nudges now only apply when the proposed position remains on a safe walk cell and the small segment does not cross blocked walk cells. Inside-building residents are no longer considered nudgeable. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-12: Added a cinematic start-of-day title. Each day rollover now triggers a large centered white `Day N` / `День N` IMGUI overlay that stays visible for 3 real seconds and then fades out without blocking gameplay or changing Tutorial flow. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Fixed mirrored words in the fullscreen Noosphere dive scene. The 3D meaning `TextMesh` billboards now use the camera's forward/up rotation so the readable side faces the viewer instead of rendering the back side. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Fixed Noosphere dive compile wiring after the new fullscreen scene split. The meanings rebuild/apply methods now live in `GameBootstrap.NoosphereDive.Meanings.cs` with the file restored under `Assets/`, and `Assembly-CSharp.csproj` explicitly includes both Noosphere dive partials so `RebuildNoosphereDiveMeanings` resolves during local builds. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-12: Added a fullscreen 3D Noosphere dive mode. Clicking the animated Noosphere panel now fades the regular HUD into a RenderTexture-driven 3D scene with its own camera, pulsing core, orbit rings, connection spokes, and flying word-meanings built from the Noosphere knowledge log, resident topic opinions, and citywide lived experience. Escape, right click, or the close button returns to the regular Noosphere HUD. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`.

- 2026-05-12: Added citywide shared lived experience aggregation for the Noosphere HUD. After resident end-of-day opinions are finalized, `GameBootstrap.CityDailyExperience.cs` builds a capped city-level daily verdict with score, confidence, consensus, tension, dominant reason, and counterpoint from all active residents. The Noosphere summary/visual core now expose the latest city experience, and future arbitrary topic opinions receive a small background mood signal from it. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-12: Added per-resident `WorkerTopicOpinion` for arbitrary player conversation topics. Conversation-topic knowledge now forms a positive/negative stance by combining rumor connotation, source trust, repeated or contradictory versions, deterministic resident disposition, and the latest "Пережитый опыт"; the result is stored by rumor root/original topic, copied back into memory/opinion metadata, propagated through later rumor iterations, and used by topic reflection thoughts. Workers/Noosphere opinion labels now say positive/negative instead of useful/doubtful for these opinions. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-11: Added per-resident end-of-day "Пережитый опыт" summaries. On each day rollover, active residents now receive a capped daily opinion record with final positive/negative tone, score, confidence, dominant reason, secondary reason, and positive/negative counterpoint derived from that day's thoughts, active critical worries, needs, work, money, housing, social, and family state. The Workers -> Thoughts tab now shows the latest "Пережитый опыт" card with the final verdict and explanation. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-11: Reduced noticeable runtime stalls after the recent street-litter/cleaner additions. `SessionDebugLogger` now buffers log lines and writes them in batches instead of appending to `debug.log` synchronously on every event. Street-litter scans now cap their game-speed scaling, reuse scratch collections, prune tracked cells, and cleaner target selection pathfinds only the top cheap candidates instead of every visible litter cell in range. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Suppressed local-bus runtime noise before the player has built any local stops. Worker travel now checks for a viable stop pair before logging unavailable bus service, bus drivers do not commute/board for an empty local-bus network, route-start/no-stop cases stay silent when `localStops.Count == 0`, and local-bus passenger skip logging ignores the zero-stop state. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Fixed the Parking shift-commute loop from `debug.log`. Truck/intercity drivers now wait on safe cells around the Parking road access instead of targeting truck-slot cells inside the Parking footprint, trade commutes abort cleanly if no safe pedestrian path exists, critical needs can interrupt pre-shift preparation when there is more than 30 in-game minutes before the shift, and future `debug.log` sessions are written as UTF-8 with BOM for safer Cyrillic viewing. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Made street litter appear more actively. The litter scan now runs faster, the crowd/activity threshold is lower, touched cells get a small base gain once eligible, spillover to neighboring cells is stronger, footpath/trash-can multipliers are higher, visible/stage thresholds are lower, quiet decay is slower, and the visible-cell cap is higher. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Fixed Forest workers getting trapped at the Lumberyard exit after adjacent building buffers sealed their entrance cell. `debug.log` showed Raul Pemmick and Villem Kvelj ending Forest field work at `(44,32)` and failing every post-shift life path with `startLocation=True`; the new Furniture Factory buffer had blocked all cardinal exits from the Forest access cell. Building walk buffers now leave cells adjacent to any building entrance/access open so one building cannot seal another building's door. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Added the Cleaner profession and Cleaning Depot service building. The build catalog, build fallback UI, unlock progression, placement, decorations, staffing screens, Labor Exchange, worker knowledge labels, quick HUD, and localization now understand `CleaningDepot` / `Cleaner`. On-shift cleaners reserve visible street-litter cells inside the depot coverage radius, walk out, sweep the target, clear it, and return or continue to the next target. The same green coverage ring is shown in build preview and when the depot is selected. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Added crowd-driven street litter visuals. A new `GameBootstrap.World.StreetLitter.cs` partial periodically samples active resident positions, accumulates litter only where multiple people gather around roads, stops, entrances, trash cans, or established footpaths, and spawns small collider-free low-poly papers/wrappers/cans/stains in capped visual stages. New worlds reset the litter system, roads/build sites clear litter underneath, and the owner map now lists the new world partial. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Added and tuned the road construction wave for player-built roads. Newly placed road cells are temporarily hidden from road visuals, road blueprints are cleared immediately after confirmation, and a textured transparent wave travels from the confirmed endpoint back to the start; follow-up tuning removed the pale leading wash, kept the motion slower than the original fast pass but slightly faster than the first tune, made the crest taller/thicker, and reveals the real road texture just before the crest reaches each cell. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Made right-click cancel Build mode like Escape. The right-click cancel check now runs before camera drag, road removal, and road placement input, and primes right-mouse drag state so the same click exits building state without also panning the camera or removing a road. Escape log text is preserved, while right-click gets its own input/debug log source. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Extended the three-second cartoon construction animation from imported Bar buildings to imported Gambling Hall buildings. The construction coroutine is now shared by imported service buildings, Gambling Hall placement starts it after `CreateLocation`, casino/neon/slot/jackpot/chip/card/dice parts are staged as late decorative pops, and Gambling Hall dust puffs use a purple-tinted construction cloud while lights still pulse on near the finish. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Fixed residents getting stuck at imported Bar doors after service visits. `debug.log` showed repeated `rescued from unsafe walk cell` loops for Bas Renne, Vera Trant, Nare Faroul, and Marta Muren around Bar footprint/buffer cells after `exited Bar interior`; driver safe-stand checks now reject normal building footprint cells while still allowing external access cells, stops, and City Park. Imported Bar/Gambling Hall visit and exit positions now resolve door markers to a safe outside stand point before walking or spawning the resident back into the world. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Added a three-second cartoon construction animation for player-built Bar buildings. `TryPlaceBarAtAnchor` now starts a Bar-specific construction coroutine after creating the location; visible renderer parts rise from a squashed/drop pose in staged waves, lower/foundation parts appear first, roof/decor/sign/window pieces pop later, dust puffs animate around the footprint, lights pulse on near the finish, and imported Bar doors get a short final open request. Procedural Bar fallback pieces now have explicit `Bar*` object names so the same animation can group them by part. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Fixed residents getting trapped at bus stops after building walk buffers were added. `debug.log` showed the first four arrival-bus workers (Matis Petrosyan, Egor Mkrtchyan, Aldor Faroul, Doru Clauber) failing safe-walk paths from Intercity Stop cells `(45,2)` and `(46,2)` with `startLocation=True`, because the one-cell buffer around the stop blocked their exit. `LocationType.Stop` and `LocationType.IntercityStop` are now exempt from the building walk buffer, stop placement no longer clears surrounding footpaths as buffer cells, and local Stop build preview no longer shows the buffer ring. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Fixed a midday performance collapse caused by repeated worker service-walk path retries after building walk buffers were added. Building entrance cells are now excluded from every building buffer, zero-step fallback walk targets are rejected, and halted service walks apply the matching worker need retry cooldown instead of instantly selecting the same blocked service again. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Added a one-cell non-walkable buffer around buildable buildings while keeping each building's own entrance cell open. Resident walk pathing, idle fallback targets, and visible footpaths now avoid the buffer; new building placement rejects existing buffer and entrance cells; placement preview shows the buffer as an orange build-mode ring around the footprint while City Park remains buffer-free. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Fixed imported Bar/Gambling Hall visible seating reservations. Imported visible seat detection no longer treats stool/chair legs, backs, posts, or supports as seats, de-duplicates nearby seat anchors, rejects occupied duplicate physical seats during reservation, prunes stale seat occupants, and raises visible-seat sitting roots so residents sit on top of the seat surface instead of inside it. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Made the Noosphere HUD open as a full-screen management overlay. `NoosphereWindowRoot` now stretches across the screen instead of using the old centered `1120x650` panel, with wider content padding and a larger visual panel so the in-window Noosphere animation scales with the full-screen layout. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Removed extra side lane marking decals from the flattened edge highway. The highway texture overlay now keeps only the central dashed line above the asphalt while preserving the flat shoulder/asphalt mesh quads and sampled-terrain placement. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Flattened road visuals into texture-like ground overlays. Built local roads now render as near-ground textured meshes instead of raised slabs, with markings kept as slightly higher decals; the edge highway was converted from stacked cube tiles into flat shoulder/asphalt/stripe mesh quads that follow sampled terrain. Gameplay road cells, pathing, vehicle lifts, and Tutorial flow were unchanged. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `dotnet build Assembly-CSharp-Editor.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Removed Intercity Stop from local road-network access requirements. `LocationType.IntercityStop` no longer auto-creates a road-access cell, no longer gets the disconnected-road world marker/microHUD warning, and world-generation smoke tests no longer require generated road-chain buildability to `layout.BusStop`; local bus stops still require roads. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `dotnet build Assembly-CSharp-Editor.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Added road-network warning information to building microHUDs. Required-access buildings now replace the regular quick status with a red `No road connection` / `Не подключено к дороге` warning and action hint while disconnected; Warehouse's custom microHUD shows a compact red `No road` / `Нет дороги` status. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan.

- 2026-05-11: Made required road-access warning markers much more noticeable. Disconnected required buildings now show a larger billboarded red/yellow diamond marker above the roof with an exclamation and broken-road glyph, plus a subtle unscaled bob/pulse animation while the warning is active. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Fixed imported service doors rotating around their mesh center instead of a side hinge. Imported Bar/Casino door registration now wraps door meshes in a generated `HingePivot` placed on the hinge-side renderer edge, chooses the opposite side from a nearby knob/handle when available, uses the outer edge for double doors, and animates the pivot while preserving the original door mesh. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-11: Fixed imported Bar/Gambling Hall visible-seat placement so seated residents no longer sink into stool/chair meshes. Visible `Stool` / `Chair` / `Bench` seat anchors now place the driver root above the renderer top instead of at the renderer bounds center, preserving the existing sitting pose while avoiding cushion clipping. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-11: Fixed residents getting stuck in walking animation after idle-footpath congestion reroutes. `debug.log` showed 246 `built walk path for IdleWander` entries, with the same residents rebuilding identical routes dozens of times while still visually walking. Idle overlap reroute cooldown now uses real time, and a still-blocked reroute attempt cancels the idle walk into a short pause, resets `WalkTargetWorld`, zeroes `WalkAnimationTime`, and reapplies the neutral pose so residents stop walking in place. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-11: Improved the procedural resident footpath texture. The texture now has a wavy packed center, subtle alternating step marks, frayed grassy edges, straw/dust variation, damp speckles, and small pebble noise; footpath cell materials use a warmer staged tint and fuller texture scale. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-11: Softened resident footpath routing after `debug.log` showed repeated `IdleWander` path rebuilds into the same small cell clusters. Footpaths now form much slower, apply a weaker pathfinding discount, add dynamic pedestrian congestion cost to occupied/adjacent cells, and idle walkers try to reroute around overlap before pausing. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-11: Refined Build menu hotkey toggling. If an item tray is open and the pressed number does not match a visible item, pressing the already-focused category number now unfocuses that category, clears the active build tool, and returns to the category layer. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Relaxed New Game third-layer build unlock prerequisites. The second-layer optional service buildings `Canteen` and `Bar` remain unlockable/buildable, but no longer block the next tier; tier three now requires the core chain `Parking`, `LaborExchange`, `Forest`, `GasStation`, and `Sawmill`. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Fixed Build menu hotkey fallback between layers. When a build category tray is open, number keys now activate the matching visible item first; if that item number is not present in the active tray, the same key falls back to selecting the matching visible category in the dock. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Added imported Gambling Hall support using `Assets/Resources/Buildings/casino.fbx`. The Gambling Hall now loads `Resources/Buildings/casino`, fits it with the imported building pipeline, keeps casino roof geometry, skips procedural window glow strips when the model is present, registers imported window/neon/night-light markers, opens imported double doors, and can keep visitors visible on imported casino sit/play markers instead of always hiding them inside. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Fixed the imported Bar interaction follow-up. The door now swings the opposite way, service visits remember the concrete service instance during the trip, and imported Bar seating prefers visible `Stool_*_Seat` / `Chair_*Seat*` meshes with renderer-bounds positions before falling back to `P_TableSeat_*` markers. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Added the first imported-Bar interaction layer. The Bar FBX now records door, entry, visitor, table look-at, and table-seat markers; leisure visitors walk to the imported door marker when it is path-safe, the door opens/closes with a timed animation, and visitors occupy visible `P_TableSeat_*` chairs when available before falling back to the old hidden interior mode. Seat reservations are released on exit, interruption, or demolition. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Hooked imported building window/light metadata into the existing night-light system. The Bar FBX now registers `Window` + `Glass` renderers as night-tinted window materials, creates invisible point lights from window bounds or explicit `P_WindowGlow` markers, and creates invisible night point lights from imported `P_*Light*` markers such as lantern points; procedural Bar glow strips are skipped when the imported model is present. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Fixed pending worker knowledge formation crashing when citywide canonization removes pending entries during formation. The pending-knowledge update loop now tolerates the list shrinking mid-pass and removes the exact pending object only if it is still present. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Made Escape step through Build menu layers. When the Build menu is open on the item tray, `Esc` now clears the selected category and active build tool, returning to the category layer; pressing `Esc` on the category layer closes the Build menu. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Removed the world-space `CategoryStripe` slabs from generated buildings. The blue blocks seen through service buildings were the old service/production category indicator cubes, so location creation no longer spawns them. The imported Bar fit was enlarged from `1.85` to `2.45` footprint fill, and the procedural fallback Bar was scaled up as well. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan.

- 2026-05-11: Added Build menu numeric hotkeys and visible numbering. Visible unlocked category and item cards now show `N.` prefixes based on current display order; while the Build menu is open, number keys and numpad keys select the Nth category first, then activate the Nth visible tool in the opened category. Unlock changes naturally rebuild labels and hotkey targets during the next Build menu redraw. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-11: Tightened the imported Bar visual fit. The Bar FBX now hides roof/platform renderers by broader `Roof*` transform names, `Awning` meshes, and roof/base material names, not only `Roof_` prefixes, and its footprint fill was raised to match the larger low-poly building scale. The procedural fallback Bar was also enlarged so it does not look tiny if the FBX cannot be loaded. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-10: Made building light-source visuals invisible while keeping actual lights active. Procedural building night lamps now hide their sphere renderer, a shared building visual pass disables renderer-only light source markers such as bulbs/flames/imported `P_*` helper points, and every current or future `CreateLocation` building runs the pass after its decorations are assembled. Imported Bar light/helper markers are hidden before bounds fitting so they no longer affect visible sizing. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-10: Replaced the Bar world decoration path with an imported Resources model hook. `Assets/Resources/Buildings/bar.fbx` is loaded as `Resources.Load<GameObject>("Buildings/bar")`, auto-fitted to the existing Bar footprint, oriented toward the anchor/front, and given the existing warm service light; the old procedural Bar remains as fallback if the asset is unavailable. The imported Bar now hides the oversized `Roof_*` and `Base_Grass_Tile` platform renderers before bounds fitting, scales the remaining model to nearly the full footprint, and hides the game-side Bar base renderer so the model is not sitting on a visible plinth. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`.

- 2026-05-10: Added resident footpaths as direct ground-cell material changes. Walking residents now add wear to each traversed ground cell, visible footpath cells swap the existing `Ground_x_y` tile to a procedural footpath texture, and pedestrian pathfinding uses weighted cell costs to prefer established paths. Roads and new building footprints clear any footpath state underneath them. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, project-file include scan, and targeted conflict-marker scan.

- 2026-05-10: Narrowed road-access requirements to production/truck-chain buildings plus stops. Parking, Gas Station, Forest, Warehouse, Sawmill, Furniture Factory, Docks, local Stop, and generated IntercityStop keep required road access; service, residential, civic, park, kiosk, car-market, kindergarten, and labor-exchange buildings no longer auto-create driveway roads, no longer auto-switch the Build tool to Road, and no longer expose driveway preview/text. Required buildings now show a red world warning marker while their access road cell is not connected to a neighboring road/highway. Tutorial copy/scenario were intentionally left untouched for this pass by user request. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, JSON parse, `git diff --check`, and targeted diff mojibake scan.

- 2026-05-10: Enlarged the night Build cursor assist light. The build preview point light now sits higher, has a wider minimum/maximum range, and scales more aggressively from the active placement footprint so nighttime building placement illuminates a much larger area. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-10: Fixed camera zoom being blocked while the Build menu is open. The Build dock no longer counts as a blocking HUD for mouse-wheel zoom, while actual UI hover targets still block world zoom through `IsPointerOverHud`. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-10: Moved the Noosphere animation inside the Noosphere window. The window is back to its wide layout and now combines a left-side Canvas animation panel with the right-side knowledge log; the animation shows the core, resident nodes, canon links, recent event arcs, and travelling pulses without spawning world objects over City Hall. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted project-file scan, and targeted mojibake scan.

- 2026-05-09: Added citywide Noosphere canon knowledge. When enough active residents have formed the same building or conversation-topic knowledge, it becomes permanent city knowledge, records a canonized Noosphere event, applies to all residents through duplicate checks/thought context/dialogue/UI without filling every personal memory list, and shows as `forever` in Residents Knowledge and Noosphere. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted conflict-marker scan, and targeted mojibake scan.

- 2026-05-09: Removed player framing controls from the first City Hall social-introduction topic scene. The scene now asks only for a conversation topic again; the first rumor/knowledge iteration is recorded with neutral source framing, while rumor connotation is left to the conversation result and later resident-to-resident iterations. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-whitespace scan, and targeted mojibake scan.

- 2026-05-09: Added rumor-state tracking for resident conversation knowledge. Conversation-topic memories now keep the original iteration-1 topic separately from the current rumor text, track distortion percentage, connotation score/confidence, and carry a stable rumor root through pending knowledge, resident sharing, opinions, the Residents Knowledge tab, and Noosphere logs. Each new resident-to-resident iteration drifts distortion and connotation, with a deterministic chance to polarize to 100% positive or 100% negative. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-whitespace scan, and targeted mojibake scan.

- 2026-05-09: Added player framing for City Hall conversation rumors. The social-introduction scene now lets the player frame a topic as supportive, neutral, or a warning; that framing travels through social memory, pending knowledge, resident opinion formation, copied knowledge, the Residents Knowledge tab, and Noosphere logs separately from the conversation success/failure result. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-whitespace scan, and targeted mojibake scan.

- 2026-05-09: Added staged resident knowledge formation. New facts now enter `PendingKnowledge`, move through heard/compare/judge stages, then become personal memory only after an opinion tone, score, confidence, and reason are formed. Building discovery, City Hall conversation topics, resident sharing, Residents Knowledge UI, and Noosphere logs now use the formed opinion path. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-whitespace scan, and targeted mojibake scan.

- 2026-05-09: Added delayed resident thought formation. Active warnings and knowledge reactions now create pending thought candidates first, mature over in-game time, cancel if the cause disappears, and enrich final text with relevant active knowledge such as known service/work locations or newly learned conversation/building knowledge. The Workers Thoughts HUD now shows a `Раздумывает` / `Осмысляет знание` current state while a thought is still forming. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-whitespace scan, and targeted mojibake scan.

- 2026-05-09: Refreshed AI memory after the resident knowledge/Noosphere work. `project-overview.md` now treats knowledge sharing and Noosphere as stable project areas, `architecture-notes.md` documents the `GameBootstrap.WorkerKnowledge*` seam, and `release-notes.md` tracks the player-facing Knowledge tab, spreading memories, and Noosphere HUD for `v.0.0.4`. Verification: `git diff --check` and targeted mojibake scan.

- 2026-05-09: Made residents with active conversation-topic knowledge more likely to initiate idle dialogue themselves. Idle conversation start chance now gets a source-side bonus when the initiating worker holds a non-expired `ConversationTopic` memory; building-existence knowledge does not affect initiation. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-whitespace scan, and targeted mojibake scan.

- 2026-05-09: Fixed City Hall topic knowledge being hidden behind building transfers. Player-prompted City Hall conversations now suppress the generic pre-topic knowledge share, create the requester topic memory first, then transfer that exact topic to the target as iteration 2 with the City Hall interaction reason. General resident knowledge sharing now prefers conversation-topic candidates before building-existence memories. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-whitespace scan, and targeted mojibake scan.

- 2026-05-09: Highlighted knowledge subjects inside live dialogue bubbles. Idle knowledge-share bubbles now render City Hall topics and building-existence memories as bright rich text, with tag-safe word reveal/wrapping. The Bar interior scene now prefers active resident knowledge lines with highlighted topics/buildings and falls back to its old ambient phrases when no active knowledge is available. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-whitespace scan, and targeted mojibake scan.

- 2026-05-09: Added per-memory knowledge iterations. Direct building knowledge starts at iteration 1, City Hall player-topic memories assign iteration 1 to the requester and 2 to the target, and every resident-to-resident transfer creates a separate copied memory at source iteration + 1 without changing duplicate matching. Residents Knowledge and Noosphere now display the stored iteration snapshot. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-whitespace scan, targeted mojibake scan, and targeted iteration wiring scan.

- 2026-05-09: Extended resident knowledge sharing beyond idle bubbles. Service co-presence, coworker shifts, City Hall social introductions, and family formation can now transfer one active non-duplicate knowledge item, with source reasons that name the interaction type for resident Knowledge and Noosphere logs. Worker arrival bus waves no longer create social memories, and the old `ArrivalWave` social kind was removed. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-whitespace scan, targeted mojibake scan, and targeted sharing/arrival wiring scan.

- 2026-05-09: Added idle knowledge sharing between residents. Idle conversations now first try to transfer one active, non-expired knowledge item the partner does not already know, using building/topic placeholder bubble templates; existing idle bubbles remain the fallback when no shareable knowledge exists. Shared memories get a fresh 48h timer, resident Knowledge/Noosphere source reasons, and duplicate knowledge does not refresh timers. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-whitespace scan, targeted mojibake scan, and targeted sharing wiring scan.

- 2026-05-09: Added a visible Noosphere knowledge timer indicator. Each knowledge-log row with a timed memory now shows a right-side TTL label, shrinking color bar, and remaining hours; burned/expired rows show a depleted red state. The indicator is decorative and does not intercept scroll/close clicks. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted trailing-whitespace scan, and targeted mojibake scan.

- 2026-05-09: Added resident building-existence knowledge. First use of a concrete building records a 48h `BuildingExistence` memory with source reason, shows it in the resident Knowledge tab and Noosphere, and does not refresh the timer on repeat visits. Sources include service interiors, truck stops, work shifts, kiosks, parks, homes, bus stops, and City Hall requests/complaints. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted diff mojibake scan, and targeted no-refresh wiring scan.

- 2026-05-09: Added the global Noosphere HUD as a citywide knowledge event log. The top menu now opens a Canvas list of received and burned resident knowledge events with names, topics, event time, outcome, and reason; knowledge creation, expiry, and memory-cap trimming feed the log. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan, and targeted clickability wiring scan.

- 2026-05-09: Added 48 in-game-hour expiry timers for Residents `Knowledge` conversation-topic memories. New topic memories store `ExpiresWorldHour`, existing topic memories derive expiry from their creation hour, runtime prunes expired memories, and the Knowledge tab shows remaining hours plus a shrinking yellow-to-red timer bar. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan, and targeted clickability/timer wiring scan.

- 2026-05-09: Highlighted player-entered City Hall social-conversation topics in bright rich text both inside the Residents `Knowledge` tab and in the City Hall dialogue/result scene. The input field stays plain text, while dialogue/body/result renderers explicitly support rich text. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, targeted mojibake scan, and targeted clickability/rich-text wiring scan.

- 2026-05-09: Added a dedicated `Knowledge` / `Знания` tab to the Residents detail HUD. It reads persistent `DriverAgent.Memories`, currently showing City Hall social-introduction conversation topics with participant, outcome, and day/time rows. The new tab is wired into the existing Residents tab click-target validation and the new partial/project file/owner map were synced. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, targeted clickability wiring scan, `git diff --check`, and targeted mojibake scan.

- 2026-05-09: Compacted AI memory after reviewing `ai/`. `work-log.md` now keeps only fresh details plus grouped summaries; `release-notes.md` and in-game Patch Notes mention persistent City Hall social-conversation memory; `systems-map.md` metadata matches the latest map touch. Verification: JSON parse, `git diff --check`, and targeted mojibake scan.

- 2026-05-09: Reused the existing F9 `SOCIAL REQUEST` debug command for City Hall social-introduction requests. The command now requires City Hall, blocks while another social scene/request is active, files a normal City Hall complaint, and no longer launches the dialogue scene directly; the scene still starts through the regular accept flow. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, and `git diff --check`.

- 2026-05-09: Implemented persistent resident memory for player-prompted City Hall social conversations. The sanitized topic entered in the special dialogue scene is now stored in both participants' personal memory lists, mirrored into their social-memory context, and shown through a new `Я узнал что-то новое` resident thought instead of the generic pleasant-talk thought. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, and targeted mojibake scan.

- 2026-05-09: Bumped the project version to `v.0.0.4`: Main Menu shows `Lo-fi Delivery Co. v.0.0.4`, in-game Patch Notes/release-notes use `v.0.0.4`, and Unity `PlayerSettings.bundleVersion` is `0.0.4`. Verification: JSON parse, old/new version search, targeted mojibake scan, `git diff --check`, and `dotnet build Assembly-CSharp.csproj -v:minimal`.

- 2026-05-09: Audited `v.0.0.3` Patch Notes against recent implemented work and refreshed them as `v.0.0.4` player-facing notes. The in-game notes now cover staged New Game building unlocks, City Hall construction requests, resident life systems, service/trade progression, compact/tethered microHUDs, Motel guest submenu, Bar interior/audio, regional map, Tutorial flow, and atmosphere fixes while leaving out CI/editor/debug-only details. Verification: JSON parse, `git diff --check`, and targeted mojibake scan.

- 2026-05-09: Reworked the Warehouse building microHUD into a dedicated compact logistics layout. The quick HUD now heads the panel as `Склад`, shows a short Russian storage/logistics description, status/loaders/logistics rows, five warehouse resource counters, keeps the worker section as `Грузчики`, and preserves the existing `Открыть ресурсы` action. Added `GameBootstrap.BuildingQuickHud.Warehouse.cs` and updated the owner map/project file for the new partial. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, and targeted mojibake scan.

- 2026-05-09: Fixed idle life-cycle restart spam and follow-up idle overlap cases from `debug.log`. Due-need idle fallback now respects overlap pauses, advances wander targets instead of rebuilding the same path every frame, and allows proposed idle steps that increase separation when workers are already visually overlapping. Verification: `dotnet build Assembly-CSharp.csproj -v:minimal`, `tools/check-line-count.ps1`, `git diff --check`, and targeted mojibake scan.

## Recent Summary

- 2026-05-08: Reworked compact building and resident microHUDs. City Hall, Motel, Warehouse, worker quick HUDs, guest submenu overlays, map tether lines, Event Feed layering, scroll/click behavior, and close/focus handling were tightened for the current UI style.

- 2026-05-08: Rebuilt the Build experience around the bottom dock. The Build menu now uses category icons and an animated tray, supports outside-click closing and category unfocus, keeps tray/category containers transparent where needed, and New Game unlocks buildings in staged layers instead of exposing everything after tutorial skip.

- 2026-05-08: Added and polished the large Bar interior scene. The Bar quick HUD can enter a separate paused interior with fade transitions, room ambience, patron idle/talk/drink/dance animation, readable TextMesh signs/bubbles, spatial vocalizer audio, and safer teardown/audio listener handling.

- 2026-05-08: Hardened worker idle placement and dialogue from `debug.log` review. Idle wandering, conversations, Motel idle slots, stationary idle actions, and overlap checks now reserve planned/current positions more carefully and avoid same-cell stacking.

- 2026-05-08: Updated Tutorial and staffing memory around the current player path. Normal play no longer has a permanent Vacancies top-HUD entry; Tutorial uses contextual Staffing/Labor Exchange flow, and `ai/tutorial-scenario.md` records the current `Обучение` sequence.

- 2026-05-08 to 2026-05-06: Expanded resident simulation. City Hall requests/trust, social-introduction requests, idle dialogue, Residents HUD, thoughts/opinions, social graph, inventory/Kiosk, families/children, Kindergarten, worker migration, contracts, needs, money, and worker focus were added or tuned.

- 2026-05-06 to 2026-05-04: Continued transport/economy/system work. Trade services, schedule helpers, Docks/regional trade, Labor Exchange automation, service staffing, personal cars, resource simplification, audio/options, Event Feed, demolition, and runtime workflow rules were implemented or hardened.

- 2026-05-03: Completed broad 900-line runtime partial cleanup. Oversized `GameBootstrap` partials were split by concern, owner map and architecture notes were refreshed, line-count tooling/CI default to 900 lines, and editor smoke tests cover road build, transport/trade, vacancies, and tutorial goals.

- 2026-05-03: Started moving data/text out of large UI C# files. `Assets/Scripts/Runtime/Data/` loaders and JSON catalogs now back Patch Notes and Build menu content first, with C# fallbacks kept.

- 2026-05-02 to 2026-04-06: The project evolved from a runtime-generated grid logistics prototype into the current town-sim slice with tutorial, workers/needs, production, local buses, regional trade, racing, water/ambient life, audio/options, and many extracted service seams.

## Active Notes / Watchouts

- Keep `ai/work-log.md` short. If it grows beyond roughly 120-160 lines, collapse older completed items into this summary format again.

- Code remains source of truth. The project has many partial `GameBootstrap.*.cs` files; memory is only a navigation aid.

- Use `ai/systems-map.md` -> `System Owner Map` as the first navigation pass for implementation, bugfix, refactor, and investigation tasks.

- When changing files listed in an owner card, update `ai/systems-map.md` only if paths, ownership, or responsibilities changed.

- Prefer `./tools/check-all.ps1` before commits or after risky code edits. Use `-SkipSmokeTests` for a fast local pass when Unity is already open or unavailable.

- For memory-only edits, a text/diff check is enough; no Unity build is needed unless code changed.

- Avoid unsafe shell rewrites for localized files. Prefer `apply_patch`, keep text UTF-8, and scan touched localized UI/HUD/tutorial files for mojibake markers after edits.

- Avoid broad Canvas localization passes immediately after dynamic HUD redraws unless necessary; several HUDs intentionally use direct localized strings to avoid mixed-language output.

- Tutorial flow is sensitive to timing, overlays, highlights, goals, and camera state. When changing tutorial steps, verify OK click behavior, skip behavior, menu highlight cleanup, and whether regular HUDs should be hidden or preserved.

- Quick HUDs should not open automatically after building placement and should close when larger HUD windows open. Fleet details are an exception: preserve Fleet's internal selected-truck state while Fleet is open.

- After adding or changing HUD/Canvas buttons, explicitly verify clickability: Canvas has an EventSystem/GraphicRaycaster, every `Button` has a `targetGraphic` and listener path, text/decorative images do not block raycasts unless they are the button target, overlay close buttons are last-sibling but small, and the new panel is included in `CloseAllMenus`/blocking-HUD state.
