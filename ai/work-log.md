# Work Log

Last updated: 2026-04-06

Purpose: short task memory for active and recent implementation work.

- 2026-04-06: Initialized shared AI memory structure for this repository. Added `AGENTS.md` and the base `ai/` memory files with generic workflow, planning, and template rules for future AI-assisted work.
- 2026-04-06: Built the first playable transport prototype in `SampleScene`. Added a runtime bootstrap that creates a low-poly 2.5D scene with a visible grid, click-to-place roads, `Forest`, `Warehouse`, and `Town` locations, one truck moving smoothly cell-to-cell on connected road tiles, automatic `wood` production and transfer (`Forest -> Warehouse -> Town`), and minimal on-screen UI for mode and resource counts. Files: `Assets/Scripts/TransportPrototypeBootstrap.cs`, `Assets/Scenes/SampleScene.unity`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Fixed road placement input for projects using the new Unity Input System. `TransportPrototypeBootstrap` no longer reads mouse state through legacy `UnityEngine.Input`; it now uses `UnityEngine.InputSystem.Mouse.current` for click detection and cursor position when casting to the grid. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added basic camera controls to the transport prototype. `TransportPrototypeBootstrap` now supports camera pan via `WASD`/arrow keys and right-mouse drag, plus zoom via mouse wheel while keeping the same fixed 2.5D viewing angle. Road placement also ignores input while right-mouse panning is active to avoid accidental road drops. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added a dedicated `Parking` location as the truck base. The truck now spawns and waits at Parking, route planning treats Parking as the required route origin, and after deliveries the truck returns there before starting the next trip. The scene also gained a simple parking-canopy placeholder so the truck reads as being housed inside a real depot area. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added a simple root project description in `README.md` summarizing the prototype loop, active locations, and current scope. File: `README.md`.
- 2026-04-06: Reworked truck motion to feel slower and more natural. The transport prototype now moves the truck through eased cell-to-cell segments instead of constant-speed snapping, with softer turning, light suspension bob, body roll/pitch, and animated wheel spin/steer so trips read more like a small vehicle drive than a sliding marker. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Tuned down the truck's turn exaggeration after the motion pass made corners read like visual drifting. Increased body rotation response, reduced steering range, lowered pitch, and changed body roll to be a light response to steering instead of a large lag-based lean. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added parking selection HUD and building highlight. Clicking the `Parking` building now selects it instead of placing a road, shows a dedicated HUD panel listing trucks currently inside the parking area, and renders a clear world-space selection highlight over the chosen building. Clicking elsewhere clears the selection. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added explicit 3-second cargo service animation at buildings. The truck now stops at load/unload points, turns its rear toward the building, spends 3 seconds servicing cargo, and shows a visible moving crate during loading/unloading instead of transferring resources instantly. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Increased the prototype map scale and spread the buildings farther apart. The grid was expanded from `16x16` to `20x20`, and `Parking`, `Forest`, `Warehouse`, and `Town` were repositioned to create a visibly roomier layout with longer road connections between major points. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Fixed the parking HUD's startup truck count. The panel now shows an explicit `inside parking: X/1` count and determines whether the truck is inside by checking the truck's world position against the parking footprint, which makes the initial parked truck read correctly as present from the start. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added starter road generation so the prototype map launches already connected. The scene now automatically lays down a simple initial road network between `Parking`, `Warehouse`, `Forest`, and `Town`, while still allowing the player to add more roads manually afterward. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Disabled the truck's automatic behavior and expanded the parking HUD into a two-layer inspection flow. The truck now stays idle instead of auto-running routes, the parking panel shows a clickable truck icon instead of only a text count, and clicking that icon opens a second HUD layer with details for `Truck #1`. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Fixed the truck-icon click not opening the second HUD layer. World input now ignores clicks that land inside the main HUD, help HUD, parking HUD, or truck-details HUD rectangles, so clicking the parking truck icon no longer gets canceled by scene click handling. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added a first cozy procedural audio pass for menu and world. `TransportPrototypeBootstrap` now generates soft runtime audio clips in code and plays a quiet wind bed, forest rustle, town hum, truck idle/rolling loops, gentle UI select/open/close sounds, and wooden cargo pickup/drop ASMR on truck service events. Files: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Simplified the parking truck icon to fix clickability. The old IMGUI truck icon used overlapping controls on top of the button area; it is now a single clean `GUI.Button("TRUCK")`, so the second HUD layer can open reliably on click. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added a manual available-routes system and assignment flow for the truck. The parking selection now shows a separate `Available Routes` HUD with up to five route cards, the truck detail HUD can assign a concrete trip such as `Forest -> Warehouse` or `Warehouse -> Town`, and the truck now executes the assigned trip step-by-step before returning to `Parking` instead of relying on the old automatic loop. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Fixed route availability and pathfinding for trucks that start inside building footprints such as `Parking`. Pathfinding now treats cells inside the start and goal locations as traversable during route search, which lets the truck correctly exit Parking to the adjacent road and makes available trips like `Forest -> Warehouse` appear as soon as resources exist. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added trip payout calculation and a simple money presentation layer. Available routes now show a computed dollar reward, the scene has a top money HUD that starts at `$0`, and completing a trip now pays out when the truck returns to `Parking`, playing a reward chime and showing a small rising/fading cartoon-style `+$...` popup near the treasury display. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added a diorama presentation pass for the prototype scene. The bootstrap now configures a miniature-style camera, warm stylized lighting, a contained plinth/border under the map, and a runtime URP global volume with subtle bloom, contrast/saturation tuning, vignette, and light depth of field. The project also gained a custom `TiltShiftRendererFeature`, fullscreen tilt-shift shader, and an editor installer that attaches the feature to the URP renderer assets so the scene gets the characteristic sharp center band with progressive blur above and below. Files: `Assets/Scripts/TransportPrototypeBootstrap.cs`, `Assets/Scripts/Rendering/TiltShiftRendererFeature.cs`, `Assets/Shaders/TiltShiftDiorama.shader`, `Assets/Editor/DioramaRendererInstaller.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` and `dotnet build Assembly-CSharp-Editor.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added a basic fuel loop for the truck and a new `Gas Station` building. The truck now consumes `Fuel` while moving cell-to-cell, the HUD shows current fuel, the map includes a connected gas station near `Parking`, and the truck detail panel can issue a manual `Refuel At Gas Station` order from parking. Refueling sends the truck to the station, performs a short service interaction there, fills fuel back to max, and returns the truck to `Parking`. Trip assignment is also fuel-aware now and shows when a route needs more fuel than the truck currently has. Files: `Assets/Scripts/TransportPrototypeBootstrap.cs`, `Assets/Scripts/Rendering/TiltShiftRendererFeature.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Tuned down the diorama pass after it made the prototype too soft and reduced road readability. The URP tilt-shift preset now uses a wider focus band with much lower blur strength/radius, the runtime depth of field/bloom/vignette are lighter, and road tiles were raised and given a slightly stronger contrast so they remain clearly visible in the miniature presentation. Files: `Assets/Scripts/TransportPrototypeBootstrap.cs`, `Assets/Scripts/Rendering/TiltShiftRendererFeature.cs`, `Assets/Editor/DioramaRendererInstaller.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` and `dotnet build Assembly-CSharp-Editor.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Updated the custom tilt-shift renderer feature to support URP RenderGraph. `TiltShiftRendererFeature` now implements `RecordRenderGraph`, creates a temporary destination texture through RenderGraph, blits the effect material there, and swaps `cameraColor` in frame data so the pass works in the current URP pipeline without requiring Compatibility Mode. File: `Assets/Scripts/Rendering/TiltShiftRendererFeature.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` and `dotnet build Assembly-CSharp-Editor.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Fixed a black-screen regression in the RenderGraph tilt-shift path. The custom renderer feature no longer relies on swapping `cameraColor` after post-processing; instead, it now uses a safer two-step `source -> temp -> source` blit sequence inside `RecordRenderGraph`, which preserves the final scene output while still applying the effect. File: `Assets/Scripts/Rendering/TiltShiftRendererFeature.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` and `dotnet build Assembly-CSharp-Editor.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Applied an emergency fallback for the custom tilt-shift renderer feature after RenderGraph regressions continued to produce a black scene. The feature is now disabled by default in code and in the editor installer, so the prototype falls back to the stable diorama presentation from camera setup, lighting, and the lightweight URP volume while the fullscreen blur pass remains in the project for a later reimplementation. Files: `Assets/Scripts/Rendering/TiltShiftRendererFeature.cs`, `Assets/Editor/DioramaRendererInstaller.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` and `dotnet build Assembly-CSharp-Editor.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Replaced the unstable custom tilt-shift hookup with the built-in URP fullscreen renderer feature path. The editor installer now creates a dedicated tilt-shift material asset, disables the legacy custom feature, and attaches/configures a `FullScreenPassRendererFeature` on the URP renderer assets so the existing tilt-shift shader runs through Unity's supported RenderGraph-compatible fullscreen flow. Files: `Assets/Editor/DioramaRendererInstaller.cs`, `Assets/Scripts/Rendering/TiltShiftRendererFeature.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` and `dotnet build Assembly-CSharp-Editor.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Improved road readability after the diorama presentation changes made them effectively disappear into the terrain. Road tiles are now raised higher above the ground, use flat unlit materials instead of lit shading, disable shadows, and include a lighter inset top surface so routes remain visible even with the miniature camera, color grading, and soft focus. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Fixed route detection after the `Gas Station` building was introduced. The starter road generation no longer tries to connect `Parking` directly to `Warehouse` through the gas station footprint; it now builds the initial network as `Parking -> Gas Station -> Warehouse`, which restores a continuous road connection for trip/path checks. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Refined the starting road layout so every core task location is visibly connected from the start. The `Gas Station` was moved slightly upward, and the starter network now begins with an explicit road exit cell outside `Parking` before continuing to `Gas Station`, `Warehouse`, `Forest`, and `Town`. This makes the connection to `Parking` visually clear and keeps the whole initial logistics chain readable and continuous. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added one more explicit starter road cell on the visual approach to `Gas Station`. This closes the last visible gap near the station entrance so the initial road chain reads as fully continuous from `Parking` through `Gas Station` to the rest of the map. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added dedicated driveway connectors for `Parking` and `Gas Station` so the road network reads as visually attached to the buildings instead of stopping at nearby anchor cells. This keeps the existing pathfinding model but closes the remaining visible gaps at the building entrances in the scene. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Simplified the fuel rules for the prototype. The truck still uses `Fuel` with a maximum of `100/100`, but movement now costs `1 fuel` per traversed road cell and route assignment no longer blocks on fuel sufficiency checks. Refuel remains a manual action from the parking HUD, while the current fuel level is still displayed for feedback. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added right-click road removal and reinforced road visual continuity. A short RMB click on a road cell now removes that tile without interfering with RMB camera drag, the in-scene help text reflects the new control, and road tile visuals were widened to overlap more strongly so the network reads as less broken between neighboring cells. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Expanded truck interaction around fuel depletion. The truck can now be selected directly in the world to open its HUD, a simple low-poly driver character exists in the scene, and if fuel reaches `0` the truck stops on the cell where it ran dry. The driver then exits, walks to `Gas Station`, carries fuel back on foot, restores the truck to `100/100`, and the truck resumes the interrupted path or route. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added basic hotkey controls for simulation speed and truck selection. `F1/F2/F3` now switch the game between `1x`, `2x`, and `3x` speed via `Time.timeScale`, the current speed is shown in the HUD, and pressing `1` focuses `Truck #1` by opening its truck HUD directly. The truck labeling in the UI was also normalized around the `Truck #1` display name for easier future scaling to multiple trucks. File: `Assets/Scripts/TransportPrototypeBootstrap.cs`. Verified `dotnet build Assembly-CSharp.csproj -nologo` completed successfully with 0 errors and 0 warnings.
- 2026-04-06: Added repository collaboration defaults for future git work. The repo now includes a Unity-friendly `.gitattributes` for text/binary handling and a lightweight `CONTRIBUTING.md` that documents branch naming, commit style, Unity-specific commit rules, and the recommended validation/push workflow for this project. Files: `.gitattributes`, `CONTRIBUTING.md`.
- 2026-04-06: Initialized git for the local project folder and connected it to the remote repository `https://github.com/VladimirVasin/gruzovichky.git` as `origin`. Added a base Unity-focused `.gitignore` so generated folders such as `Library`, `Temp`, `Logs`, and `UserSettings` are excluded before the first commit. Files: `.gitignore`, `.git/` config. Verified `git remote -v` and `git status --short --branch` show an initialized local repository on `main` with the expected remote configured.
